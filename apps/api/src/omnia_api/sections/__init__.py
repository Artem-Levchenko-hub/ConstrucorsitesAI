"""Shared colour palettes.

The landing-page section catalog (JSON IR + Jinja renderer) that used to live in this
package left with the site builder; the palettes stay because the design tokens of the
agent's design contract are built from them.
"""
