"""Совместимость со старым именем пакета (`omnia_api` → `yleum_api`).

Пакет переименован при ребрендинге в Yleum. Этот модуль существует только ради
задач, которые уже лежали в очереди Redis в момент выкатки: RQ хранит в задаче
не функцию, а её путь строкой, поэтому задача, поставленная старой версией,
после переименования искала бы несуществующий модуль и падала бы навсегда.

Здесь нет логики — только переадресация на новое имя. Удалять можно после того,
как очередь превью гарантированно опустела (задачи живут минуты, так что
достаточно одного спокойного дня после выкатки).
"""
