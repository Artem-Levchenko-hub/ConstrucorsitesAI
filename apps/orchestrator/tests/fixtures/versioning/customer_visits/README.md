Synthetic customer/visits app used by `test_versioning_customer_visits_scenario.py`.
Names, phones and e-mails are fixtures (`.invalid` domain), not customer data.
v1: clients. v2: + required email (technical surrogate) + visits.
