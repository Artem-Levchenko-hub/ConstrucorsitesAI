// Bare placeholder home. The agent REPLACES this with the real app from the
// user's prompt — auth, data, realtime, UI, all written from scratch.
export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center p-8">
      <p className="text-sm text-neutral-500">Building…</p>
    </main>
  );
}
