"use client";

// Stable public import used by the generation contract. The implementation
// lives in client.ts so the clean starter keeps one managed, tenant-safe
// integration path without shipping any prebuilt product UI.
export * from "./client";
