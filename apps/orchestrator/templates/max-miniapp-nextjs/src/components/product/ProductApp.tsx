"use client";

/**
 * Intentionally empty product canvas.
 *
 * MAX auth, Bridge, webhook, legal routes and managed integrations live in the
 * protected runtime around this component. The generator owns this file and
 * replaces it with the customer's product; no sample product or visual direction
 * is provided here to anchor the generated result.
 */
export default function ProductApp() {
  return <main data-max-product-canvas="empty" data-testid="max-miniapp-preview" />;
}
