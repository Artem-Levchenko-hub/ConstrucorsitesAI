# MAX Mini App design skills

Apply `max-ui-design.md` to every product screen. `DESIGN.md` in the generated
project is the persistent product-specific source of truth for palette,
typography, information architecture and the main user flow.
