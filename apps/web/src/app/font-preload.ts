/**
 * Предзагрузка шрифтов первого экрана.
 *
 * Объявления шрифтов живут в `fonts.css`, поэтому браузер узнаёт о файлах
 * только после того, как дочитает стили. Замер на проде 26.09: стили
 * заканчиваются на 1583 мс, а шрифты лишь НАЧИНАЮТ грузиться на 1595 мс и
 * заканчивают на 3040 мс. Полторы секунды крупный заголовок витрины показан
 * запасным шрифтом, а потом перерисовывается — это заметно и выглядит дёшево.
 *
 * Раньше предзагрузку добавлял встроенный механизм шрифтов; вместе с отказом от
 * загрузки из Google ушла и она. Возвращаем руками, но только для трёх файлов
 * первого экрана — предзагружать всё подряд значит отнимать канал у того, что
 * нужно сейчас.
 *
 * Пути постоянные и совпадают с теми, что в `fonts.css`: расхождение означало бы
 * скачивание файла дважды — предзагруженного и настоящего. За совпадением следит
 * проверка в тестах, потому что глазами такую ошибку не видно: страница работает,
 * просто вдвое тяжелее.
 */
export const PRELOADED_FONTS = [
  // Заголовок витрины — самый крупный текст на первом экране.
  "/fonts/onest-cyrillic-v1.woff2",
  // Основной текст и латиница вперемешку («MAX», «Free»).
  "/fonts/inter-cyrillic-v1.woff2",
  "/fonts/inter-latin-v1.woff2",
] as const;
