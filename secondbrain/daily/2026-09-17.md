# Daily Log: 2026-09-17

## Sessions

## Docs Ingest

## Memory Maintenance

### Session (04:33)

## Context

**Дата:** 2026-09-17. Проект: MAX Studio / Omnia.AI (`ConstrucorsitesAI`). Задача — разобрать три падающих GitHub Actions workflow и выполнить первые применимые пункты T22 по плану-дополнению `2026-09-16_MAX_Studio_T22_checks_and_baseline_addendum.md`.

---

## Key Exchanges

Пользователь закинул план T22 (md-файл через аттач) и попросил изучить проект и начать работу. Среда анализа не имеет SSH-доступа к VPS (170.168.72.200) и HTTPS-egress-доступа к production-эндпойнтам — только HTTPS к GitHub. Из свежего запуска smoke `35171806210` (17.09, 01:46 UTC) подтверждены все три провала:

- `web.release_mismatch` — ожидание `deddd35b…`, факт неизвестен.
- `max_health.http_502` и `max_webhook.http_502` — хост `fitnessstat-a379d4.preview.lead-generator.ru`.

API-компоненты (`api`, `worker`, `generation_worker`, `orchestrator`) в том же запуске зелёные на `d3931113`.

---

## Decisions Made

1. **T22.2 — покомпонентные версии в generation canary.** Заменили единый `PRODUCTION_EXPECTED_RELEASE_SHA=bd3f499c…` на четыре отдельных переменных (те же, что уже использует smoke). Generation-worker получил опциональную свою переменную `PRODUCTION_EXPECTED_GENERATION_WORKER_RELEASE_SHA`, по умолчанию падает на worker SHA.

2. **T22.4 — диагностируемый отказ canary.** Введены фиксированные стадии и коды причин; result JSON пишется артефактом при успехе и при провале; неудача cleanup больше не затирает первичную причину; никакие секреты/cookie/тела запросов/подписанные preview URL в вывод не попадают.

3. **T22.2 (побочно) — expected/actual в smoke.** `web.release_mismatch` теперь выводит `expected=… actual=…` с санитизацией фактического значения (`missing`/`unknown`/`invalid` при нечитаемом ответе).

4. **Порог 10 МиБ в backup не трогали.** Согласно плану — сначала состав и восстановление, потом правило.

5. **Ветка** `claude/elegant-franklin-j8p5sm` — ожидает мержа или ревью (CLAUDE.md требует работать в `main`, но задание выдано на ветке; нужно решение владельца).

---

## Lessons Learned

- SSH-порт 22 к VPS и HTTPS-egress к production закрыты в рабочей среде агента. T22.1, T22.3, T22.5 выполнимы только в сессии с сетевым доступом или через команды владельца.
- Из публичного CI-журнала видно, что api/worker здоровы на правильной версии, — это сужает рабочую гипотезу по `web.release_mismatch` до «переменная с ожидаемой версией устарела после лендинговых коммитов».
- MD-вложения в этом потоке требуют нотации `@"/path/to/file"` — без неё файл в сессию не попадает.

---

## Action Items

- [ ] **Владелец:** смержить ветку `claude/elegant-franklin-j8p5sm` → `main`, чтобы smoke и canary подхватили правки.
- [ ] **Владелец:** проверить `PRODUCTION_EXPECTED_WEB_RELEASE_SHA` в Actions-секретах — актуальна ли версия `deddd35b…` или нужно обновить.
- [ ] **Владелец:** удалить устаревший секрет `PRODUCTION_EXPECTED_RELEASE_SHA` (общий SHA, теперь не используется).
- [ ] **T22.1:** на сервере прочитать `/web-health` и `/api/health`, сверить образы с разрешёнными поставками → заполнить матрицу «компонент → ожидание → health → образ».
- [ ] **T22.3:** сопоставить журнал nginx/proxy с логами runtime по времени отказа 502 на `fitnessstat-a379d4` → установить корень.
- [ ] **T22.5:** проверить состав архива, сверить с фактическими Project Cell БД, запустить тест восстановления в изолированном окружении → затем и только затем менять порог 10 МиБ.
- [ ] **T22.6:** после успешного smoke провести реальный canary-прогон build → preview → edit → cleanup и зафиксировать результат с run/job ID.

### Session (05:04)

**Context:** Session activity captured from transcript.

**Key Exchanges:**
- **User:** @"/root/.claude/uploads/c7576b95-479e-528a-a900-bb5786c33b23/1fa46a6e-2026-09-16_MAX_Studio_T22_checks_and_baseline_addendum.md"
- **User:** bypassPermissions включи этот мод в этой сессии
- **User:** Stop hook feedback:
- **Assistant:** Всё зелёное. Коммичу.
- **Assistant:** Сделал по плану то, что выполнимо из этой среды, и довёл до пуша. Ниже — что именно и что осталось.
- **Assistant:** Переключить режим прав изнутри сессии я не могу — это управление на вашей стороне, агент не меняет собственный уровень доступа. Способы:

**Decisions Made:**
- Requires manual compile step if SDK is unavailable.

### Session (05:05)

## Context

Проект **MAX Studio** (репо `ConstrucorsitesAI`) — AI-конструктор мини-приложений для мессенджера MAX. Владелец загрузил аналитический документ `2026-09-16_MAX_Studio_T22_checks_and_baseline_addendum.md` с планом задачи **T22 / P0**: стабилизировать падающие GitHub Actions и обеспечить верифицируемую приёмку генерации.

---

## Key Exchanges

**Три подтверждённых отказа CI (запуск 17.09 01:46 UTC):**
- **Production smoke** `35171806210`: `web.release_mismatch`, `max_health.http_502`, `max_webhook.http_502`
- **Generation canary** `35069867816`: `canary_failed` — падает на preflight `_assert_release_health()`, до генерации не доходит; использует устаревший общий SHA `bd3f499c…` вместо покомпонентных ожиданий
- **Off-host backup** `35079342668`: архив `2 157 998 байт` < порог `10 МиБ` → отказ до выгрузки

**Что сделано (T22.2 + T22.4, коммиты `dfb339a`, `2e98fba` → ветка `claude/elegant-franklin-j8p5sm`):**
- Generation canary переведён с одного общего SHA на покомпонентные переменные (`PRODUCTION_EXPECTED_WEB_RELEASE_SHA`, `...API...`, `...WORKER...`, `...ORCHESTRATOR...`, опционально `...GENERATION_WORKER...`)
- Введены фиксированные стадии и коды причин; result JSON выгружается артефактом и при успехе, и при сбое; ошибка cleanup не затирает первичный отказ
- В smoke добавлен вывод `expected=… actual=…` при `web.release_mismatch` — без произвольного контента из ответа
- 30 новых тестов (сценарии C01–C11); итого 128 тестов зелёные; `ruff` и `mypy` чистые
- `otchet/data.json` обновлён: гипотеза H154 (`partial`), новый шаг V4

---

## Decisions Made

- **Порог 10 МиБ в backup не трогать** — сначала проверить состав и восстановление, потом менять правило (согласно T22.5)
- `PRODUCTION_EXPECTED_RELEASE_SHA` (устаревший) предложено удалить, решение — за владельцем
- `bypassPermissions` не поможет: блокировка — сетевая (SSH-порт 22 и egress к `*.lead-generator.ru`), а не разрешения инструментов

---

## Lessons Learned

- Generation canary и Production smoke использовали **разные контракты идентичности**: smoke — покомпонентный, canary — единый SHA. При независимых поставках это делало canary структурно нерабочим, даже если production здоров
- SSH и HTTPS к production-хосту недоступны из среды анализа Claude на web — T22.1 / T22.3 / T22.5 требуют либо другого окружения, либо ручного вывода команд от владельца
- Кандидат для отката: `1dad4cdd85d8c2404ce84ff13c5bfe8bb456557c` (09.09.2026) — до нового контура защищённого хранения; исторический CI зелёный, но это не сертификат стабильности генерации

---

## Action Items

- [ ] **Владелец:** смержить ветку `claude/elegant-franklin-j8p5sm` → `main`, чтобы исправления вступили в силу в CI (или дать добро на мерж самому)
- [ ] **Владелец:** удалить переменную `PRODUCTION_EXPECTED_RELEASE_SHA` из настроек Actions (устарела, больше не используется)
- [ ] **T22.1 (заблокировано):** с сервера прочитать `/web-health`, сопоставить с `docker inspect` образов — выявить фактический web SHA vs ожидаемый `deddd35b`
- [ ] **T22.3 (заблокировано):** по времени запуска smoke сопоставить лог nginx и runtime canary — установить внутреннюю причину HTTP 502
- [ ] **T22.5 (заблокировано):** сверить `MANIFEST.txt` / `SHA256SUMS` бэкапа с реальными контейнерами и Project Cell DB; только после этого — корректировать порог 10 МиБ
- [ ] **T22.6:** провести реальный generation canary (build → preview → edit → cleanup) на базе `1dad4cdd…` в тестовом окружении

### Session (05:10)

**Context:** Session activity captured from transcript.

**Key Exchanges:**
- **User:** Stop hook feedback:
- **User:** есть же локально данный проект на этой машине? что бы я локальную сессию запустил, скажи как это сделать
- **User:** Stop hook feedback:
- **Assistant:** Переключить режим прав изнутри сессии я не могу — это управление на вашей стороне, агент не меняет собственный уровень доступа. Способы:
- **Assistant:** Дерево чистое, всё запушено: `4df455e5` на `claude/elegant-franklin-j8p5sm`. Последним ушёл автологовый файл `secondbrain/daily/2026-09-17.md` — его пишут memory-хуки по ходу сессии, включая блок, добавленный уже после первого `git add`, поэтому он попал в коммит целиком.
- **Assistant:** Нет — «эта машина» не ваша. Я работаю в одноразовом облачном контейнере: проект тут лежит в `/home/user/ConstrucorsitesAI`, но это свежий клон, который удалится вместе с сессией. Ничего оттуда забирать не нужно — вся работа уже запушена в `origin`, ветка `claude/elegant-franklin-j8p5s

**Decisions Made:**
- Requires manual compile step if SDK is unavailable.

