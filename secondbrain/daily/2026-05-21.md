# Daily Log: 2026-05-21

## Sessions

## Docs Ingest

## Memory Maintenance

### Session (08:30)

## Daily Engineering Memory Log — 2026-05-21

---

**Context**

Omnia.AI MVP project — AI site builder for Russian market. Repo at `/home/user/ConstrucorsitesAI/`. Active branch: `claude/connect-vps-load-projects-hkWDS`. Last significant merge: PR #5 (static-first enterprise generation + never-blank preview, commit `47fb482`). Prod VPS on Serverum.ru running Debian 12, 8 cores / 15 GB RAM, Docker 29.2.1, user `i48ptgvnis`.

---

**Key Exchanges**

- User attempted to use a **cloud (web) Claude Code session** to SSH into the production VPS — the same workflow that previously worked when running Claude Code **locally on their machine**.
- Claude failed to connect, initially mis-diagnosed the cause (claimed port 22 was blocked globally), then confirmed via live `ssh` attempt: `Connection timed out` (exit 255). Real cause: cloud container's egress policy blocks port 22 to all hosts; only HTTP/HTTPS via allowlisted proxy is permitted.
- User pushed back correctly — SSH-based deploys had worked before. Claude clarified: local Claude Code runs on the user's machine (full network); cloud Claude Code runs in an ephemeral container (restricted network). Same model, different runtime environment.
- Claude extracted VPS state snapshot from `docs/08-vps-setup.md` as fallback.
- ⚠️ **User pasted plaintext VPS credentials (IP, username, password) directly into chat.** Claude flagged this and recommended a password change.

---

**Decisions Made**

- To resume VPS-connected work (SSH, deploys, live container inspection), the user must use **local Claude Code** (desktop app or `claude` CLI on their own machine), not the web interface.
- In cloud web sessions: git repo content is accessible; live VPS access is not.

---

**Lessons Learned**

- Web Claude Code ≠ local Claude Code for network-sensitive tasks. SSH/deploy workflows require local execution environment.
- Cloud session network policy: ports 80/443 proxied through allowlist, port 22 blocked globally. Even after installing `openssh-client`, the port is unreachable.
- Never paste server credentials into Claude chat (any session type) — transcripts persist in cloud infrastructure.

---

**Action Items**

- [ ] **Change VPS password immediately** (`passwd` on the server) — credentials were exposed in this cloud session transcript.
- [ ] Re-run this workflow in a **local Claude Code session** to get live VPS state, running containers, and git repos on the server.
- [ ] Document in team notes: cloud web sessions cannot SSH to VPS — reserve infra/deploy tasks for local sessions.
- [ ] Continue V2 Phase A work (orchestrator + `infra/` handoff from Agent C to Agent D) per `docs/07-v2-architecture.md`.

