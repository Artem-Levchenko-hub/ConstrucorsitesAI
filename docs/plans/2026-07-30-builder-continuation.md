# БИЛДЕР — обязательный checkpoint продолжения

Дата фиксации: 2026-07-30
Рабочая папка: `C:\Users\Артём\ConstrucorsitesAI`
Ветка доставки: `main`

Этот файл — постоянная точка продолжения после сжатия контекста, перезапуска
задачи или передачи работы. Задача не считается завершённой, пока не закрыты
все обязательные пункты ниже.

## Текущий цельный этап: MAX UX и real dev preview

- [x] Довести настройки MAX-приложения до полноценного центра управления:
  продукт и сценарий, возможности, визуальный стиль, управляемый контент,
  оператор, поддержка, юридические параметры, связь с MAX и инфраструктура.
- [x] При действии «Открыть кабинет MAX» автоматически копировать ссылку
  опубликованного приложения и показывать короткое понятное уведомление об
  успехе или ошибке копирования.
- [x] Сделать публикацию пошаговым мастером: завершённый, текущий и следующие
  шаги должны быть явно различимы; у текущего шага должно быть конкретное
  действие.
- [x] Использовать реальную защищённую preview-сессию работающего MAX
  dev-контейнера, без подмены публичной страницей и без запуска повторной
  генерации.
- [x] Завершить responsive-версию кабинета на desktop, tablet и mobile:
  без горизонтального overflow, лишних вложенных прокруток и недоступных
  действий; с touch targets не меньше 44 px, мобильной навигацией, реальным
  мокапом viewport 390 × 844 и устойчивыми модалками.
- [x] Проверить реальные взаимодействия в браузере на 1440 px, 768 px и
  390 px: меню, preview, публикацию, настройки, копирование ссылки, закрытие
  панелей и модалок. Сохранить визуальные доказательства и результаты
  overflow/console/network-проверок.

## Следующий обязательный этап: Hero-media MVP

- [x] Подключить и проверить реальный planner/model path вместо stub-only пути.
- [x] Выполнить controlled real render только на dev.
- [x] Довести пользовательский путь:
  `фото → план → подтверждение → preview → apply`.
- [x] Проверить preview и apply реальными взаимодействиями на desktop и mobile,
  включая отказ, повтор и отсутствие непреднамеренного применения.

## Отчётность

- [x] Добавить проверенный результат и визуальный аудит на существующую страницу
  «Отчёты».
- [x] Добавить на главную короткий блок «Что сделано сегодня».
- [x] Указать только подтверждённые результаты, реальные ограничения и
  незакрытые блокеры.

## Обязательная доставка каждого завершённого этапа

1. Тесты, lint/type checks, diff/data sanity checks.
2. Чистый commit с понятным сообщением.
3. Push текущей ветки в GitHub без force-push.
4. Deploy той же pushed-ревизии на общий dev-сервер в `/opt/omnia` только
   документированным compose/project.
5. Проверка revision, статусов сервисов и relevant HTTP/health endpoints.

Production-публикация пользовательского приложения запрещена без отдельного
подтверждения владельца. Проверка MAX preview и hero-media render выполняется
только в dev-контуре.

## Подтверждённое закрытие

- MAX UX: `8b89da54`, `05957263`, `3bbe362a`; browser evidence на
  1440/768/390 px сохранён в `max-responsive-audit`.
- Hero-media UX: `5b6d376c` и `050e728d`; upload race устранён, путь разделён
  на пять шагов, page/panel overflow равен нулю, видимые кнопки не меньше 44 px.
- Real media: planner работал без stub/mock, подтверждённый video-план дал
  5-секундный MP4 через `seedance-2.0-fast`, preview и Apply создали новый
  snapshot только после явного клика.
- Retry: `5fe5d9d4`; render использует immutable `render.media_plan`, UI
  обновляет точный cache-key. Regression motion-retry завершился с
  `video_model=none`, без video asset, без Apply и без нового snapshot.
- Media metadata: `2fe72aff`; storage key сохраняется для прямого URL и
  nginx-prefixed `/minio/<bucket>/…`. Одноразовые WebP/MP4, проект и связанные
  assets/renders удалены, тестовый аккаунт отозван.
- Честная оговорка: до retry-фикса mutable-plan дефект вызвал один
  непреднамеренный второй video provider call во время контролируемой проверки.
  Дефект исправлен, покрыт тестом и повторно проверен на dev.
- Пользовательское приложение в production не публиковалось.
