# MAX Studio остаётся, конструктор сайтов отделяется: опись и план

Дата: 19 сентября 2026. Решение владельца: в основном репозитории остаётся всё,
что нужно MAX Studio; остальное временно выносится в соседний проект, без
удаления насовсем.

Документ — опись (что нужно MAX, что нет, чем это доказано) и порядок отделения.
Сама правка кода идёт пакетами по правилам
[плана рефакторинга](2026-09-08-safe-total-refactoring-plan.md): тесты до правки,
независимая проверка, CI, выкатка, живая проверка.

## 1. Факты с прода (19.09)

| Что | Значение |
|---|---|
| Проекты | 3, все `max_miniapp`, у всех `project_cell_enabled = true` и есть рабочая ячейка; своих серверов (BYO) нет |
| Флаги | `USE_AGENTIC_BUILDER=true`, `PROJECT_CELL_GENERAL_AVAILABILITY_ENABLED=true`, `PROJECT_CELL_DOCKER_CANARY_ENABLED=true` |
| Контейнеры старого рантайма (`omnia-dev-*`, `omnia-app-*`) | 0 |
| Строки в таблицах старых функций | `hero_media_*` 0, `leads` 0, `custom_domains` 0, `deploy_targets` 0 |
| Живые запросы за 15 дней (nginx, 5–19.09) | hero media, exe, GitHub, шрифты, заявки, `/p/<slug>`, страницы `/projects`, пресеты, свой VPS, домены — 0. Ручные правки статических страниц, откат снимка, форк, импорт — только мои проверки при рефакторинге |
| Доска задач `/doska` | 295 запросов, 4 задачи — **рабочий внутренний инструмент**, остаётся |

Данных пользователей в отделяемых функциях нет; таблицы и миграции в любом случае
не трогаются.

## 2. Опись по частям

### 2.1 Фронтенд (`apps/web/src`: 263 файла / 33 953 строки, тесты 86 / 9 988)

`/` уже показывает лендинг MAX Studio; аккаунт, биллинг, админка, юридические и
корпоративные страницы — платформа под брендом MAX.

| Список | Исходники | Тесты |
|---|---|---|
| **Остаётся** (MAX + платформа) | 212 файлов / 23 947 строк | 80 / 8 964 |
| **Отделяется** (старый редактор `/projects/**`, `/deep-research` и всё, что монтируют только они) | 48 / 9 054 | 5 / 644 |
| из них мёртвое — удалить, а не переносить | `MaxIntegrationButton.tsx` (367), `GithubConnectionCard.tsx` (189) | |
| Отрезать, затем перенести (сидят в общих файлах) | 6 / 454: карточка ремикса, выбранные элементы инспектора, `joy-moment`, `brief-narration` и др. | 3 / 434 |
| Внутренний инструмент `/doska` | 3 / 952 | 1 / 380 |

Старый редактор — это `Workspace`, `TopBar`, `PreviewFrame`, `StylePanel`,
`HeroMediaPanel`, `Timeline`, потоковый предпросмотр статического HTML, кнопки
exe/GitHub/заявок/журналов. Ни один файл `components/workspace/**` не является
«только MAX»: 15 общих (чат, ввод, версии, кнопка рантайма, мастер своего сервера)
и 23 только старых.

Что придётся развязать: переход после входа по умолчанию ведёт на `/projects`
(`(auth)/actions.ts:142,176`); редактор MAX отправляет не-MAX проект на
`/projects/<id>`; `ChatPanel` по умолчанию строит ссылки на `/projects/<id>`;
общие `ChatMessage`/`PromptInput`/`usePromptStream` статически тянут куски
старого редактора (ремикс, инспектор, hero media, joy); API-редирект ремикса
`/p/<slug>/remix` ведёт на `/projects/<id>`; два структурных теста читают
`PreviewFrame.tsx`/`HeroMediaPanel.tsx` по пути; после ухода `TopBar` пропадает
единственный переключатель языка.

Мёртвые зависимости `package.json` независимо от отделения: `next-auth`,
`@radix-ui/react-tabs`, `react-accordion`, `react-tooltip`.

### 2.2 Orchestrator (`apps/orchestrator/src`: 86 файлов / 31 737 строк)

| Категория | Исходники | Тесты |
|---|---|---|
| **Project Cell / MAX** — ячейки, машины, окружения и чекпоинты, черновой и владельческий предпросмотр, публикация, восстановление версий, ядро и шлюз MAX, ёмкость и ограждение | 47 файлов / 20 803 | 66 / 20 386 |
| **Старый рантайм** — dev-контейнеры Next.js/fullstack/spa, `builder`, `provisioner`, `remote_deploy` (свой VPS по SSH), `byo`, `ingress` wake, усыпление, `exe_builder`, сидер демо-данных, `docker_client`, `postgres_admin`, `port_allocator` | 22 / 9 119 | 23 / 6 578 + 18 / 2 892 тестов шаблона `nextjs-entities` |
| Общее | 17 / 1 815 | 8 / 1 114 (нужно делить) |

Ни один модуль ячеек не импортирует `docker_client`, `postgres_admin`,
`port_allocator`, `hibernate`, `builder`, `remote_deploy`.

Что придётся развязать: ~150 строк помощников живут в роутере старого рантайма,
а нужны ячейкам (`_EXEC_DENY`, `_redact_exec_output`, `_safe_app_path`,
`_workspace_revision`, `_MAX_PREVIEW_BOOTSTRAP_*` …); статус и история публикации
ячеек отдаются маршрутами старого роутера (`GET /{id}/deploy`, `deploy/history`,
`destroy`); `provisioner._template_source_dir` используют два модуля ячеек; каждый
виртуальный хост nginx ссылается на страницу «просыпаюсь» (`/_omnia/wake`), которая
есть только в старом `ingress`; цикл усыпления стартует в общем `main.py`; образ
`omnia-template-max-miniapp-nextjs:dev` собирает только старый `provisioner` или
скрипт, перебирающий все шаблоны.

**Развилка «старый путь MAX».** Проект MAX, созданный импортом из GitHub или
форком, не получает ячейку и едет на старом рантайме (на проде таких 0). Либо
импорт/форк для MAX закрываются (или начинают создавать ячейку), и старый рантайм
уходит целиком, либо его MAX-ветка остаётся обязательной частью.

**Находка: «Собственный сервер» в панели запуска MAX.** Интерфейс предлагает его
любому проекту MAX, но для проекта в ячейке публикация на свой сервер блокируется
отказом 409 (`api services/cell_publication.py:351`), а сам выбранный сервер
(`deploy_target_id`) затем блокирует и обычную публикацию, пока его не сбросить.
Домены для ячеек тоже не поддержаны. При отделении пункт нужно скрыть.

### 2.3 Шаблоны приложений

| Каталог | Строк | Нужен MAX |
|---|---|---|
| `max-miniapp-nextjs` | 4 548 | да |
| `shared-public` | 3 353 | частично: манифест и три JS-ассета (1 813); `source/` (1 540) — только старые стеки |
| `nextjs-entities`, `nextjs-postgres-drizzle`, `nextjs-realtime`, `fastapi-postgres`, `telegram-bot-aiogram`, `vite-react-spa`, `bare-nextjs` | ≈ 25 000 | нет |
| API: `templates/{blank,landing,portfolio,blog,fullstack,shared-assets}` | 2 438 | нет |

Образ API запекает весь каталог шаблонов; читателям MAX нужен только
`max-miniapp-nextjs` и часть `shared-public`. Реестр стеков и два теста жёстко
связывают реестр с каталогами — их придётся сузить вместе.

### 2.4 API (`apps/api/src`: 320 модулей / ~95 000 строк)

**Главный вывод трассировки.** При настройках прода (`USE_AGENTIC_BUILDER=true`,
`use_surgical_edit` по умолчанию включён) проект MAX всегда идёт по агентному
конвейеру (`lifecycle.py:210-221, 302`). Конвейер статической генерации достижим
для MAX только через два аварийных выключателя: агент выключен флагом либо
выключены «точечные правки» и запрос признан лёгким. Импортированным проект MAX
быть не может (импорт даёт только `blank`/`code`), пустого slug не бывает.

**Отделяется (недостижимо для MAX после одного отсечения в `lifecycle.py` и снятия
старых роутеров и воркеров): 77 модулей, ≈ 26 000 строк.**
- конвейер статической генерации: `streamed_pipeline`, `stream_*`, `static_*`,
  `asset_composition`, `container_realization`, `surgical_recovery`;
- авторы и промпты: `prompt_builder` (4 627; MAX берёт из него одну константу
  `KIT_FILES`), `art_director_writer`, `director_polish`, `multipass_generator`,
  `lean_prompt`, `rag`, `pipeline_debug`;
- проверки качества: `accept_gauntlet`, `acceptance`, `entity_gate`, восемь
  `*_gate` для страниц, `reference_corpus`, `vision_audit`, `originality`,
  `ui_audit`, `structure_audit`, `link_validator`, `contrast_guard`,
  `palette_guard`, пять `*_registry`;
- постобработка статических страниц: `image_edit`, `zone_edit`,
  `visual_enricher`, `app_theme`, `entity_theme`, `brief_narration`,
  `share_meta`, `ui_shims`, `overrides`, `fonts`, `app_doctor`;
- функции: hero media (роутер, три сервиса, воркер), exe (`exe_build`,
  `exe_doctor`, воркер), `user_uploads`, ручные правки страниц (`uploads`,
  `style_patch`), ветка отката статического снимка в `rollback` (сам адрес
  остаётся: его вызывает редактор MAX и получает 409),
  GitHub (`github`), `design_presets` и `fonts` (нет вызовов из интерфейса),
  воркер `quality`;
- в `projects.py`: импорт, claim, форк, заявки, exe; в `public.py`: ремикс и
  статические файлы (для проекта в ячейке уже 404). **Поправка 19.09:** маршрут
  `GET /api/kit/{file}` из того же файла остаётся — черновые предпросмотры ячеек
  берут через него `omnia-inspector.js` (nginx-хост ячейки проксирует
  `/_omnia/inspector.js` на `api:8200/api/kit/omnia-inspector.js`), без него в
  редакторе MAX перестаёт работать выбор элементов.
Вместе с ними уходят 43 тестовых файла / 9 204 строки; 9 файлов / 3 219 строк
нужно делить. Ещё четыре модуля уже сейчас живут только в тестах:
`agent_vision`, `chip_causality`, `regen_convergence`, `role_gate` (649 строк).

**Остаётся, хотя выглядит «старым» — это исполняется для MAX:**
- до выбора конвейера: `discovery`, `clarify`, `generation/onboarding`,
  `lightweight_turns`, `stack_routing`, `intent_triage` (уточняющие вопросы при
  первом запросе, набранном вручную; `detect_appification` на каждом уточнении);
- договор о дизайне на сборочных ходах: `design_plugin` → `skill_library`,
  `design_presets`, `preset_classifier`, `design_tokens` → `sections.palettes`;
- проверки агентного пути: `sast_gate`, `security_gate`, типы `functional_gate`,
  `render_settle`, `auth_session`, `agent_gate_feedback`, `attestation`,
  `release_proof`;
- инструменты агента: `agent_media` → `image_resolver`, `agent_probe`;
- скачивание проекта: `run_bundle`, `project_export`; снимок экрана версии:
  `workers/preview.capture_live_url_report`.

**Ветки «не MAX» внутри остающегося кода** (вычищаются отдельным пакетом после
отделения): `build_plan`, `coverage_gate`, `functional_gate.run_functional_gate`,
`isolation_gate`, `design_dna.inject_into_globals`,
`backend_guardrail.check_backend`, блоки realtime в `agent_prompt.py`.

**Точки развязки:** `lifecycle.py:36, 322-338` (вызов статического конвейера) и
`:41, 249` (`KIT_FILES`); `main.py` (регистрация роутеров); `services/queue.py`
(имена задач RQ); `public.py:35-61` читает kit-ассеты при импорте — удаление
`templates/shared-assets` без правки роняет запуск API; `projects.py:48-57`
(импорт `repo_import`, `fork_recap`, `enqueue_build_exe`);
`stack_routing.py:57` и `discovery.py:461` умеют превращать статический проект в
`max_miniapp` в обход проверки доступа — уходит вместе с созданием не-MAX проектов.

**Находка: ответ «про установщик» в чате MAX.** Детектор намерения «запустить /
установить» (`acceptance.py:383-397`) не смотрит на тип проекта. В проекте MAX с
готовой сборкой фразы «как запустить приложение в MAX?», «хочу запустить», «как
установить» не запускают работу, а возвращают карточку «Готово — собрал
установщик… run.bat» из старого конструктора. Проверено на детекторе; чинится
первым пакетом отделения.

## 3. Как «вынести, но не удалить»

Соседний проект — **полная копия репозитория** на метке
`sitebuilder-full-2026-09-19` (последнее состояние с целым конструктором сайтов).
Перенос одних вырезанных файлов дал бы обломки без общего ядра; в полной копии
старый конструктор продолжает собираться и запускаться. Вернуть функцию в основной
репозиторий — это перенос нужных файлов из копии; карта «что и каким коммитом
убрано» ведётся в этом документе. История git основного репозитория сохраняет всё
в любом случае.

## 4. Порядок отделения

Каждый этап — отдельная поставка: тесты до правки, независимая проверка, CI,
выкатка, живая проверка MAX (создание проекта, сборка, предпросмотр, публикация,
восстановление версии), запись в `otchet`.

| Этап | Что | Объём | Зависит от решения владельца |
|---|---|---|---|
| 0 | Опись, метка, соседний проект | — | где хранить копию |
| 1 | Починка ответа «про установщик» для MAX; вход по умолчанию на `/max` | десятки строк | нет |
| 2 | Фронтенд: старый редактор `/projects/**`, `/deep-research` и всё только их | −9 100 + 640 тестов | нет |
| 3 | API: старые роутеры и воркеры; создание только `max_miniapp` | −6 000 | нет |
| 4 | API: статический конвейер генерации (4а — модули и тесты; 4б — мёртвые ветки внутри остающихся модулей: `sections`, аудит пикселей в `chip_pixel_gate`/`wow_dom_gate`, статические части `workers/preview.py`, `stack_routing`, `discovery`, ярлык установщика) | −27 000 + 15 000 тестов (4а) | аварийный откат на статический конвейер снят: для ячеек он не работал |
| 5 | Orchestrator: старый рантайм | −9 100 + 9 500 тестов | «Собственный сервер» и форк/импорт для MAX |
| 6 | Шаблоны: семь старых стеков, статические шаблоны API | −27 000 | идёт вместе с этапом 5 |
| 7 | Хвосты: флаги настроек, ветки «не MAX» в агентном пути, i18n, зависимости `package.json`, сборка образов, CI | −2 000…3 000 | нет |

Итог: рабочий код ≈ −74 000 строк (из них шаблоны 27 000), тесты ≈ −20 000.
Таблицы и миграции базы не меняются.

## 5. Журнал отделения (что и каким коммитом убрано из основного репозитория)

Всё перечисленное целиком лежит в соседнем проекте `~/projects/omnia-sitebuilder`
(метка `sitebuilder-full-2026-09-19`, пути те же).

| Этап | Коммит | Что |
|---|---|---|
| 0 | `a42cb240` + метка | опись, метка, соседний проект (полная копия с историей) |
| 1 | `f1f05b53` | починка: «как запустить?» в чате MAX больше не возвращает карточку установщика exe |
| 1 | `c4be388a` | вход без указанной цели ведёт на `/max` |
| 2а | `58ab7392` | фронтенд: старый редактор `/projects/**`, `/deep-research` и всё, что монтировали только они — 48 файлов / 9 054 строки и 5 тестов / 644 строки. Старые адреса перенаправляются на `/max` (`next.config.ts`), не-MAX проект из редактора MAX уходит в кабинет, `ChatPanel` по умолчанию строит ссылки на `/max/<id>` |
| 3а | `07104850` | API: роутеры `design_presets`, `fonts`, `github`, `hero_media`, `style_patch`, `uploads`; exe-эндпоинты в `projects.py`; воркеры `build_exe`, `hero_media` и их задачи в очереди; сервисы hero media, exe, шрифтов, пользовательских загрузок; схемы — 19 модулей / 3 743 строки, ещё ~150 строк помощников exe в `core/minio.py` и `orchestrator_client.py`, 9 тестовых файлов и `scripts/hero_media_e2e.py` (1 492 строки). Таблица маршрутов 159 → 131: ушли ровно 28 маршрутов старого конструктора, новых нет. Модели `hero_media_*` и миграции оставлены — таблицы не трогаются. На 3б остаются: создание только `max_miniapp`, импорт/claim/форк/заявки в `projects.py`, ремикс и kit-ассеты в `public.py`, воркер `quality`. Адрес `rollback` остаётся: его вызывает редактор MAX (`MaxWorkspaceShell`), для MAX он отвечает 409, рабочее восстановление идёт через `/restorations`; на 3б в нём убирается только ветка старого рантайма |
| 3б | `00480228` | API: создание только `max_miniapp` (другой шаблон — 422, гость — 403 как и раньше); убраны GitHub-импорт, claim, форк с «ремиксом», входящие заявки, анонимные владельцы и весь публичный предпросмотр `/p/<slug>` (страница, файлы, заявка, ремикс); сервисы `repo_import`, `fork_recap`, `github_client`, `repo.duplicate_repo`, 30 кодов ошибок вынесенных функций, 11 тестовых файлов. Таблица маршрутов 131 → 123: ушли ровно 8 адресов. Рабочий код −1 798 строк, тесты −1 623. Остаётся `GET /api/kit/{file}` (инспектор редактора MAX) и адрес `rollback`. Закрыты три дыры: заявку можно было записать в любой проект без проверки ячейки, гость мог создавать проекты и плодить пустых пользователей с кошельками, владелец мог форкнуть свой проект MAX в обход ячейки и проверки доступа |
| 4а | `7e442942` | API: конвейер статической генерации. `lifecycle.py` больше не уходит в `run_streamed_generation` — агент остаётся единственным сборщиком; проект, который агент взять не может (шаблон конструктора, импорт, пустой slug), и выключенный агент завершаются понятной ошибкой до любых действий со средой. Убрано 59 модулей / 22 937 строк (`streamed_pipeline`, `stream_*`, `static_*`, `asset_composition`, `container_realization`, `surgical_recovery`, `prompt_builder`, арт-директор и многопроходный автор, проверки качества страниц и их реестры, постобработка статических страниц, воркер `quality` с задачей очереди, четыре модуля, уже живших только в тестах), 8 скриптов оценки качества сайтов (≈ 3 100 строк), 60 тестовых файлов и 5 фикстур, осиротевшие данные (эталонные страницы корпуса, `axe.min.js`, копия `omnia-brief-narration.js`); 17 смешанных тестов приведены к агентному пути. Рабочий код −27 035 строк, тесты −15 000. Аварийный переход MAX на статический конвейер снят сознательно: для проекта в ячейке он сначала поднимал бы старый контейнер рядом с ячейкой, то есть страховкой не был |
| 4б | `036c5d57` | API: мёртвые модули внутри остающихся пакетов. Пакет `sections` сведён к палитрам (ушли JSON-описание лендингов `ir`, `catalog`, `defaults`, рендерер и 22 шаблона Jinja — MAX берёт из пакета только `palettes` для договора о дизайне); `generation/file_transforms.py` сведён к единственной живой функции; ушли `file_extractor`, `lucide_icon_names`, `vendor_profiles` и 7 тестовых файлов; зависимость `jinja2` убрана из `pyproject.toml` и `uv.lock`. Рабочий код −4 551 строка, тесты −2 124; модулей API 239 → 232. Осталось на 4в: попиксельный аудит в `chip_pixel_gate`/`wow_dom_gate` с `render_url/render_files`, статические части `workers/preview.py`, ветки «не MAX» в `generation/acceptance.py`, `discovery.py`, `stack_routing.py`, ярлык установщика, 44 настройки без читателей и их записи в compose |
| 5а | `4ba5e637` | Фронтенд MAX: убран раздел «Собственный сервер» и свой домен. Из панели запуска ушла карточка «Собственный сервер» (раздел теперь называется «Сервисы»), из редактора — окно `panel=hosting` вместе с мастером настройки чужой VPS; старые ссылки (`?panel=hosting`, `/max/<id>/settings?tab=vps`) больше не открывают пустое окно, а ведут на публикацию. В соседний проект перенесены `ExternalDeployWizard.tsx` и клиенты `lib/api/{deploy-targets,domains,external-deploy}.ts` с двумя тестовыми файлами (−1 200 строк вместе с тестами). На проде свой сервер никто не использует: 3 записи `deploy_targets` от 19.07, все со статусом `failed` и без единого проекта у владельца, `custom_domains` — 0 строк. Маршруты API `/api/deploy-targets` и `/api/domains` пока остаются: их снимают на этапе 5 вместе со старым рантаймом orchestrator, у которого с ними общие помощники |
| 4в (1/4) | `c82dd1f2` | API: попиксельный аудит страниц. `wow_dom_gate` целиком и половина `chip_pixel_gate` судили КАРТИНКУ статической страницы; вызовов, кроме собственных тестов, у них не осталось. Уцелевшая половина — словарь ответов онбординга, их разбор и превью акцента — переехала в честно названный `build_spec.py` (читают `discovery`, `generation.onboarding`, `generation.acceptance`). Общая сессия рендера (`render_url`/`render_files`/`run_gate_cli`) ушла вместе с обоими своими вызывающими; в `render_settle` осталась навигация `goto_and_settle`, которой пользуются поведенческие гейты, и структурный храповик «ни один `*_gate.py` не ходит в `page.goto` сам». Рабочий код −1 346, тесты −1 264; модулей API 256 → 254 |
| 4в (2/4) | `e021130d` | API: очередь задач RQ. В ней была ровно одна задача — отложенный рендер PNG-превью версии, — и для приложения MAX она завершалась первой строкой (превью снимается под замком генерации). Убраны `services/queue.py`, воркерский путь задачи (снимок статической страницы с диска и снимок dev-контейнера), три места постановки в очередь и зависимость `rq`. Живой скриншот работающего приложения (`capture_live_url_report`) остался — его зовёт `snapshot_preview_capture`. Процесс `worker` остаётся ради своих фоновых циклов (сердцебиение, подписки, уборка, досведение восстановлений). Рабочий код −446, тесты −237 |
| 4в (3/4) | `133374b5` | API + фронтенд: ярлык «Скачать установщик». Три режима хода (сильное намерение, вопрос с чипами, отказ), их тексты, три словаря сигналов в `discovery` и разбор маркера `<install-bundle>` с карточкой в чате. Ветка была закрыта условием «шаблон не max_miniapp» — то есть недостижима. Рабочий код −222, тесты −152 |
| 4в (4/4) | `17f29dd3` | API: 52 настройки без единого читателя (пороги приёмки страниц, восемь гейтов качества вёрстки, оригинальность, vision-аудит, hero media, exe, GitHub OAuth, арт-директор entity, эталонный корпус) вместе с 57 строками окружения в прод-compose, 6 строками образца `.env` и двумя утверждениями проверки политики compose. Отдельно найдено: `project_cell_operation_timeout_seconds` — настройка с MAX-именем, которую не читает никто, и `auto_regenerate_enabled`, чей код ушёл на 4а (запрет держится отсутствием функции). Рабочий код −516 |
| 5б | `a14dbc27` | Оркестратор + API: публикация на чужой VPS, свой домен и сборка установщика. Оркестратор — роутеры `/internal/deploy-targets/*`, `/internal/domains/publish`, `/internal/build-exe`, сервисы `remote_deploy` и `exe_builder`, клиент SSH, BYO-ветка сборщика (маршрутов 62). API — роутеры `/api/deploy-targets` и `/api/domains`, пять вызовов оркестратора, логи с чужого VPS, выбор цели публикации, снос проекта с чужой машины, десять кодов ошибок и лимит тарифа «Своих доменов» (маршрутов 118). Таблицы и колонки не тронуты. Рабочий код −2 235, тесты −1 063 |
| 5з | `c6472ecb` | Проверки публикации перестали ходить в несуществующий контейнер. Выпуск доказательства выкатки (`ensure_current_release_proof`) сведён к чтению состояния: он и так всегда возвращался раньше, чем доходил до своей работы — для проекта в ячейке предыдущая проверка отвечает `project_cell_publish_unavailable`. `run_release_proof` требует ячейку и потерял три обхода (сборка, проверка рантайма, поиск адреса превью через `get_status`). Убран предварительный прогрев маршрутов (запускался только без ячейки), модуль `dev_container` без единого импорта, «доктор зависимостей» `dep_doctor` и четыре адреса оркестратора без вызывающих (`/agent/build`, `/runtime-status`, `/warm`) вместе со старым зондом данных MAX. Рабочий код −330, тесты −590 |
| 6 | `65181bb6` | Шаблоны: шесть стеков конструктора (`nextjs-entities`, `nextjs-postgres-drizzle`, `nextjs-realtime`, `fastapi-postgres`, `telegram-bot-aiogram`, `vite-react-spa`), общий каталог `shared-public/source` (24 файла, у MAX нет ни одной ссылки в манифесте) и пять статических заготовок API вместе с «золотой» фикстурой и двумя шагами CI. Остались шаблон приложения MAX, пустая коробка `bare-nextjs` (выключенный опыт владельца) и `shared-assets` для `/api/kit/<файл>`. По цепочке: карточка «замороженных примитивов» realtime (67 строк) с настройкой `use_primitive_contract`, реестр стеков 8 → 2, таблица соответствий API → одна запись. Девятнадцать наборов приёмки entity-движка ушли за шаблоном; сверки инспектора, брифа, CTA, зависимостей, материализации и экспорта сужены до живого шаблона. Шаблоны −26 998, рабочий код −269, тесты −4 884 |
| 5в | `7e0166cb` | API перестаёт разговаривать со старым рантаймом: сняты маршруты `/runtime/logs`, `/runtime/keep-alive`, `/runtime/stop` (для ячейки они могли только отказать 409) вместе с компонентом управления контейнером на фронтенде (525 строк) и клиентскими функциями; чтение состояния, запуск и публикация — только через ячейку, проект без ячейки получает `runtime_unavailable`. Снят старый гейт публикации (`blocking_required`, `DEPLOY_ATTESTATION_BLOCKING`): он был вшит только в старый путь, ячейка проверяет доказательство строже. Маршрутов API 119 → 116. Рабочий код −909, тесты −212 |
| 5г | `b0e6cb84` | Сборка только в ячейке: создание приложения отказывает, если ячейку выдать нельзя; подготовка среды MAX без ячейки прерывает сборку; снят модуль авто-маршрутизации стека вместе с вызовом `provision`. Удалены пять сценариев контракта генерации на шаблоне `nextjs_entities` (шаблона нет с этапа 6). Рабочий код −277, тесты −1 185 |
| 5д | `feb6b664` | Оркестратор: шесть маршрутов без вызывающих (`provision`, `wake`, `stop`, `read-file`, `POST /deploy`, отмена публикации), сборщик прод-образа и журнал выкаток. Состояние и история публикации отвечают из службы ячейки, «нет публикации» = `idle`. Модуль `buildkit` сохранён намеренно: после ухода сборщика у него не осталось вызывающего, решение за владельцем. Маршрутов 62 → 56. Рабочий код −1 005, тесты −601 |
| 5ж | `28229bca` | Последний исполнитель старого конструктора. `make_container_executor` умел 14 действий агента, но после переезда на ячейки до него доходили три: справка по внешней библиотеке, справка по провайдеру, генерация картинки/видео. Одиннадцать остальных (чтение, список, поиск, запись, правка, сборка, команды, логи, проверка среды, зонд, проверка изоляции) вели в общий dev-контейнер, которого нет. Исполнитель сведён к трём действиям и назван `make_docs_media_executor`, параметр `legacy_execute` → `docs_media_execute` (имя обещало запасной путь, которого нет). Снят модуль авто-починки `autoheal` (125 строк, ноль вызывающих, флаг выключен, на проде отсутствует в настройках) и подсказка по ошибкам сборки `_enrich_build_failure`, жившая только внутри удалённой ветки `build`. Сняты пять адресов оркестратора без вызывающих (`/agent/read-file`, `/agent/list-dir`, `/agent/exec`, `/logs`, `/compile-status`) с их клиентами и двумя схемами ответов. Рабочий код −728, тесты −297 |
| 7а | `52e6cb4c` | Фронтенд: два компонента интерфейса без импортов, пять зависимостей без упоминаний (`next-auth`, scroll-area, tabs, accordion, tooltip) с записями lock-файла, два словарных раздела старого редактора и два правила CSS. −440 строк |
| 5е | `ca79b768` | Сборка только в ячейке — в коде. Из конвейера генерации убраны все развилки «с ячейкой / без ячейки» (подготовка контекста, применение правок к превью, откат небезопасной правки, восстановление дерева, команды оболочки): вместо тихого перехода на общий dev-контейнер — одна проверка `_require_project_cell`, которая честно останавливает сборку. Модуль `generation/runtime.py` больше не импортирует клиента оркестратора вовсе. Снято мёртвое подтверждение возможностей песочницы (`max_sandbox_capabilities`/`max_sandbox_attested` всегда были `{}`/`False`) вместе с маршрутами `/agent/sandbox-capabilities` и `/agent/exec-sandbox`, помощниками `run_sandbox_command`, `docker_runtime_facts`, `container_security_facts` и настройкой `agent_sandbox_runtime`. Рабочий код −853, тесты −1 200 |
| 7б | `6944c374` | Фронтенд: панель чата без двух режимов («обычный»/MAX, встроенный/отдельный) и всех веток под них — заголовок с кнопкой сворачивания, пустой экран, подсказка про лендинг пиццерии, вторая строка ввода; два мёртвых экспорта клиентов API. −82 строки |
| 7в | `5baa949e` | Ветки для шаблонов, которых больше нельзя завести. Семейство стеков, которые генерация умеет собирать, сужено с пяти до одного — после этого развилки схлопнулись сами. Восстановление версии оказалось сплошным отказом: для MAX оно и так отвечало 409 до любых действий, а весь код после проверки (чтение файлов, накат на контейнер, компенсация при сбое) был недостижим — `snapshot_restore` сократился с 95 строк до одного допуска, роутер `rollback` — до той же проверки. Снят планировщик сборки в промпте (`build_plan` запускался только для не-MAX) и платная LLM-подстраховка классификатора пресета (`classify_preset` + `_llm_classify`, Haiku на амбивалентных названиях, то же условие); синхронный классификатор остался, генератор переспрашивает его в момент сборки. Плюс два десятка развилок в подготовке контекста, промпте, проверках, публикации и приёме запроса. Рабочий код −572, тесты −224 |

Осталось на этап 7 (не мешает работе): пропавший переключатель языка, хвосты
сборки образов и CI, признак `mobile` в дизайн-плагине (всегда истинный, тянет
за собой desktop-ветки помощников). Пограничные охранники «не MAX» в роутерах
оставлены сознательно: они не выбирают поведение, а отказывают, и стоят копейки.
**Следующий очевидный кусок:** план сборки и гейт покрытия. `build_plan` строил
план только для не-MAX (то есть никогда), поэтому `_build_plan` всегда `None`,
а гейт покрытия включается условием `_build_plan is not None and not
is_empty` — то есть не запускается вовсе, даже если вернуть `USE_NATIVE_AGENT=0`.
Кандидаты на снятие: `services/build_plan.py`, `services/coverage_gate.py` и
настройки `use_build_plan`, `use_coverage_gate`, `coverage_max_attempts`.

Осталось на этап 5: dev-контейнеры и `provisioner`, усыпление, сидер демо-данных,
страница «просыпаюсь» у nginx-виртуалхостов и последние вызовы старых адресов —
`hot_reload` и `get_status` из запасной ветки `max_studio` (её выбирает
`resolve_project_cell_public_selection`, когда ячейку выдать нельзя) и
`agent_grep` из гейта публичного доступа. Обе ветки оставлены сознательно: они
не мертвы структурно, их держат настройки, а не недостижимость.

## 6. Найдено попутно: три механизма самопочинки сборки, умерших при переезде в ячейки

Это НЕ регрессия отделения — все три перестали работать тогда, когда проекты
переехали на ячейки, а отделение лишь убрало их трупы вместе с контейнерным
кодом. Но если сборки генератора стали падать чаще, чем раньше, — вот
правдоподобное объяснение. Решение, портировать их на путь ячейки или нет,
за владельцем: это изменение поведения генератора, а не уборка.

1. **Снятие `<html>/<body>` из вложенного макета** (`_sanitize_nested_layout`).
   Дубль ломает гидратацию React и убивает все клиентские компоненты. Жил
   только в записи старого контейнера; ячейка кладёт ровно то, что написал
   агент, и это было закреплено тестом осознанно. В шаблоне MAX вложенных
   макетов нет — сработал бы, только если модель создаст его сама.
2. **Подъём `@import` в начало CSS** (`_sanitize_css_imports`). Иначе Turbopack
   валит всю сборку. Для MAX роль частично играет живой
   `normalize_max_globals_css` на `src/app/globals.css` — файле, который и несёт
   импорт шрифта, — но любой другой CSS-файл не прикрыт.
3. **«Доктор зависимостей»** (`services/dep_doctor.py`, `use_dep_doctor`
   по умолчанию включён). Перед проверкой типов сканировал импорты, находил
   пакет, который модель использовала, но не добавила в `package.json`, и
   доставлял его из белого списка. У ячейки эквивалента нет: она ставит только
   объявленное в манифесте. Ближайший родственник — подстановка заглушек
   компонентов интерфейса (`ui_shims`) — закрывает shadcn, но не произвольные
   пакеты npm.

Отдельно: гейт публичного доступа (`isolation_gate.run_public_access_gate`)
оставлен, но на проде не работает по двум причинам сразу — весь цикл гейтов
выключен флагом `USE_NATIVE_AGENT=1`, а внутри он ищет маршруты через
`agent_grep` в контейнере `omnia-dev-<slug>`, которого у ячейки нет. То есть
даже если флаг вернуть, гейт не найдёт ни одного маршрута.
