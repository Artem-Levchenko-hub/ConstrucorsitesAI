# Dogfood-Eval Routine — протокол (источник правды)

**Создан:** 2026-06-16. **Запуск:** scheduled-task `dogfood-eval-loop`, cron `*/10 * * * *` (каждые 10 мин), последовательно, уступает живым сессиям. Токены не ограничены.

## Миссия

Автономно догфудить ПРОДУКТ (constructor.lead-generator.ru) как живой пользователь: генерить приложения / скрипты / ботов, оценивать **дизайн И функциональность** по скриншотам + логам, **находить тупняки и слепые зоны** генератора, и **чинить их по доказательствам (логи + скриншоты), НЕ вслепую**. Каждый прогон — один цикл, evidence → diagnosis → (fix или proposal) → ledger.

## Главный известный тупняк (гипотеза H1 — проверить ПЕРВОЙ)

Владелец: «написал "создай сайт" → получил статику; кинул 5 промптов "переделай в полноценное веб-приложение" — генератор по сути ничего не делал, чуть перегенривал статику, не отвечал по запросу».

**Гипотеза причины** (из root-cause анализа пайплайна):
- Первый промпт → `discovery` выбирает stack; дефолт `static` (`discovery._DEFAULT_STACK`), а полноценный апп — только если LLM-рекомендер активно выберет контейнер.
- **Follow-up промпты идут через triage → surgical-edit / edit-путь, который правит СУЩЕСТВУЮЩИЙ static HTML и НИКОГДА не пере-запускает discovery / stack_routing.** То есть «переделай в приложение» трактуется как правка статики, а не как смена стека static→nextjs_entities/spa. Эскалации стека на follow-up НЕТ.
- Проверить: `apps/api/src/omnia_api/routers/messages.py` (triage/intent → surgical/edit ветка), `services/intent_triage.py`, `services/discovery.py` (запускается ли на follow-up), `services/stack_routing.py` (`switch_to_stack` вызывается ли вне первого билда).
- **Фикс-направление** (когда подтверждено логами): detect intent «сделай настоящее приложение / добавь вход / БД / кабинет» на follow-up → re-trigger discovery/stack-escalation (static→container), а не surgical-edit статики. С тестом-репро.

## Протокол одного прогона (строго по шагам)

### 0. ГАРД (если не прошёл — выйти, записать строку в LEDGER «skipped: <причина>»)
- **Sequential-lock:** файл `_routine/dogfood.lock`. Если есть и моложе 15 мин → другой прогон идёт → ВЫЙТИ. Иначе записать в него ISO-время старта.
- **Уступи живой сессии:** glob `C:\Users\79133\.claude\coordination\omnia-mvp-*\active-sessions.json`. Если есть сессия с heartbeat за последние 5 мин и это НЕ этот прогон → ВЫЙТИ («yielded to active session»). Нет файла/дир → считать idle, продолжать.
- **Resource-guard:** `ssh i48ptgvnis@170.168.72.200 'free -m'`. Если available < 3000 МБ → НЕ генерировать: `docker rm -f` висяки `omnia-dev-{routine,test,dogfood}-*`, перепроверить; всё ещё мало → ВЫЙТИ.
- Снять lock в КОНЦЕ прогона всегда (даже при ошибке).

### 1. ВЫБОР СЦЕНАРИЯ (ротация по номеру прогона = число строк RUNS в LEDGER mod N)
0. **H1-репро (приоритет, пока H1 открыт):** «создай сайт для кофейни» → дождаться → затем «переделай это в полноценное веб-приложение: вход, личный кабинет, база записей» → ещё 1-2 таких. Замерить: эскалировал ли stack static→container или остался статикой.
1. **Web-app с нуля:** «CRM для записи клиентов: вход, список клиентов, добавление, заметки».
2. **Скрипт/инструмент (spa):** «калькулятор ипотеки с графиком».
3. **Телеграм-бот:** «бот для записи в барбершоп» (проверить — доступен ли вообще tgbot-стек: ранее orphaned).
4. **Свободный:** что-то из реальных запросов в логах прода (грепни недавние user-промпты).

### 2. ГЕНЕРАЦИЯ (через продукт)
- Драйв через браузер (авторизован) — gstack/playwright/Chrome MCP на `constructor.lead-generator.ru`: создать проект, отправить промпт(ы), дождаться сборки, открыть live-превью. ЛИБО через API минт-токеном (надёжнее headless): owner проекта-песочницы; токен — `docker exec omnia-prod-api /app/.venv/bin/python -c "from omnia_api.core.security import create_access_token; from uuid import UUID; print(create_access_token(UUID('<owner>')))"`, api на localhost:8200 (с VPS). Публичное превью: `<slug>-dev.preview.lead-generator.ru`.
- Снять **скриншоты** результата (desktop+mobile, light+dark если есть): сохранить в `_routine/runs/<ISO>/`.
- Скачать сгенерённый артефакт (HTML/.tsx/файлы) для анализа.

### 3. ОЦЕНКА (две оси)
- **Функциональность:** совпал ли результат с запросом? static vs реальный апп (есть `/app`-роуты, auth, БД, интерактив)? Для H1-репро — ЭСКАЛИРОВАЛ ли стек на follow-up? Кнопки/ссылки живые или тупики? Бот/скрипт реально делает заявленное?
- **Дизайн:** скриншот → оценка (rubric: иерархия, глубина, палитра-под-нишу, типографика, «не серый дефолт»). Можно прогнать через vision-судью продукта или оценить самому по скриншоту. Балл 0-10 + 2-3 конкретные проблемы.

### 4. EVIDENCE с сервера (корреляция «что увидел» ↔ «что решил сервер»)
- SSH прод, по project_id/slug грепнуть логи `omnia-prod-api` / `omnia-prod-worker`:
  - какой stack выбрал discovery, какой `gen_mode` (freeform/static/catalog), какая модель, был ли re-discovery на follow-up;
  - `[VSEGPT] stream usage … cache_hit=…` (кэш B), ошибки/traceback, пустые брифы, таймауты.
- Зафиксировать ИМЕННО серверное решение, которое привело к видимому провалу.

### 5. ДИАГНОЗ + СЛЕПЫЕ ЗОНЫ
- Назвать тупняк одним предложением + file:line, где решение принимается неверно.
- **Слепые зоны:** чего генератор НЕ проверяет/НЕ ищет (напр. не перечитывает интент на follow-up; не эскалирует стек; не зовёт судью; не видит, что страница — мёртвая статика с нерабочими кнопками).

### 6. УЛУЧШЕНИЕ (по доказательствам, не вслепую)
- Если фикс ЯСНЫЙ, безопасный, доказан логом+скриншотом → реализовать МАЛЫМ диффом + тест-репро → доставить (commit+push main+деплой нужных сервисов) → health-check. Цитировать строку лога/скриншот, что доказывает.
- Если фикс крупный/рискованный → НЕ шипить вслепую: записать PROPOSAL в LEDGER (что, почему, какой evidence, какой план).
- Один прогон — максимум один фикс (последовательность, не ломать прод).

### 7. ЗАПИСЬ
- Append строку в RUNS (ниже): время MSK, сценарий, stack/gen_mode/модель, design-балл, тупняк, действие (fix `<commit>` / proposal / none), PASS/FAIL.
- Сырьё (скриншоты, дампы, полный лог прогона) — в `_routine/runs/<ISO>/` (gitignored).
- Снять lock.

## Доставка / прод (CLAUDE.md)
- atomic commit + трейлер `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`; push main (через origin/main, `git fetch` сперва — main двигают 4 агента; rebase при race).
- Деплой: api/worker → `cd /opt/omnia/apps/llm-gateway/deploy/full && docker compose up -d --build api worker`; gateway → `… up -d --build gateway`; **фикс ШАБЛОНА `apps/orchestrator/templates/*` → ПЕРЕСБОРКА base-образа на VPS** (`cd /opt/omnia/apps/orchestrator && docker build -t omnia-template-nextjs-postgres-drizzle:dev -f templates/nextjs-postgres-drizzle/Dockerfile.dev templates/nextjs-postgres-drizzle/`), git pull НЕ достаточно.
- Прод-compose = `apps/llm-gateway/deploy/full` (контейнеры `omnia-prod-*`), НЕ `infra/`. Прод git: `git fetch && git merge --ff-only origin/main` (не pull). Health-check: curl прод 200 ИЗВНЕ (с VPS NAT-hairpin даёт ложный 000).

## ЗАПРЕТЫ
- Стоимость/токены/бюджет — НЕ упоминать НИ В КАКОЙ форме.
- НЕ трогать клиентские аппы `kofeinia`/`legomagazin` и чужие проекты `signal-telekom`/`crm-sistema`; не ломать прод. Запрещённые действия (оплата, удаление пользовательских данных, права доступа) — не выполнять.
- Песочница: использовать ТОЛЬКО свои тест-проекты (слаг с префиксом `dogfood-`), удалять тест-контейнеры после прогона.
- Модели — всё через vsegpt; proxyapi не использовать.

---

## BLIND-SPOTS LEDGER (накопительный — слепые зоны генератора)
<!-- одна строка на подтверждённую слепую зону: ID | дата | описание | file:line | статус(open/fixed commit) -->
- H1 | 2026-06-16 | follow-up «переделай в приложение» не эскалирует stack (surgical-edit правит статику, discovery/stack_routing не пере-зовутся) | messages.py:626,675 | **CONFIRMED** (run #1, code-path proof + live prod repro) → PROPOSAL ниже, фикс рискованный (деструктивный re-scaffold) → НЕ шипить вслепую
- BS-2 | 2026-06-16 | `decide_intent` не имеет «app-ification» интента: «вход / личный кабинет / база записей» не матчат ни _REBUILD, ни _STRUCTURAL keywords → CHEAP surgical-edit (auth/login-стемы намеренно исключены, чтобы не фолс-фолсить на «кнопка входа») | intent_triage.py:62,103 | CONFIRMED (run #1) — часть H1; xfail-репро в test_intent_triage.py
- BS-3 | 2026-06-16 | ЗЕРКАЛО H1: discovery ПЕРЕ-эскалирует инструмент с явным «без регистрации/входа/аккаунтов» в auth-стек nextjs_entities → калькулятор оказывается за /signin + ненужная БД; safety-net `_infer_stack_from_text` однонаправленный (только static→entities, никогда spa, не вето́ит LLM-pick) | discovery.py:838 | **FIXED 04bb7cf** (run #2: live-репро dogfood-mortgage-calc-68f79c + screenshot CTA→/signin; негативный safety-net `_explicit_no_backend`→spa, 3 теста-репро, задеплоено api/worker, VETO_FIRES verified live)
- BS-6 | 2026-06-16 | **E-commerce/storefront запрос схлопывается в authed-dashboard CRM** — нет публичного каталога (он за /signin), нет КОРЗИНЫ, нет ОФОРМЛЕНИЯ ЗАКАЗА, хотя бриф их явно называет. Entity writer SYSTEM_PROMPT.md жёстко зашивает «app home is /dashboard (binding)» + «functional app screens (dashboard/CRM/SaaS), not a landing» + «route per entity + a dashboard» → витрина с покупательской воронкой структурно недостижима. Bouquet=access:public, но публичного каталога-UI не генерится; CTA лендинга «В каталог»→/signin (тупик). Order=access:owner single-bouquetId (админ-CRUD), не customer-checkout | apps/orchestrator/templates/nextjs-entities/SYSTEM_PROMPT.md:82,96,118 | **CONFIRMED** (run #5: live-репро dogfood-flowershop-flora-6b00df — entities+routes+CTA+screenshot; BS-4 эскалация сработала end-to-end, но воронка отсутствует) → PROPOSAL P-EC, фикс = новый storefront-архетип (крупный) → НЕ шипить вслепую
- BS-6b (design) | 2026-06-16 | На entity-аппах writer красит страницу INLINE-хексом (`bg-[#450A0A] text-[#FAFAFA]` в page.tsx), но НЕ переопределяет `:root`-токены (globals.css `:root` остаётся дефолтным LIGHT: `--background oklch(0.99 0 0)`=white) → themed shadcn-компоненты рассинхронятся: `Button variant="outline"`=`bg-background`(white) + унаследованный near-white текст = НЕВИДИМАЯ вторичная CTA. Entity palette-guard HTML-only (07/08_palette_guard chars=0 на entity) → нет enforcement токенов на entity-поверхностях | globals.css:17 + nextjs-entities/SYSTEM_PROMPT.md:89 | CONFIRMED (run #5 screenshot: «Войти в кабинет» = пустой белый бокс) — часть design-фейла, фикс generator-prompt-level → не вслепую
- BS-4 | 2026-06-16 | ПЕРВЫЙ билд с пропущенным discovery (quiz/«просто сгенерируй» шлёт skip_clarify=true, либо select-mode) НЕ эскалирует stack: `switch_to_stack` живёт ТОЛЬКО в discovery-BUILD ветке, а discovery гейтится `interview_eligible = is_first_build and not skip_clarify` → явный app-запрос («CRM, вход, кабинет, база записей») собирается freeform-статикой с мёртвыми кнопками входа. Это ПЕРВО-билдовый близнец H1 (H1 = follow-up, остаётся PROPOSAL P-H1) | messages.py:706 | **FIXED 5878059** (run #3: live-репро dogfood-crm-zapis-d640f6 → template=blank/gen_mode=freeform; фикс — на first-build без discovery переиспользовать `_infer_stack_from_text`, is_first_build-гейт = недеструктивно в отличие от P-H1; verify dogfood-crm-verify-90b839 → template=nextjs_entities, реальный entity-CRM brief (clients/bookings/notes + auth-кабинет), задеплоено api/worker; 88 passed/1 xfail)
- BS-8 | 2026-06-16 | Сгенерённые entity-аппы гейтят защищённую `(app)`-группу ТОЛЬКО КЛИЕНТСКИ: writer пишет `(app)/layout.tsx` как `"use client"` с `auth.me()` в `useEffect`→`router.push("/signin")`. Shell рендерится+гидрируется ДО редиректа; без JS (curl/бот) гейт не срабатывает → анонимный GET /dashboard и /dashboard/clients = **HTTP 200 + полный app-shell**. Данные НЕ текут (API 401 + owner-scope), но «приватный кабинет» визуально сломан + `safeCollection` глотает 401 в `[]` → истёкшая сессия видит пустой апп («Пока пусто»), а не релогин. Корень — промпт: SYSTEM_PROMPT.md L78 делает клиентский `auth.me()`-гейт ОСНОВНЫМ, серверный `requireUser()` (session.ts:54, делает server-redirect) — лишь опциональным «you may» (L79); middleware.ts нет | nextjs-entities/SYSTEM_PROMPT.md:78,118 + (gen)(app)/layout.tsx + sdk/index.ts:112 | **CONFIRMED** (run #6, live-репро dogfood-crm-live-b4c0c7: anon 200+shell, anon API 401, full транскрипт) → PROPOSAL P-AUTHGATE, фикс security-sensitive+кросс-зонно (edge-safe middleware ИЛИ mandatory server-gate в промпте + base-rebuild + regen-verify) → НЕ шипить вслепую; acceptance-lock test_entity_auth_gate.py (2 green evidence + 1 xfail)
- BS-7 | 2026-06-16 | КОНСЬЮМЕР-БУКИНГ → /signin-стена: лендинг с «запись на X / бронирование Y» (без «без регистрации») форс-эскалируется в auth-gated nextjs_entities; ВСЕ конверсионные CTA («Записаться онлайн», «Записаться») ведут на /signin → клиент обязан ЗАВЕСТИ АККАУНТ, чтобы записаться на замену масла. `_BACKEND_SIGNALS` трактует lead-capture-слова («запись на», «бронирован») как proof нужды в аккаунтах/CRUD (discovery.py:90); `_infer_stack_from_text` всегда возвращает nextjs_entities (discovery.py:130), а негативный safety-net `_explicit_no_backend` спасает только дословное «без регистрации». Явный выбор «Лендинг» в quiz НЕ имеет веса в stack-решении. Новый триггер класса BS-3, который BS-3-downgrade не ловит | discovery.py:90,130,867-885 | **CONFIRMED** (run #5, live prod repro dogfood-autoservice-turbofix-342143: «Лендинг»+автосервис+«запись на ремонт онлайн» → nextjs_entities, page.tsx 5× href="/signin", screenshot в _routine/runs/; live `_infer_stack_from_text` «запись на ремонт»/«запись на стрижку»/«бронирование столика» → все nextjs_entities, «лендинг кофейни» → None) → PROPOSAL P-BS7 ниже; фикс двунаправленно-рискованный (сузить «запись на»/«бронирован» → недо-эскалация настоящих букинг-аппов) + архитектурный пробел (нет пути «static-лендинг + lead-form без клиентского auth») → НЕ шипить вслепую; acceptance-lock в test_stack_routing.py (3 xfail + 3 evidence)
- BS-9 | 2026-06-16 | **МУЛЬТИ-РОЛЕВОЙ / RBAC ПРОБЕЛ** — entity-рантайм НЕ имеет модели ролей: `AccessPolicy = owner\|public\|admin` (registry.ts:46), `normalize()` молча коэрсит любой иной `access` → `owner` (registry.ts:107-108), `roleForNewUser()` штампует только `admin\|user` (auth.ts:149), `authorize()` сравнивает `user.role` лишь с `"admin"` (engine.ts:55-67). AD-бриф для школы спроектировал 3 роли с compound-доступом (Student: teacher write/parent read; JournalEntry: teacher write/student+parent **read-own**), но writer ВЫНУЖДЕН схлопнуть всё в 3 ведра → Student/Teacher/Parent/Lesson/Homework/Announcement=`public`, JournalEntry=`owner`. Два live-фейла: (1) **УТЕЧКА ПЕРСДАННЫХ** — anon `GET /api/entities/Parent` → 200 + телефоны родителей в открытый интернет («+7 (993) 584-93-56», engine.ts:60-61 public-read открыт всем); (2) **ЦЕНТРАЛЬНАЯ ФИЧА МЕРТВА** — JournalEntry owner-scoped → родитель/ученик (роль "user") читает только own-created → ПУСТОЙ журнал, оценки ребёнка не видны никогда. Класс шире школы: clinic doctor/patient, marketplace buyer/seller — любой мульти-актор с разными вью структурно невозможен | registry.ts:46,107 + auth.ts:149 + engine.ts:55-67 + nextjs-entities/SYSTEM_PROMPT.md | **CONFIRMED** (run #7, live-репро dogfood-school-zhurnal-da6a5c: anon Parent-leak 200+phones + JournalEntry 401/owner + writer-access-flattening + screenshot; BS-4 эскалация снова сработала ✓) → PROPOSAL P-RBAC ниже, фикс = реальные per-app роли + role-assignment UX + relationship-scoped read-own (engine+registry+auth+apps/web+writer-промпт, security-sensitive) → НЕ шипить вслепую; acceptance-lock test_entity_rbac_gap.py (4 green + 1 xfail)
- BS-10 (low-sev) | 2026-06-16 | НЕТ детерминированного static→spa пути: `spa` выбирает ТОЛЬКО LLM-discovery. Детерминированный `_infer_stack_from_text` (discovery.py:120) возвращает лишь `nextjs_entities`|None, дефолт `_DEFAULT_STACK="static"` (discovery.py:55). Следствия: (а) на gateway-ReadTimeout запрос-инструмент падает в static (probe: 1/6 «калькулятор ипотеки» → ReadTimeout → stack=static); (б) BS-4-путь (skip_clarify/quiz «просто сгенерируй») переиспользует `_infer_stack_from_text` → инструмент без backend-сигналов собирается freeform-СТАТИКОЙ, в spa никогда не эскалирует (live: dogfood-mortgage-calc-tool-db81b7 skip_clarify=true → template=blank/gen_mode=freeform). НИЗКАЯ severity: для self-contained калькулятора static+inline-JS даёт ПОЛНОСТЬЮ рабочий инструмент (run #8 verified: live-слайдеры пересчитывают платёж 83 311₽→347 430₽, SVG-график 7 эл., lead-форма). Дыра проявится на тяжёлых tool-аппах (нужен React-state/npm-либы: редактор слоёв, карта с кластеризацией) — freeform-static недотянет | discovery.py:55,120 + messages.py:706 | CONFIRMED low-sev (run #8) — НЕ фиксить: LLM на discovery-пути уже корректно берёт spa (probe 5/6); форс static→spa эвристикой = риск фолс-фолса + лишний build-cost, выигрыша нет для текущих запросов
- BS-11 | 2026-06-16 | **REMOVAL-EDIT ОРФАНИТ IN-PAGE ЯКОРЯ** — surgical-правка «убери секцию X» удаляет `id="X"`, но nav-ссылка `href="#X"` остаётся → мёртвый якорь. Dead-link-пасс СОЗНАТЕЛЬНО пропущен на surgical-правках (messages.py:2586 `not surgical` — чтобы не трогать ПРЕД-существующие мёртвые ссылки, не упомянутые юзером), поэтому якорь, осиротевший САМОЙ запрошенной правкой, отгружается дохлым. Вторично: `repair_dead_links_inline` чинит лишь плейсхолдеры (`#`/`javascript:void(0)`), НЕ anchor-to-missing-id (link_validator.py:112), а LLM-repair гейтнут policy `auto_regenerate_enabled=False`. Также наблюдение: removal почти всегда промахивается `<edit>` SEARCH/REPLACE на cheap deepseek-chat → дорогой whole-file rewrite-fallback (медленно, но guarded ratio≥0.6 сохраняет контент — НЕ silent no-op). ПОЗИТИВ прогона: additive+modify правки (новая секция «Цены» + телефон) приземлились идеально, все 6 секций сохранены — owner-жалоба «генератор ничего не делал» НЕ воспроизвелась на легитимных контент-правках (изолирована к app-ification = H1) | link_validator.py:112 + messages.py:2586 | **FIXED 585e65c** (run #9, live-репро dogfood-bakery-edit-18ca73: «убери секцию отзывов» → `id="reviews"` удалён, nav `href="#reviews"` повис ×3; фикс `repair_orphaned_anchors_inline(old,new)` delta-scoped — чинит ТОЛЬКО id, удалённые ЭТОЙ правкой, детерминированно/без LLM/без regen, редирект на выживший CTA-таргет; 3 теста-репро + 5 зелёных, задеплоено api/worker, health 200 извне; live-verify на DEPLOYED-бинаре: 3 dead→0, `#reviews`→`#contacts`; live E2E «удали секцию Три дня…» → лог `orphaned_anchors repaired`, snapshot 1a66807f: `#process` починен, пред-существующий `#reviews` корректно НЕ тронут = delta-scope доказан)
- BS-12 | 2026-06-16 | **ENTITY-ФАЙЛ ИМЯ≠NAME → ВЕСЬ АПП DOA** — рантайм-реестр резолвит сущность СТРОГО по `entities/<Name>.json` (registry.ts `entityPath`→`${name}.json`, `listEntities` ключует по имени файла, registry.ts:57-103), и ВСЕ потребители ссылаются по `name` (SDK `entities.Client`, reference-поля, `<CrudResource entity="Client">`). Контракт — «filename === Name» (SYSTEM_PROMPT.md + стартовый `Task.json`). Но AD-бриф порой перечисляет файлы в lowercase/plural (`clients.json`) и writer копирует имя ДОСЛОВНО (art_director_writer.py:565), → `entities/clients.json` декларирует `{"name":"Client"}`, а рантайм стат'ит `entities/Client.json` → ENOENT → `loadEntity`=null → **404 «unknown entity 'Client'» на КАЖДЫЙ read/write** → апп полностью мёртв (пустые списки везде, запись не создаётся), хотя бриф/экраны/референсы внутренне консистентны | apps/api routers/messages.py + nextjs-entities/src/lib/entities/registry.ts:57-103 | **FIXED 989a7a8** (run #10, live-репро dogfood-crm-edit-563092: console `ApiError: unknown entity 'Client'/'Booking'`, все коллекции пусты, create молча падал; фикс `_normalize_entity_filenames(files)` в container-guard-цепочке рядом с `_preserve_auth_schema` ПЕРЕД commit+hot_reload — детерм/идемпотентно/fail-soft: переименовывает `entities/<X>.json` под внутренний `name` только если JSON парсится, name валидный идентификатор≠stem и нет уже корректного сиблинга; 5 тестов-репро + 43 passed/6 xfail без регресса, задеплоено api/worker; live-verify DEPLOYED: guard на broken-input → `clients->Client, notes->Note`; новый билд dogfood-crm-verify-2a9337 → `Client.json/Note.json/Booking.json`, `GET /api/entities/Client` 404→**401** (entity резолвится, апп жив), dashboard рендерит KPI+nav Заметки)
- BS-13 (architecture) | 2026-06-16 | **`CrudResource` НЕ ИМЕЕТ СЛОТА ДЛЯ СВЯЗАННЫХ СУЩНОСТЕЙ** — detail-панель managed-компонента (`detailFor`→`RecordDetail`, crud-resource.tsx:312-329) рендерит ТОЛЬКО собственные колонки/поля записи; в `CrudResourceProps` (crud-resource.tsx:46-108) нет `renderDetail`/`detailExtra`/`children`. AD-бриф для CRM детально спроектировал master-detail карточку клиента с разделом «Заметки по клиенту» (лента Note, фильтр по clientId, add+delete) + «История визитов» (Booking по clientId), но writer, опираясь на `CrudResource` («fast path for any list screen»), не может выразить per-record child-секции → либо ДРОПАЕТ их (билд #1 dogfood-crm-edit: Note-сущность вообще без UI/роута/nav — осиротевшая), либо деградирует до отдельного ГЛОБАЛЬНОГО списка (билд #2 dogfood-crm-verify: standalone роут «Заметки», но не «заметки ЭТОГО клиента в его карточке»). Класс шире CRM: любой master-detail с дочерними записями (пациент+визиты, заказ+позиции, проект+задачи) | nextjs-entities/src/components/omnia/crud-resource.tsx:46-108,312-329 + SYSTEM_PROMPT.md | **CONFIRMED** (run #10, source-proof + 2 живых билда) → PROPOSAL P-RELATED (additive `detailExtra?:(row)=>ReactNode` слот в CrudResource + writer-директива хэндбилдить detail-страницу с child-CrudResource scoped по ref-полю; kit+prompt+base-rebuild+regen-verify → не вслепую)
- BS-14 | 2026-06-16 | **МУЛЬТИ-СТАТЕЙНЫЙ БЛОГ → НЕКЛИКАБЕЛЬНЫЕ КАРТОЧКИ-ТУПИКИ** — запрос «блог … отдельная страница для каждой статьи (полный текст, фото, автор, дата)» собирается freeform-СТАТИКОЙ (template=blank/gen_mode=freeform; discovery не эскалирует и не существует blog/content-collection-архетипа — `STATIC_TEMPLATES={blank,landing,portfolio,blog}` это лишь стартовые index.html, не мультистраничник, prompt_builder.py:2297). Writer написал ПОЛНЫЙ текст ровно для ОДНОЙ featured-статьи (in-page секция `#article-kyoto`, достижима из hero-CTA), а остальные 4 статьи (свои title/анонс/автор/время чтения) выдал как `<article class="article-card hover-lift">` БЕЗ `<a>`/href/onclick → 4 из 5 запрошенных статей нечитаемы (Playwright: все 4 `hasLink=False, cursor=auto`). Категорийный фильтр (JS) и форма подписки РАБОТАЮТ — провал именно в достижимости контента. Два слоя: (1) writer НАРУШИЛ собственный промпт — freeform-правила уже ЗАПРЕЩАЮТ «кнопка-обманка без обработчика»/«пункт меню на несуществующий id» (prompt_builder.py:499-500) и велят делать мультистраничные пункты in-page-якорями (prompt_builder.py:496-497); (2) детерминированный safety-net СЛЕП — `find_dead_links` смотрит только `<a href>` на placeholder/missing-anchor (link_validator.py:24-46), а интерактивно-СТИЛИЗОВАННАЯ карточка вообще без `<a>` ему невидима → acceptance-vision поймал 2 футерных `href="#"`, но не 4 actionless-карточки. Класс шире блога: любой feed-of-items, где карточки должны открывать детали (каталог→товар, портфолио→кейс, новости→новость) | prompt_builder.py:496-500,2297 + link_validator.py:24-46 | **CONFIRMED** (run #11, live-репро dogfood-travel-blog-029341: 4 карточки hasLink=False + source + acceptance struct=False; картинки 200/490KB у реального юзера — белая полоса в моём рендере = NAT-hairpin контейнера к public MinIO, НЕ баг продукта) → PROPOSAL P-BLOG ниже, фикс = blog/content-collection-архетип ИЛИ writer-директива «каждая статья достижима» + base-rebuild + regen-verify (детерм. авто-репэйр FP-опасен: 4 карточки некуда вести при 1 статье) → НЕ шипить вслепую; acceptance-lock test_blog_feed_reachability.py (1 green evidence + 1 xfail)
- BS-15 | 2026-06-16 | **LEAD-ФОРМА = КЛИЕНТСКИЙ ТЕАТР, ЗАЯВКА В НИКУДА** — freeform-static лендинги генерят контакт/lead/подписка-формы, которые СОБИРАЮТ PII (имя/телефон/email), показывают success («Спасибо, перезвоним за 15 минут»), и МОЛЧА ВЫБРАСЫВАЮТ данные: нет `action`, нет `mailto:`, нет `fetch` → лид не доходит НИКОМУ. Хуже мёртвой кнопки — посетитель получает ложное подтверждение, владелец получает НОЛЬ заявок, пайплайн этого не замечает. Два слоя: (1) freeform-промпт (prompt_builder.py:491-493,606-607) велит форме `e.preventDefault()`+валидация+ВИДИМЫЙ success, `action`=«mailto: либо formspree как ПЛЕЙСХОЛДЕР» — даже при дословном следовании formspree-плейсхолдер не сконфигурен → лид теряется; на практике writer вообще дропает action. (2) НЕТ lead-приёмника: `routers/public.py` GET-only (get_index/get_file/remix), ни одного POST-эндпойнта, куда статик-лендинг мог бы слать заявку; escape-hatch промпта (prompt_builder.py:607-610) — «лучший static + ОДНОЙ строкой предложи full-stack», но сайт-визитка ЭТО и есть канонический кейс → его #1 конверсия молча мертва by design. Детерминированный нет-сейф слеп: `find_dead_links` смотрит только `<a href>` (link_validator.py:24-46), форму без `<a>` не видит (как actionless-карточки BS-14). Класс шире фотостудии: любой сайт-визитка/лендинг с «оставить заявку/обратная связь/подписка» | prompt_builder.py:491-493,606-610 + routers/public.py (GET-only) + link_validator.py:24-46 | **CONFIRMED** (run #12: live-gen заблокирован WALLET-EMPTY → пивот на код+2 РЕАЛЬНЫХ артефакта из repo_svc.read_files: dogfood-mortgage-calc-tool-db81b7 lead-форма name+phone → handler читает name/phone и hide-form+show-success, action=0/mailto=0/fetch=0 = ВЫБРОС; dogfood-travel-blog-029341 `onsubmit="preventDefault(); this.innerHTML='Спасибо!'"` = чистый театр) → PROPOSAL P-LEAD ниже, фикс архитектурный+кросс-зонный (public lead-receiver + storage + owner-inbox apps/web + writer-POST-директива) → НЕ шипить вслепую; acceptance-lock test_lead_capture_sink.py (2 green evidence + 1 xfail)
- BS-16 | 2026-06-16 | **REFERENCE-ПОЛЕ НЕВИДИМО В СПИСКЕ — "чья это запись?" не ответить** — `<CrudResource>` авто-РАСШИРЯЕТ reference-поля в list-params (`expand = fields.filter(f=>f.kind==="reference").map(f=>f.name)`, crud-resource.tsx:171-184 → `row._expanded.<ref>` грузится на каждый list), но НИКОГДА авто-не-РЕНДЕРИТ для них колонку: колонки берутся из props дословно (только `sortable`-дефолт, crud-resource.tsx:168), а SYSTEM_PROMPT не требует показывать relation колонкой. Когда writer забывает ref-колонку — расширенные данные грузятся и ВЫБРАСЫВАЮТСЯ, relation невидим в главном (list) вью. Чистая writer-вариативность под ОДНИМ генератором: dent-crm appointments рендерит `r._expanded?.patientId?.name`+doctorId ✓, а crm-verify bookings (cols=дата/время/услуга/цена/статус) И notes («Текстовые примечания к клиентам», cols=текст/дата) ОБА дропнули clientId-колонку → в списке записей/заметок не видно КЛИЕНТА. ЗДОРОВО (опровергнутая гипотеза): сам picker формы корректен — writer мапит schema-reference→`kind:"reference"`+`refEntity`, EntityForm грузит `entities[refEntity].list({limit:200})`→реальный `<Select>` (не UUID-текстбокс) в обоих аппах. Класс шире CRM: любой feed записей с родителем (заказ→клиент, тикет→проект, визит→пациент, заметка→клиент) | crud-resource.tsx:168,171-184 + nextjs-entities/SYSTEM_PROMPT.md (нет директивы) | **CONFIRMED** (run #13, wallet-empty→артефакт-анализ repo_svc.read_files 2 реальных песочниц crm-verify-2a9337 + dent-crm-bdc3f3 + template-source) → PROPOSAL P-REFCOL ниже, фикс = детерм. авто-колонка из reference-полей в CrudResource (template→base-rebuild+regen-verify, сейчас wallet-blocked) ИЛИ prompt-директива → НЕ шипить вслепую; acceptance-lock test_reference_column_surfacing.py (4 green + 1 xfail)
- BS-17 | 2026-06-16 | **РЕДАКТИРОВАНИЕ НЕ МОЖЕТ ОЧИСТИТЬ ОПЦИОНАЛЬНОЕ ПОЛЕ — молчаливый no-op с ложным success** — в entity-аппе можно ИЗМЕНИТЬ поле, но НЕЛЬЗЯ ОЧИСТИТЬ опциональное: стираешь телефон/заметку/срок, жмёшь «Сохранить» → тост «Изменения сохранены» → значение возвращается. Три поверхности: (1) `EntityForm.validate()` пустое non-required поле → `continue` ДО `out[f.name]=…` → стёртое поле ВООБЩЕ не уходит в payload (entity-form.tsx:158-164); (2) `updateSchema` каждое поле `zodForField(f).optional()` — НЕ `.nullable()` → даже если послать `null` чтобы очистить, zod отвергнет и 400-ит весь апдейт → очистка НЕВЫРАЗИМА в контракте (registry.ts:176-182); (3) `updateRecord` shallow-merge `{...existing, ...parsed.data}` → отсутствующее в payload поле сохраняет старое значение (engine.ts:286). omit-on-empty + merge-keeps-absent = очистка исчезает; наивный form-only фикс (послать null) отвергнется схемой. Класс шире барбершопа: любой entity-апп, любое опциональное поле, операция un-set (убрать телефон, обнулить заметку, снять срок/статус). Семья «ложного подтверждения» (BS-11 осиротевшие якоря, BS-15 lead-форма-театр) | entity-form.tsx:158-164 + registry.ts:176-182 + engine.ts:286 | **CONFIRMED** (run #14, LIVE RUNTIME на DEPLOYED-бинаре omnia-dev-dogfood-barber-crm-25cc9e starter Task: CREATE notes+due → PUT {title,priority} (точный payload формы при очистке, omit пустых) → 200 + updated_at сдвинулся (апдейт ВЫПОЛНИЛСЯ) → GET: notes/due ВЫЖИЛИ = CLEAR_BUG; CONTROL смена на новые non-empty значения применяется ✓ — ломается ТОЛЬКО un-set; live-gen WALLET-EMPTY обойдён через прямой CRUD к живому контейнеру, генерация не нужна — баг в базовом engine, одинаков во всех аппах) → PROPOSAL P-CLEAR ниже, фикс мульти-поверхностный (nullable updateSchema + form-emit-clear в edit + number/date/reference null-edge + base-rebuild + regen-verify), меняет write-validation семантику всех аппов → НЕ шипить вслепую; acceptance-lock test_entity_field_clear_gap.py (3 green + 1 xfail)
- BS-18 | 2026-06-16 | **СПИСОК ENTITY-АППА МОЛЧА ОБРЕЗАН НА 50 — нет server-pagination, записи 51+ недостижимы через UI** — managed `<CrudResource>` грузит коллекцию ОДНИМ вызовом `useEntity`→`entities[name].list(params)` БЕЗ `limit`/`page`, и движок отдаёт `Number(limit) || DEFAULT_LIMIT`=50 (engine.ts:38-39,187-189). DataTable пагинирует ТОЛЬКО эти ≤50 строк В ПАМЯТИ через `pageSize` (crud-resource.tsx:549,555) → выглядит полным («5 страниц по 10»), хотя ~10% данных не отдано. `useEntity` грузит один раз и не имеет page-state/fetch-next (use-entity.ts: «client-side paging happen in <DataTable>, not here»). Движок ПАГИНИРОВАТЬ умеет (`page`/`offset`, engine.ts:191-192) — UI просто не просит. SYSTEM_PROMPT перечисляет `list({…limit,page})` (L71), но НЕ велит wire-ить пагинацию, а CrudResource — документированный «fast path for any list screen» → 50-кап = дефолт почти всех генеримых списков. При дефолтном sort created_at DESC исчезают САМЫЕ СТАРЫЕ записи. Класс шире CRM: любой растущий список — клиенты, заказы, товары, брони, лиды, ученики; семья BS-14 (недостижимые карточки) + BS-16 (невидимая ref-колонка) | engine.ts:38-39,187-189 + use-entity.ts + crud-resource.tsx:177-184,549,555 + nextjs-entities/SYSTEM_PROMPT.md:71 | **CONFIRMED** (run #15, LIVE-RUNTIME на DEPLOYED base-образе: throwaway omnia-dev-dogfood-listcap-probe-cd7c7c, starter Task, auth Auth.js; CREATE 55 Task #1..#55 → GET без limit = 50 строк #6..#55 (старейшие #1-#5 пропали), GET `?limit=200`=55, GET `?page=2`=5 → движок умеет, UI не просит; live-gen WALLET-EMPTY обойдён прямым CRUD как run #14) → PROPOSAL P-PAGINATE ниже, фикс TEMPLATE-уровня (base-rebuild): band-aid `limit:200` лишь двигает обрыв + грузит 200×ref-expand на каждый экран; правильный = реальная server-pagination (engine total + useEntity page-state + DataTable refetch) мульти-поверхностно → НЕ шипить вслепую; acceptance-lock test_list_pagination_cap.py (4 green + 1 xfail)
- BS-19 | 2026-06-16 | **ENTITY-ДВИЖОК БЕЗ РЕФЕРЕНЦИАЛЬНОЙ ЦЕЛОСТНОСТИ — удаление родителя молча сиротит детей** — три слоя нулевой integrity: (1) `createRecord` (engine.ts:217-226) только zod-парсит body + `insert` — НЕ грузит target-сущность чтобы проверить, что referenced-строка существует (reference = просто `z.string()`) → запись с несуществующим ref принимается; (2) `deleteRecord` (engine.ts:295-315) удаляет по id и возвращает `{deleted:true}` — НИКОГДА не сканит другие сущности на детей, ссылающихся сюда, нет restrict/cascade/warn/affected-count; (3) `expandRecords` (engine.ts:143-148) `_expanded[field]=map.get(refId) ?? null` — мёртвый родитель НЕОТЛИЧИМ от «связь не задана» → осиротевший ребёнок рендерит relation-колонку как «—» без сигнала, что родителя снесли. Семья BS-13 (нет слота для child-записей) + BS-16 (ref-колонка невидима). Класс шире CRM: заказ→клиент, бронь→пациент, задача→проект, комментарий→пост, счёт→клиент — удаление родителя тихо корраптит каждого ребёнка | engine.ts:143-148,217-226,295-315 | **CONFIRMED** (run #16, LIVE-RUNTIME на DEPLOYED base-образе: throwaway omnia-dev-dogfood-refdel-probe-41de56, template nextjs_entities, 2 сущности через docker cp — Client{name,phone}+Note{text,clientId:ref→Client}, auth Auth.js; CREATE Note с bogus clientId «00000000-…»→**201** (write не валидирует ref), DELETE Client b966bf5a→**200** (нет restrict), GET Note?expand=clientId после удаления → обе ноты `_expanded.clientId=null` при живом `clientId` на мёртвый id = ОРФАН; live-gen WALLET-EMPTY обойдён прямым CRUD как run #14/#15) → PROPOSAL P-REFINTEGRITY ниже, фикс = семантическое решение политики (RESTRICT 409 / CASCADE / WARN-affected-count), мульти-поверхностный engine+apps/web, base-rebuild+regen-verify, data-integrity-чувствительный → НЕ шипить вслепую; acceptance-lock test_entity_referential_integrity.py (3 green + 1 xfail)
- BS-20 | 2026-06-16 | **СГЕНЕРЁННЫЙ ENTITY-АПП НЕ МОЖЕТ НАЙТИ ЗАПИСЬ ПРИ МАСШТАБЕ — поиск ложно сообщает «Ничего не найдено»** — два слоя компаундятся: (1) движок НЕ имеет server-side текст-поиска: `listRecords` фильтр = `data->>field = value` ЧИСТО точное равенство (engine.ts:176), нет ILIKE/substring/`%`/case-insensitive нигде → запрос поисковика («Zebra») невозможно протолкнуть на сервер, матчит только ПОЛНОЕ значение в ТОЧНОМ регистре; (2) managed-поиск ЧИСТО клиентский: `<DataTable>` фильтрует `String(v).toLowerCase().includes(q)` по `rows`-пропу (data-table.tsx:299), а rows = ≤50 загруженных одним `useEntity`-вызовом без limit (корень BS-18, use-entity.ts «search/sort/paging happen in <DataTable>, not here») → поиск видит только новейшие 50, старше — невидимо И нет серверного эндпойнта чтобы дотянуться. Отлично от BS-18 (P-PAGINATE): даже починка 50-капа НЕ чинит поиск — некуда проталкивать text-query. Хуже тихого обрыва BS-18: видимый поисковик АКТИВНО утверждает, что записи нет. Семья BS-14/BS-16/BS-18 — видимый контрол подразумевает достижимость всего набора. Класс шире CRM: любой растущий список (клиенты/заказы/товары/брони/лиды/ученики) | engine.ts:176 + data-table.tsx:299 + use-entity.ts | **CONFIRMED** (run #17, LIVE-RUNTIME на DEPLOYED base-образе: throwaway omnia-dev-dogfood-search-probe-972063, starter Task, 55 записей, owner-auth Auth.js; OLDEST «Zebra-UNIQUE-OMEGA-001» с заметкой «найди-меня-секрет»: GET без params=50 строк MARK ОТСУТСТВУЕТ, ?title=Zebra-UNIQUE-OMEGA-001(точное)=1, ?title=Zebra(префикс)=0, ?title=UNIQUE(инфикс)=0, ?title=zebra-…(неверный регистр)=0, ?notes=найди-меня-секрет(точное)=1, ?notes=секрет(substring)=0, CONTROL ?priority=high(точное)=1 / ?priority=hi(префикс)=0, ?limit=200=55 MARK ПРИСУТСТВУЕТ → запись в БД есть, но UI-дефолт её не грузит, клиент-поиск не видит, серверного text-search нет; live-gen WALLET-EMPTY обойдён прямым CRUD как run #14/#15/#16) → PROPOSAL P-SEARCH ниже, фикс = новый server-side text-search примитив (engine ILIKE/`q`-параметр по text-полям) + проталкивание поиск-запроса из useEntity/CrudResource на сервер; TEMPLATE+engine, base-rebuild+regen-verify, меняет list-data-flow контракт всех аппов → не вслепую; acceptance-lock test_entity_search_findability.py (4 green + 1 xfail)
- BS-21 | 2026-06-16 | **СОРТИРОВКА ВЫРОСШЕГО СПИСКА ВОЗВРАЩАЕТ ЭКСТРЕМУМ ЗАГРУЖЕННЫХ ~50, А НЕ ВСЕГО НАБОРА — "сортируй по цене/сроку" даёт уверенно НЕВЕРНЫЙ топ** — движок ИМЕЕТ корректную типизированную server-side сортировку: `listRecords` читает `sort`/`order` (engine.ts:180-181), `fieldExpr` кастит jsonb-значение `::numeric` для number и `::timestamptz` для date (engine.ts:74-75) → весь набор сортируется правильно (числа численно, даты хронологически). НО managed-UI её НЕ ИСПОЛЬЗУЕТ: CrudResource делает каждую колонку сортируемой по умолчанию (crud-resource.tsx:168), клик по заголовку → `toggleSort` ставит КЛИЕНТСКИЙ стейт (data-table.tsx:363), таблица пересортировывает загруженный `rows`-проп В ПАМЯТИ (data-table.tsx:305-307); rows = ≤50 от одного `useEntity.list()` без `sort`/`order`, хук явно делегирует сортировку таблице (use-entity.ts:21 "search/sort/paging happen in <DataTable>, not here"). Отлично от BS-18 (записи 51+ недостижимы) и BS-20 (НЕТ server text-search примитива): здесь примитив СУЩЕСТВУЕТ и работает даже при 50-капе — дыра лишь в непроброшенном read-path. Хуже тихого обрыва BS-18 и ложного «не найдено» BS-20: сортировка возвращает НЕВЕРНУЮ строку, которая ВЫГЛЯДИТ авторитетной, без сигнала. Класс шире CRM: любой растущий список с упорядоченной колонкой (заказы по сумме, счета по сроку, товары по цене, лиды по дате, ученики по оценке) | engine.ts:74-75,180-181 + data-table.tsx:305-307,363 + use-entity.ts:21 + crud-resource.tsx:168 | **CONFIRMED** (run #18, LIVE-RUNTIME на DEPLOYED base-образе: throwaway omnia-dev-dogfood-sortcap-probe-1bb521 port 3302, starter Task + built-in `due` date-поле, owner-auth Auth.js; 55 Task, OLDEST «AAA-EARLIEST-DUE-MARK» due 2018-01-01 (глобальный минимум), 54 филлера due 2026-*: GET без params=50 строк MARK ОТСУТСТВУЕТ, min-due в окне=2026-01-15 (лучшее, что покажет клиент-сорт), ?sort=due&order=asc&limit=200 → TOP=MARK 2018-01-01, ?sort=due&order=asc (default 50) → TOP=MARK 2018-01-01 MARK ЕСТЬ (сервер сортирует ВЕСЬ набор ПОТОМ берёт топ-50 → проброс на сервер чинит даже при капе), ?limit=200=55 MARK есть; live-gen WALLET-EMPTY обойдён прямым CRUD как run #14/#15/#16/#17) → PROPOSAL P-SORT ниже (пробросить sort-стейт DataTable в server-параметры useEntity, переиспользуя готовую engine-сортировку; малый, но TEMPLATE→base-rebuild+regen-verify, меняет list-data-flow всех аппов, тесно связан с BS-18/BS-20 — логично делать с P-PAGINATE; wallet-blocked → не вслепую) + acceptance-lock test_entity_sort_windowing.py (4 green + 1 xfail)
- BS-22 | 2026-06-16 | **ОДНА JS-ВАЛИДНАЯ-НО-PG-НЕВАЛИДНАЯ ДАТА 500-ит ВЕСЬ СПИСОК ПРИ СОРТИРОВКЕ — тотальный denial-of-view от обычного небрежного ввода даты** — расхождение write↔sort: (1) `date`-поле валидируется ТОЛЬКО JS `Date.parse` (registry.ts:139 `z.string().refine(s=>!Number.isNaN(Date.parse(s)))`) — лениво, пропускает «2025», «2025-06», «June 2025», «Jun 2025», «2025/06», «2025-02-30»; сырая строка кладётся в JSONB без нормализации; (2) сортировка кастит сохранённый текст `(data->>field)::timestamptz` (engine.ts:75) — парсер Postgres СТРОЖЕ и ОТВЕРГАЕТ каждое из этих значений; (3) сырая Postgres-ошибка не `EngineError` → `run()` отдаёт generic 500 «internal error» (http.ts:13-19); (4) CrudResource делает каждую колонку сортируемой (BS-21, crud-resource.tsx:168) → дата-колонка в одном клике от 500. Фильтр (`data->>key=value`, без каста) БЕЗОПАСЕН — запись фильтруется, падает только sort. Хуже BS-21 (неверный-но-присутствующий топ) и BS-18 (тихо скрытые строки): ВЕСЬ список крашится opaque-ошибкой для ВСЕХ юзеров от одной набираемой-руками даты (год / год-месяц / «June 2025»). Класс шире CRM: любой растущий список с дата-колонкой (счета по сроку, заказы по дате, брони, лиды) | registry.ts:139 + engine.ts:75 + http.ts:13-19 | **FIXED ad0a71b** (run #19, LIVE-RUNTIME на DEPLOYED base-образе: throwaway omnia-dev-dogfood-datecast-probe-2fe2f6 port 3399, starter Task built-in `due`, owner-auth Auth.js; pre-fix: POST due="June 2025"→201, ?sort=due&order=asc/desc→**500** «internal error», ?due=June 2025 фильтр→200 (находит), CONTROL delete-poison→?sort=due→200 (доказывает именно это значение), лог `invalid input syntax for type timestamp with time zone: "June 2025" at listRecords engine.ts:194`; live-gen WALLET-EMPTY обойдён прямым CRUD как run #14-18. ФИКС crash-proof: `safe_to_timestamptz(text)` STABLE plpgsql→NULL на ошибке каста (init-db.mjs, только exception-catch truly crash-proof — regex не ловит range-ошибки «2025-02-30») + engine.ts fieldExpr дата-сорт ЧЕРЕЗ обёртку вместо сырого `::timestamptz`; детерм/fail-soft/non-regressive: `safe_to_timestamptz(valid)===valid::timestamptz`, ZERO read/write-контракт-change (в отличие от P-PAGINATE/SEARCH/SORT); deploy = base-rebuild на проде; VERIFY на ПЕРЕСОБРАННОМ base (fresh omnia-dev-dogfood-datecast-verify-95e13b): POST «June 2025»→201, ?sort=due&order=asc→**200** (Beta 2026-01-15, Alpha 2026-03-01, poison→NULL sorts last), desc→200, фильтр→200, лог 0×«unexpected error»; 5 green + 1 xfail test_entity_date_cast_crash.py). СЛЕДУЮЩИЙ ШАГ (НЕ шипнут, P-DATECAST): write-валидатор остаётся ленивым (ужесточить = сменить write-семантику всех аппов, ср. BS-17 P-CLEAR)
- BS-23 (low-sev, robustness; integration HEALTHY) | 2026-06-16 | **МЕДИА/UPLOAD-ИНТЕГРАЦИЯ ЗДОРОВА, но конфиг хрупок** — провижер `_integration_env()` читает ТОЛЬКО `OMNIA_MINIO_*`-префиксные env БЕЗ фолбэка на плоские `MINIO_*`, которыми пользуется остальной стек (api/worker). Работает СЕЙЧАС только потому, что в `/opt/omnia/apps/orchestrator/.env` руками заданы `OMNIA_MINIO_SECRET_KEY` + `OMNIA_MINIO_PUBLIC_URL`; остальное падает в КОРРЕКТНЫЕ дефолты (endpoint `omnia-prod-minio:9000` резолвится из `omnia-runtime_default`; access `omnia` = реальный). VERIFY (опровергнута severe-гипотеза «upload мёртв у всех аппов»): live-e2e faithful server.ts-репликой (node+minio pkg на omnia-runtime_default) — STEP1 дефолты-без-секрета→`UPLOAD_FAIL AccessDenied`, STEP2 реально-инжектируемый env→`UPLOAD_OK` + внешний curl public URL → **HTTP 200** точные байты; `_integration_env()` спред в env контейнера (provisioner.py:193); kit рендерит картинки плоским `<img>` везде (gallery-grid/storefront/testimonial/record-detail/master-detail + entity-form, comment «no next/image remotePatterns needed») — без remotePatterns-гочи; sendEmail = stub `{sent:false,stubbed:true}` ВСЕГДА (server.ts:98-110), но writer-промпт честно это документирует (prompt_builder.py:1248-1249). ЛАТЕНТНЫЙ РИСК: если тот единственный `.env` дрейфнёт к плоской `MINIO_*`-конвенции стека / перегенерится / оркестратор мигрирует → upload МОЛЧА умрёт стек-широко (AccessDenied→500→«Не удалось загрузить файл») без сигнала, генератор мёртвый storage не детектит | provisioner.py:52-77,193 + integrations/server.ts:76-78,98-110 + nextjs-entities `<img>`-кит | **VERIFY HEALTHY + low-sev latent** (run #20) — НЕ фиксить: текущего видимого провала нет; дефенсив-фолбэк (`OMNIA_MINIO_*`→`MINIO_*` для access/secret/public_url, endpoint НЕ фолбэчить — `minio:9000` алиас стека не резолвится на omnia-runtime_default) мал и доказан-безопасен (== STEP2 values), но шип без видимого провала = спекулятивно (ср. BS-10 low-sev не фикшен)
- BS-5 | 2026-06-16 | tgbot/api стеки ОРФАНЫ от discovery: шаблоны `telegram-bot-aiogram` + `fastapi-postgres` полностью собраны и провижатся (stack_registry.py:63, prompt_builder `_TGBOT_STACK`/`_BACKEND_TEMPLATES`, schemas/project.py:46 tgbot→telegram-bot-aiogram), НО ни один NL-запрос их не достигает. discovery `_STACKS` = {static, fullstack, nextjs_entities, spa} и меню `_SYSTEM` предлагают LLM только эти 4; out-of-vocab pick → форс в static (discovery.py:843). Двойной разрыв: `_DISCOVERY_STACK_TO_TEMPLATE` (stack_routing.py:47) тоже без tgbot. Юзер просит бота → молча получает веб-апп | discovery.py:53,418-450 + stack_routing.py:47 | **CONFIRMED** (run #4, live prod repro: «сделай телеграм-бота для записи в барбершоп»→`nextjs_entities`; «telegram бот … без сайта»→`fullstack`) → PROPOSAL P-BS5 ниже; фикс крупный (вокаб+меню+routing-map+backend-only provisioning без web-preview+TELEGRAM_BOT_TOKEN secret-UX) → НЕ шипить вслепую; acceptance-lock в test_stack_routing.py (2 xfail + evidence)

## PROPOSAL P-H1 — эскалация стека на follow-up (2026-06-16, run #1)

**Проблема (доказана):** на follow-up `is_first_build=False` → discovery и `stack_routing.switch_to_stack` ВООБЩЕ не вызываются (gate `interview_eligible = is_first_build and …`, messages.py:626; switch только внутри first-build-ветки, messages.py:675). Параллельно `decide_intent` роутит «сделай настоящее приложение» в CHEAP (BS-2). Итог: статический проект НИКОГДА не станет контейнерным аппом, что бы юзер ни писал. Live-репро (prod-контейнер): все 4 формулировки app-ification → `CHEAP`.

**Почему не фикшу вслепую:** `switch_to_stack` → `repo_svc.init_repo` создаёт СВЕЖИЙ репо с parentless-коммитом и `_upload` ПЕРЕЗАПИСЫВАЕТ MinIO-ключ репо (repo.py:79-102). На first-build это ок (пустой starter), но на follow-up это СНОСИТ существующий сайт юзера + ломает rollback в таймлайне (старые snapshot-SHA исчезают из репо). Плюс нужна consent-UX (спросить юзера перед сменой стека) — это cross-zone (apps/web).

**План безопасного фикса (для отдельного прогона/согласования):**
1. `decide_intent` (или новый `detect_appification`) — распознать «сделать настоящее приложение / вход+кабинет+БД» на static-проекте. Тайтовый набор сигналов, тест-репро против фолс-фолса (см. xfail в test_intent_triage.py).
2. На таком follow-up + `project.template == 'static'` — НЕ surgical-edit, а **non-destructive** эскалация: `switch_to_stack`-вариант, который НЕ сносит историю (новый снапшот как child текущего; старый static остаётся rollback-абельным), ИЛИ consent-turn («это статика; пересоздать как приложение? старую версию сохраню в таймлайне») через существующий `_spawn_text_turn`.
3. Health-check на песочнице `dogfood-*`: static → app, кнопки живые, rollback к старому static работает.

**Acceptance-lock (уже в репо):** `apps/api/tests/test_intent_triage.py::test_appification_followup_should_escalate_not_surgical_edit` (xfail strict=False — XPASS, когда фикс приземлится).

## PROPOSAL P-BS5 — вернуть tgbot/api в NL-пайплайн (2026-06-16, run #4)

**Проблема (доказана):** `telegram-bot-aiogram` и `fastapi-postgres` — полноценные, провижабельные стеки (stack_registry.py:63, `is_fullstack` true, prompt_builder `_TGBOT_STACK` + `_BACKEND_TEMPLATES`, schemas/project.py:46), но НЕДОСТИЖИМЫ из любого пользовательского запроса. discovery — единственный, кто выбирает стек по NL — имеет `_STACKS = {static, fullstack, nextjs_entities, spa}` (discovery.py:53) и `_SYSTEM`-меню, которое предлагает LLM ровно эти 4 (формат build-JSON хардкодит `"stack":"static|spa|nextjs_entities|fullstack"`, discovery.py:450). Любой out-of-vocab pick форсится в `static` (discovery.py:843). Параллельно `_DISCOVERY_STACK_TO_TEMPLATE` (stack_routing.py:47) тоже без tgbot/api — даже если discovery вернёт "tgbot", routing вернёт None → стек не переключится. Live-репро (prod-контейнер `run_discovery(force_build=True)`): «сделай телеграм-бота для записи в барбершоп» → `nextjs_entities`; «telegram бот … без сайта» → `fullstack`. Единственное упоминание tgbot в NL-слое — инструкция nextjs-промпта «скажи юзеру: пересоздай как tgbot через омнию и ОСТАНОВИСЬ» (prompt_builder.py:1124-1128) — тупик: нет ни кнопки, ни discovery-пути это сделать.

**Почему не фикшу вслепую:**
1. Минимум 3 кодовых поверхности: discovery `_STACKS` + `_SYSTEM`-меню (+ детект-сигнал «бот/telegram»), `_DISCOVERY_STACK_TO_TEMPLATE`, и first-build provisioning, который должен принять backend-only шаблон.
2. **Нет web-preview:** tgbot/api — backend-only, нет `dev_url`-страницы. Таймлайн/preview-UX ожидает живой URL → бот покажет «сломанный» preview. Нужна отдельная trim-UX для бэкенд-стеков (логи/статус вместо iframe).
3. **TELEGRAM_BOT_TOKEN:** бот без токена — мёртв при старте (main.py:38 «missing TELEGRAM_BOT_TOKEN — set it in the Omnia secrets panel»). Авто-роутинг бота БЕЗ сбора токена = DOA, хуже чем веб-апп. Нужен secrets-UX (cross-zone apps/web) ДО того как роутить.

**План безопасного фикса (для отдельного прогона/согласования):**
1. Детект «телеграм-бот / aiogram / бот для X» (тайтовый, против фолс-фолса на «кнопка телеграма на сайте») → discovery предлагает tgbot как явный выбор/вопрос consent.
2. Добавить tgbot (и api) в `_STACKS` + `_SYSTEM`-меню + `_DISCOVERY_STACK_TO_TEMPLATE` ОДНОВРЕМЕННО (иначе двойной разрыв).
3. Backend-only preview-mode + secrets-turn (запросить TELEGRAM_BOT_TOKEN до запуска) через `_spawn_text_turn`/secrets-panel.
4. Health-check на песочнице `dogfood-tgbot-*`: запрос бота → tgbot-контейнер, preview не сломан, бот стартует с токеном.

**Acceptance-lock (уже в репо):** `apps/api/tests/test_stack_routing.py::test_tgbot_should_be_a_discovery_stack` + `::test_tgbot_should_route_to_template` (xfail strict=False — XPASS, когда фикс приземлится); `::test_tgbot_is_currently_unreachable_evidence` (зелёный сегодня).

## PROPOSAL P-EC — storefront/commerce-архетип (2026-06-16, run #5)

**Проблема (доказана, BS-6):** реальный prod-промпт «Магазин цветов «Флора»: каталог букетов, КОРЗИНА, оформление заказа с доставкой, отзывы, личный кабинет с историей» (skip_clarify=true) → BS-4 эскалация корректно подняла stack blank→nextjs_entities, entity-аппа собралась (Bouquet/Client/Order/Review + /signin + (app)/dashboard/{orders,profile,reviews}). НО покупательская воронка отсутствует: каталог за /signin, нет корзины, нет checkout. Причина — entity writer SYSTEM_PROMPT.md зашивает authed-dashboard как единственный архетип (`/dashboard` binding; «app screens, not a landing»; route-per-entity+dashboard). Bouquet=access:public, но публичный каталог-UI не генерится; CTA «В каталог»→/signin.

**Почему не фикшу вслепую:** добавить storefront-воронку — это НЕ prompt-твик. Нужны: (1) распознавание commerce/storefront-ниши (магазин/каталог/корзина/доставка/заказ) vs internal-tool; (2) публичные роуты `src/app/(shop)/{catalog,product/[id],cart,checkout}` помимо authed (app)/dashboard; (3) клиентская корзина (state) + checkout, пишущий Order для гостя/клиента; (4) kit-компоненты витрины (ProductGrid/ProductCard/CartDrawer/CheckoutForm) — часть уже есть (storefront-hero, gallery-grid), но cart/checkout/public-catalog нет. Это новый template-архетип + writer-инструкции + verify по нишам (магазин/доставка/услуги) → крупно и кросс-зонно (orchestrator templates + api system-prompt). Шип вслепую сломает текущие dashboard-аппы.

**План безопасного фикса (для отдельного прогона/согласования):**
1. Сигнал «storefront»: niche/brief содержит каталог+корзина+заказ/доставка → пометить gen как commerce.
2. Entity template: добавить опциональную public-shop ветку (catalog/product/cart/checkout) рядом с (app)/dashboard; CTA лендинга «В каталог» → публичный `/catalog`, НЕ /signin.
3. Order: гостевой/клиентский checkout-путь (не только owner-CRUD).
4. Verify на песочнице dogfood-shop-*: гость видит каталог без логина, добавляет в корзину, оформляет заказ; админ-dashboard остаётся для владельца.
**Дополнительно (BS-6b, отдельно):** entity-палитра — заставить writer переопределять `:root`-токены под brief-палитру (а не inline-хекс), ИЛИ детерминированный entity-palette-guard, инжектящий `:root{--primary…}` из brief → чинит невидимые/рассинхроненные shadcn-компоненты.
## PROPOSAL P-BS7 — консьюмер-букинг не должен запирать запись за клиентским auth (2026-06-16, run #5)

**Проблема (доказана live + кодом):** запрос «лендинг автосервиса … запись на ремонт онлайн» при ЯВНОМ выборе «Лендинг» в quiz → discovery собирает `nextjs_entities` (контейнерный entity-app со staff-кабинетом), и сгенерённый page.tsx делает ВСЕ конверсионные CTA `href="/signin"` (hero «Записаться онлайн», 3× прайс-карты «Записаться», CTA-band) — клиент обязан зарегистрировать аккаунт, чтобы записаться на услугу. Корень: `_BACKEND_SIGNALS` (discovery.py:90) содержит lead-capture-стемы «запись на», «бронирован», которые трактуются как нужда в аккаунтах/CRUD; `_infer_stack_from_text` (discovery.py:130, upgrade-net 867-870) форсит nextjs_entities; негативный net `_explicit_no_backend` (879-885) рескьюит ТОЛЬКО дословное «без регистрации». Live: «запись на ремонт» / «запись на стрижку» / «бронирование столика» → все `nextjs_entities`; «лендинг кофейни» → None. Явный quiz-pick «Лендинг» в stack-решении не участвует.

**Почему не фикшу вслепую:**
1. **Двунаправленный риск.** Просто вырезать/сузить «запись на»/«бронирован» из `_BACKEND_SIGNALS` → недо-эскалация настоящих букинг-/CRM-аппов («система записи с кабинетом мастера», «бронирование с личным кабинетом»), которые backend реально нужен. Сигнал амбивалентен: «запись на стрижку» (consumer lead) vs «управление записями» (staff CRUD) различаются контекстом, не стемом.
2. **Архитектурный пробел.** Правильный результат для консьюмер-лендинга с записью — public lead-capture форма (имя+телефон+услуга+время → staff), БЕЗ клиентского аккаунта. У Omnia нет стека/паттерна между `static` (нет backend для лида) и `nextjs_entities` (полный auth-gated entity-app). Лид-форме нужен куда-то слать заявку.
3. **Даже при правильном стеке** customer-facing «Записаться» не должен вести на /signin — это template/prompt-builder-уровень (writer сам выбрал href:"/signin"). Фикс рассыпан по поверхностям.

**План безопасного фикса (для отдельного прогона/согласования):**
1. Различить consumer-lead-booking vs staff-CRUD-booking: «запись на <услуга>» / «бронирование <ресурс>» на ЛЕНДИНГЕ без слов про кабинет/аккаунт/роли → lead-capture, не auth-stack. Тайтовый детект против фолс-фолса; учитывать явный quiz-pick «Лендинг» как сигнал-кап.
2. Дать lead-capture-путь: либо static-лендинг с формой → существующий backend для заявок (email/CRM-стаб), либо новый «landing+leadform»-вариант nextjs_entities, где CTA ведёт на public `/booking` форму, а НЕ на /signin (auth — только для staff-кабинета, отдельным входом).
3. Health-check на песочнице `dogfood-*`: запрос букинг-лендинга → primary CTA = живая форма записи (имя/телефон/услуга), НЕ /signin; заявка долетает.

**Acceptance-lock (уже в репо):** `apps/api/tests/test_stack_routing.py::test_consumer_booking_landing_should_not_force_customer_auth` (3 xfail strict=False — XPASS, когда фикс приземлится) + `::test_consumer_booking_landing_is_currently_force_escalated_evidence` (3 зелёных сегодня).

## PROPOSAL P-AUTHGATE — серверный гейт защищённой `(app)`-группы (2026-06-16, run #6)

**Проблема (доказана, BS-8):** entity-аппы защищают `(app)`-группу только клиентски. Live-репро (dogfood-crm-live-b4c0c7, anon без cookie извне): `GET /dashboard` и `/dashboard/clients` → **HTTP 200 + полный app-shell** (sidebar «Дашборд/Клиенты/Заметки»), редиректа на /signin нет. `GET /api/entities/Client` (anon) → 401 — данные owner-scoped, НЕ текут. Корень — writer-генерённый `(app)/layout.tsx` = `"use client"` с `auth.me()` в `useEffect`→`router.push("/signin")`: shell рендерится+гидрируется до редиректа, без JS гейт не срабатывает. Это верно по SYSTEM_PROMPT.md (L78 клиентский `auth.me()` = ОСНОВНОЙ; L79 серверный `requireUser()` = опциональный «you may»; L118 определяет route-group layout без упоминания auth). `requireUser()` (session.ts:54) уже делает server-`redirect('/signin')` — кирпич есть, writer не направлен его применить к layout. Вторичный баг: `safeCollection` (sdk:112-119) глотает 401 в `[]` → истёкшая сессия видит пустой апп.

**Почему не фикшу вслепую:** это security-чувствительная auth-правка, кросс-зонная (orchestrator templates), требует пересборки base-образа + regen-verify:
1. Option A (middleware.ts): наивный `export { auth as middleware }` тащит node-postgres/drizzle auth-конфиг в EDGE-runtime → ломает билд КАЖДОГО нового аппа. Нужен split edge-safe (jwt-only) auth-конфиг + matcher, исключающий `/`, `/signin`, `/signup`, `/api/auth`. Неверный matcher = лочит юзеров / 500-ит публичный лендинг на каждом аппе.
2. Option B (промпт): сделать `(app)/layout.tsx` СЕРВЕРНЫМ компонентом с `await requireUser()` до рендера (клиентский `auth.me()` — только для in-page UI). Меньше, но LLM-вероятностно + base-rebuild + regen-verify.
Оба = medium-risk → PROPOSAL, не блайнд-шип, один фикс/прогон.

**План безопасного фикса (для отдельного прогона/согласования):**
1. Решить A vs B (A детерминированнее: гейт не зависит от LLM; но сложнее edge-safe-конфиг).
2. Для B — добавить в SYSTEM_PROMPT MANDATORY-директиву: route-group `(app)/layout.tsx` — серверный, `await requireUser()` до рендера; обновить acceptance-lock на XPASS.
3. Заодно: `safeCollection` на 401 не глотать молча — пробросить/триггерить релогин (отдельно, чтобы истёкшая сессия не выглядела пустой базой).
4. Health-check на dogfood-*: anon GET /dashboard → 307→/signin (а не 200+shell); залогиненный — апп работает; публичный лендинг + /signin/signup живы.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_entity_auth_gate.py` — `test_server_gate_building_block_is_already_shipped` + `test_entity_template_ships_no_route_middleware_today` (green evidence) + `test_protected_route_group_layout_must_be_server_gated` (xfail strict=False — XPASS, когда серверный гейт станет обязательным/появится edge-safe middleware).

## PROPOSAL P-RBAC — мульти-ролевой доступ для entity-аппов (2026-06-16, run #7)

**Проблема (доказана live + кодом, BS-9):** реальный prod-промпт «веб-приложение для школы №7 … личные кабинеты ученик/родитель/учитель (учитель ставит оценки, ученик и родитель только смотрят)» (skip_clarify=true) → stack корректно эскалировал blank→nextjs_entities, AD-бриф спроектировал 3 роли с per-role access (Student: teacher write/parent+student read; JournalEntry: teacher write/student+parent read-own; Parent: parent read). НО рантайм поддерживает ровно 3 режима — `AccessPolicy = "owner"|"public"|"admin"` (registry.ts:46). Writer ВЫНУЖДЕННО схлопнул: 6 сущностей → `public`, JournalEntry → `owner`. Live-репро (dogfood-school-zhurnal-da6a5c, external curl): anon `GET /api/entities/Parent` → **HTTP 200 + телефоны родителей** (`"+7 (993) 584-93-56","Мария Соколова"`) — персданные в открытый интернет (public-read открыт всем, engine.ts:60-61); anon `GET /api/entities/JournalEntry` → 401 owner-scope → залогиненный родитель/ученик (роль "user", auth.ts:149 даёт только admin|user) читает только own-created строки → **пустой журнал, оценки ребёнка не видны**. Role-conditional UI из брифа («кнопка "Выставить оценку" только для роли "Учитель"») не может сработать — роли "teacher" не существует.

**Почему не фикшу вслепую:** реальный RBAC = крупная кросс-зонная security-правка:
1. **engine + registry:** расширить `AccessPolicy` за owner|public|admin → app-specific роли + relationship-scoped read-own (родитель видит строки СВОЕГО ребёнка через `parentId`-связь, а не `created_by`). Это новая модель скоупинга в `authorize()`/`listRecords` (engine.ts:53-67,166-169).
2. **auth:** `roleForNewUser` + назначение ролей — кто-то должен мочь сделать юзера «учителем»/«родителем»; нужна role-assignment UX (cross-zone apps/web) + JWT-роль за пределами admin|user (auth.ts:98-117,149).
3. **writer-промпт:** SYSTEM_PROMPT должен уметь декларировать роли и маппить compound-access брифа на них, а не флэтить в public/owner.
4. **Регрессия:** наивно поменять дефолт `public`→что-то закрытое сломает текущие публичные витрины/каталоги; поменять флэттинг writer'а вероятностно. Security-sensitive (сейчас УТЕЧКА персданных) → требует пересборки base-образа + regen-verify по нишам (школа/клиника/маркетплейс).

**План безопасного фикса (для отдельного прогона/согласования):**
1. Решить модель ролей: фиксированный набор (owner/admin/member) + relationship-scope, ИЛИ декларируемые per-app роли в entity-JSON. Relationship-scoped read-own (`scope: {via: "parentId"}`) закрывает «родитель видит своего ребёнка».
2. До полноценного RBAC — СУЗИТЬ утечку: сущности с явно персональными полями (phone/email родителя/ученика) НЕ должны флэтиться в `public` (anon-read). Минимальный кирпич: writer-директива «персданные → owner/admin, не public» + детект persональных полей.
3. Health-check на песочнице dogfood-*: учитель ставит оценку → родитель ребёнка видит ИМЕННО её, посторонний/anon — нет; персданные не читаются анонимно.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_entity_rbac_gap.py` — `test_access_policy_has_only_three_modes_today` + `test_unknown_access_is_silently_coerced_to_owner_today` + `test_new_users_can_only_be_admin_or_user_today` + `test_engine_only_distinguishes_admin_today` (4 green evidence) + `test_entity_runtime_should_support_app_specific_roles` (xfail strict=False — XPASS, когда per-app роли + relationship-scope приземлятся).

## PROPOSAL P-BLOG — мультистатейный блог должен делать каждую статью достижимой (2026-06-16, run #11)

**Проблема (доказана live + кодом, BS-14):** prod-промпт «блог о путешествиях «Дальние страны»: лента статей, ОТДЕЛЬНАЯ СТРАНИЦА для каждой статьи (полный текст, фото, автор, дата), рубрики с фильтром, «Об авторе», подписка» (skip_clarify=true) → freeform-static (template=blank/gen_mode=freeform). Writer дал полный текст 1 featured-статье (in-page `#article-kyoto`) + 4 article-card-тизера БЕЗ ссылки/обработчика → 4 из 5 статей нечитаемы. Категорийный фильтр и подписка работают; «Об авторе» — in-page секция (не страница, что для static приемлемо). Картинки у реального юзера грузятся (200/490KB — verified с Mac); «отдельная страница для каждой статьи» структурно невыполнима в одностраничной статике.

**Почему не фикшу вслепую:**
1. **Нет blog/content-collection-архетипа.** `STATIC_TEMPLATES={blank,landing,portfolio,blog}` (prompt_builder.py:2297) — это лишь разные стартовые index.html + ассеты, НЕ мультистраничник со статья-страницами. discovery не выбирает `blog` по NL (проект создаётся `blank`, discovery роутит лишь static/spa/nextjs_entities/fullstack). Дать «страницу на статью» = либо мультистраничная static-генерация (writer создаёт article-1.html… в том же ответе — промпт это допускает на L496, но writer не делает для feed), либо новый content-collection-стек. Крупно.
2. **Детерминированный авто-репэйр FP-опасен.** При 4 actionless-карточках и 1 in-page-статье ссылать все 4 на ту же статью = вводить в заблуждение; «обернуть в `<a href="#article-X">`» требует, чтобы для каждой карточки СУЩЕСТВОВАЛА своя секция — её нет. Детект «интерактивно-стилизованная карточка без `<a>`/onclick» сам по себе FP-склонен (декоративные карточки легитимны) → нельзя как блокирующий гейт («flip gates only after generator is good»).
3. **Слой промпта вероятностен.** Правило уже есть (prompt_builder.py:496-500), writer его нарушил; усиление = LLM-вероятностно + base-rebuild + regen-verify по нишам (блог/каталог/портфолио/новости).

**План безопасного фикса (для отдельного прогона/согласования):**
1. Сигнал «content-collection» (блог/журнал/новости/портфолио-кейсы/каталог-как-чтение): N однотипных item-ов, каждый со своим контентом → пометить gen как collection.
2. Дать достижимость: либо writer-директива MANDATORY «каждый item фида = либо своя `<article-slug>.html` (создаётся в этом же ответе), либо in-page секция + card→anchor; запрещено оставлять item без входа» (усилить L496-500 + few-shot), либо content-collection-генерация со списком+детал-страницами.
3. Health-check на песочнице dogfood-blog-*: каждая карточка статьи кликабельна и ведёт к полному тексту; фильтр/подписка живы.

**Acceptance-lock (уже в репо):** `apps/api/tests/test_blog_feed_reachability.py` — `test_actionless_article_cards_are_invisible_to_dead_link_finder` (green evidence: `find_dead_links` не видит карточки без `<a>`) + `test_blog_feed_article_cards_should_be_reachable` (xfail strict=False — XPASS, когда каждая статья станет достижимой или валидатор начнёт флагать actionless-карточки).

## PROPOSAL P-LEAD — у сгенерённой lead-формы должен быть настоящий приёмник (2026-06-16, run #12)

**Проблема (доказана код + 2 реальных артефакта, BS-15):** freeform-static лендинги генерят контакт/lead/подписка-формы, которые собирают имя/телефон/email и МОЛЧА их выбрасывают, показав фейковый success. Live-доказательство (repo_svc.read_files прямо из прод-хранилища):
- `dogfood-mortgage-calc-tool-db81b7` hero lead-форма (имя+телефон): submit-handler `e.preventDefault()` → читает `name`/`phone` в локальные переменные → `ctaForm.style.display='none'; ctaSuccess.classList.remove('hidden')`. `action=`0, `mailto:`0, `formspree`0, `fetch(`0 → PII прочитан и ВЫБРОШЕН.
- `dogfood-travel-blog-029341` подписка: `<form onsubmit="event.preventDefault(); this.innerHTML='Спасибо! Проверьте почту.'">` — email ниоткуда не читается, никуда не шлётся.
Корень: (1) freeform-промпт (prompt_builder.py:491-493, 606-607) санкционирует «action = mailto: либо formspree как ПЛЕЙСХОЛДЕР» — placeholder-formspree не сконфигурен, лид теряется; (2) `routers/public.py` GET-only — нет POST lead-эндпойнта, куда форма могла бы слать заявку. `find_dead_links` слеп к форме без `<a>` (link_validator.py:24-46).

**Почему не фикшу вслепую:** правильный фикс — настоящий lead-pipeline, кросс-зонно и архитектурно:
1. **apps/api:** публичный POST lead-receiver (напр. `POST /api/public/{slug}/leads`) с rate-limit/anti-spam + хранение заявок (новая таблица `leads`), привязка к project/owner.
2. **apps/web:** owner-видимый inbox заявок (где владелец читает лиды) — иначе «доходит», но невидимо.
3. **writer-промпт:** директива — форма POST'ит на этот receiver (`fetch('/api/public/<slug>/leads', …)`), а не mailto-плейсхолдер; success-state только ПОСЛЕ 2xx. base-rebuild не нужен (static writer = prompt-only), но regen-verify по нишам.
4. Наивный prompt-only нудж на `mailto:` — клуджи/часто-мёртв на мобиле и НЕ owner-видим (не закрывает дыру). Поэтому = PROPOSAL, один фикс/прогон.

**План безопасного фикса (для отдельного прогона/согласования):**
1. Решить sink: встроенный lead-receiver+inbox (полноценно, но cross-zone) ИЛИ минимальный «leads» эндпойнт+таблица + временный owner-вид в дашборде.
2. Writer-директива: контакт/lead/подписка-форма ОБЯЗАНА слать данные на receiver и показывать success только при успехе; запрет на «success без отправки».
3. Health-check на dogfood-*: отправка формы → запись в leads видна владельцу; success-state не показывается при сетевой ошибке.

**Acceptance-lock (уже в репо):** `apps/api/tests/test_lead_capture_sink.py` — `test_generated_lead_forms_capture_pii_but_have_no_sink` + `test_dead_link_finder_is_blind_to_a_lead_form_that_drops_data` (green evidence) + `test_generated_lead_forms_should_reach_a_real_destination` (xfail strict=False — XPASS, когда лид станет доходить до настоящего owner-видимого приёмника).

## PROPOSAL P-REFCOL — reference-поле должно быть видно в списке по умолчанию (2026-06-16, run #13)

**Проблема (доказана код + 2 реальных артефакта, BS-16):** `<CrudResource>` авто-расширяет reference-поля (crud-resource.tsx:171-184: `expand = fields.filter(f=>f.kind==="reference").map(f=>f.name)`, мёржит в list-params → `row._expanded.<ref>` есть на каждый list), НО колонки берутся из props дословно (crud-resource.tsx:168, только `sortable`-дефолт) — никакой колонки из reference-полей не выводится, и SYSTEM_PROMPT этого не требует. Итог: если writer не добавил ref-колонку вручную, расширенные данные грузятся и выбрасываются, а relation невидим в списке. Live-вариативность (один генератор, repo_svc.read_files): dent-crm appointments/page.tsx рендерит `{key:"patientId",render:(r)=>(r._expanded?.patientId)?.name ?? "—"}`+doctorId ✓; crm-verify bookings/page.tsx (cols=дата/время/услуга/цена/статус) и notes/page.tsx («Текстовые примечания к клиентам», cols=текст/дата) — clientId есть в `fields` (форма), но НЕ колонка → в списке не видно клиента. Picker формы при этом здоров в обоих (kind:"reference"+refEntity → EntityForm `entities[refEntity].list({limit:200})` → `<Select>`).

**Почему не фикшу вслепую:**
1. **Template-change → base-rebuild.** Правка `crud-resource.tsx` требует пересборки base-образа `omnia-template-nextjs-entities:dev` на проде (git pull недостаточно).
2. **Нужен regen-verify по нишам, а live-gen сейчас заблокирован WALLET-EMPTY** (vsegpt out of budget) — не могу прогнать свежую генерацию, чтобы доказать фикс end-to-end на dogfood-*. Без этого — не шипить (правило: фикс доказан логом+скриншотом + health-check).
3. **Риск авто-инъекции.** Слепо добавлять колонку на КАЖДОЕ reference-поле может (а) дублировать колонку, которую writer уже добавил под другим `key`, (б) показать вторичный/служебный ref, который writer намеренно скрыл. Нужен дедуп по уже присутствующим колонкам + аккуратный label-резолв (`_expanded[name]?.name ?? title ?? label ?? "—"`).

**План безопасного фикса (для отдельного прогона/согласования):**
1. В CrudResource: после нормализации колонок — для каждого reference-поля, чьего `name` НЕТ среди `columns[].key`, авто-добавить колонку `{key:name, header: field.label, render:(r)=>(r._expanded?.[name])?.name ?? "—"}` (детерм., дедуп по key, в конец после первой title-колонки или рядом). Поведение опционально-отключаемо пропом на случай намеренного скрытия.
2. Альтернатива/дополнение: MANDATORY-директива в SYSTEM_PROMPT — «каждое reference-поле обязано присутствовать колонкой списка, рендерящей имя связанной записи (`_expanded`)».
3. Health-check на песочнице dogfood-* (когда live-gen разблокируется): список записей с родителем (booking→client, note→client, appointment→patient) показывает имя родителя в колонке, не пусто и не UUID.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_reference_column_surfacing.py` — `test_reference_picker_is_healthy_today` + `test_crud_resource_expands_reference_data_today` + `test_crud_resource_never_auto_renders_a_reference_column_today` + `test_system_prompt_does_not_require_reference_columns_today` (4 green evidence) + `test_reference_fields_should_be_surfaced_in_the_list` (xfail strict=False — XPASS, когда CrudResource начнёт авто-выводить ref-колонку или промпт её потребует).

## PROPOSAL P-CLEAR — редактирование должно уметь очищать опциональное поле (2026-06-16, run #14)

**Проблема (доказана LIVE RUNTIME + кодом, BS-17):** в сгенерённом entity-аппе можно ИЗМЕНИТЬ значение поля, но НЕЛЬЗЯ ОЧИСТИТЬ опциональное — очистка молча проваливается, а UI рапортует «Изменения сохранены». Live-репро на DEPLOYED-бинаре (omnia-dev-dogfood-barber-crm-25cc9e, starter Task: notes/due/priority опциональны; auth через public HTTPS, AuthJS ставит secure-cookie только по TLS):
- CREATE Task `notes="перезвонить клиенту в 18:00"`, `due="2026-07-01"` → 200.
- PUT `{title, priority}` — ТОЧНЫЙ payload, который шлёт форма при очистке notes+due (форма omit-ит пустые опциональные) → 200, `updated_at` сдвинулся 15:05:51→15:05:55 (апдейт ВЫПОЛНИЛСЯ).
- GET после очистки: `notes` и `due` ВЫЖИЛИ → CLEAR_BUG.
- CONTROL: PUT с новыми non-empty значениями (`title→"Стрижка Пётр"`, `notes→"новая заметка"`) — применяется. Правка значения работает; ломается ТОЛЬКО un-set.

Корень (три поверхности): (1) `EntityForm.validate()` — пустое non-required поле → `continue` ДО `out[f.name]=…`, стёртое поле не попадает в payload (entity-form.tsx:158-164); (2) `updateSchema` — каждое поле `zodForField(f).optional()`, НЕ `.nullable()` → послать `null` для очистки нельзя (zod 400-ит весь апдейт), очистка невыразима в контракте (registry.ts:176-182); (3) `updateRecord` — shallow-merge `{...existing, ...parsed.data}`, отсутствующее поле сохраняет старое (engine.ts:286). omit-on-empty (1) + merge-keeps-absent (3) = очистка исчезает; даже наивный form-only фикс (слать null) разобьётся о (2).

**Почему не фикшу вслепую:** правильный фикс мульти-поверхностный и меняет write-validation СЕМАНТИКУ каждого аппа:
1. `updateSchema` (registry.ts:179): опциональные поля → `.nullable().optional()`, чтобы `null` принимался как «очистить». Риск: null теперь валиден для ЛЮБОГО опц. поля во всех аппах — нужно проверить, что engine-merge запишет null (запишет: спред перезатирает) и что потребители (`_expanded`, фильтры, рендер колонок) корректно читают null.
2. `EntityForm.validate()` (entity-form.tsx:158-164): в EDIT-режиме поле, которое БЫЛО в `initial` (имело значение) и теперь пусто → слать `null`, а не omit-ить; в CREATE — omit как сейчас. Нужен доступ к `initial` в `validate()` и diff.
3. Edge-cases по типам: number очищенный → null (не `Number("")=NaN`); date → null; reference (`z.string().min(1)`) → требует nullable; boolean не очищается. Каждый тип — своя проверка.
4. TEMPLATE-change → пересборка base-образа `omnia-template-nextjs-entities:dev` на проде + regen-verify по нишам. Verify можно сделать БЕЗ генерации (throwaway-контейнер из пересобранного base со starter-Task + повтор runtime-репро), но сама правка схемы рискованна → PROPOSAL, один фикс/прогон.

**План безопасного фикса (для отдельного прогона/согласования):**
1. `updateSchema` → `.nullable().optional()` для опц. полей; убедиться, что engine-merge пишет null и читатели его переживают.
2. `EntityForm` — в edit-режиме (есть `initial`) emptied-поле с непустым `initial` слать как `null`; типовые edge-cases (number/date/reference).
3. Health-check БЕЗ генерации: пересобрать base, поднять throwaway omnia-dev-* со starter Task, повторить runtime-репро — очистка теперь стирает (`notes`/`due` → null/пусто), CONTROL-смена и CREATE не задеты.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_entity_field_clear_gap.py` — `test_form_omits_empty_optional_fields_today` + `test_update_schema_makes_a_clear_inexpressible_today` + `test_engine_update_is_a_shallow_merge_that_keeps_absent_fields_today` (3 green evidence) + `test_clearing_an_optional_field_should_persist_the_clear` (xfail strict=False — XPASS, когда updateSchema примет null И форма начнёт слать явную очистку в edit-режиме).

## PROPOSAL P-PAGINATE — managed-список должен достигать записи за дефолтным капом (2026-06-16, run #15)

**Проблема (доказана LIVE-RUNTIME + кодом, BS-18):** генеримый managed-список молча обрезается на 50 записях. Live-репро на DEPLOYED base-образе `omnia-template-nextjs-entities:dev` (throwaway omnia-dev-dogfood-listcap-probe-cd7c7c, starter `Task`, owner-auth через Auth.js credentials — без LLM, без генерации):
- CREATE 55 Task (#1..#55).
- GET `/api/entities/Task` (БЕЗ limit — ровно то, что шлёт `useEntity`/`CrudResource`) → **50 строк, #6..#55** (старейшие #1-#5 отсутствуют, дефолтный sort created_at DESC).
- GET `/api/entities/Task?limit=200` → 55 строк #1..#55 (все на сервере).
- GET `/api/entities/Task?page=2` → 5 строк #1..#5 (движок ПАГИНИРОВАТЬ умеет).
Корень: (1) `engine.ts` DEFAULT_LIMIT=50/MAX_LIMIT=200, listRecords клампит `Number(limit) || DEFAULT_LIMIT`→50 (engine.ts:38-39,187-189); (2) `use-entity.ts` грузит коллекцию ОДНИМ `entities[name].list(params)` (params дословно), без `limit`/`page`/`offset` и без fetch-next — комментарий «client-side paging happen in <DataTable>, not here»; (3) `crud-resource.tsx` mergedParams инжектит только `expand`, без дефолтного limit; DataTable получает `rows={data.rows}` (уже загруженные ≤50) и пагинирует их В ПАМЯТИ через `pageSize` (crud-resource.tsx:177-184,549,555) — никогда не запрашивает серверную страницу; (4) SYSTEM_PROMPT.md перечисляет `list({…limit,page})` (L71), но не велит wire-ить пагинацию, а CrudResource — «fast path for any list screen» → 50-кап = дефолт почти всех генеримых списков. Хуже мёртвой кнопки: DataTable рисует «5 страниц по 10» → список ВЫГЛЯДИТ полным, ~10% данных недостижимо без ошибки/индикации.

**Почему не фикшу вслепую:**
1. **TEMPLATE-change → base-rebuild.** Правка `use-entity.ts`/`crud-resource.tsx`/`engine.ts` требует пересборки base-образа `omnia-template-nextjs-entities:dev` на проде (git pull недостаточно) + regen-verify по нишам (verify можно без генерации — throwaway-контейнер из пересобранного base + повтор runtime-репро, как сам этот прогон).
2. **Band-aid лишь двигает обрыв.** Дефолтить managed-fetch в `limit: 200` (MAX_LIMIT) детерминированно и просто, НО (а) >200 записей всё равно исчезают; (б) каждый managed-экран начнёт грузить 200 строк + их `_expanded` ref-расширения на каждый рендер — реальная нагрузка, не замерена; меняет дефолт для ВСЕХ будущих аппов.
3. **Правильный фикс мульти-поверхностный.** Реальная server-pagination: движок возвращает total/has-more (сейчас не возвращает — `listRecords` отдаёт голый массив), `useEntity` держит page-state и рефетчит, DataTable-пейджер драйвит её вместо in-memory нарезки. Меняет контракт data-flow списка. Крупно/рискованно.

**План безопасного фикса (для отдельного прогона/согласования):**
1. Решить: (A) минимальный — managed-fetch дефолтит `limit: 200`, закрывает подавляющее большинство малых CRM (<200 записей) детерминированно; ИЛИ (B) полноценная server-pagination (engine total + useEntity page-state + DataTable refetch).
2. Для (B): `listRecords` вернуть `{rows,total}` (или заголовок), `useEntity` экспонировать `page`/`setPage`/`hasMore`, DataTable-пейджер триггерит reload.
3. Health-check БЕЗ генерации: пересобрать base, поднять throwaway omnia-dev-* со starter Task, засеять 55+ строк → managed-список достаёт ВСЕ (limit-bump: 55/55 ≤200; pagination: страница 2 догружает #1-#5), DataTable-пейджер ходит по полному набору.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_list_pagination_cap.py` — `test_engine_caps_list_at_fifty_by_default_today` + `test_use_entity_loads_once_with_no_limit_or_page_today` + `test_crud_resource_does_not_request_a_limit_or_paginate_server_side_today` + `test_system_prompt_does_not_require_wiring_pagination_today` (4 green evidence) + `test_managed_list_should_reach_records_beyond_the_default_cap` (xfail strict=False — XPASS, когда managed-fetch дефолтит MAX_LIMIT ИЛИ появится реальная server-pagination).

## PROPOSAL P-REFINTEGRITY — entity-движок должен иметь референциальную целостность (2026-06-16, run #16)

**Проблема (доказана LIVE RUNTIME + кодом, BS-19):** entity-движок не имеет НИКАКОЙ референциальной целостности — родителя можно удалить, оставив каждого ребёнка с мёртвой ссылкой, и UI молча резолвит её в null без ошибки/предупреждения. Live-репро на DEPLOYED base-образе `omnia-template-nextjs-entities:dev` (throwaway omnia-dev-dogfood-refdel-probe-41de56, 2 сущности через docker cp: Client{name,phone}, Note{text, clientId:reference→Client}, owner-auth через Auth.js — без LLM, без генерации):
- CREATE Client «Ирина Петрова» → 200 (id b966bf5a…).
- CREATE Note {clientId: b966bf5a…} → 200 (резолвится до удаления).
- CREATE Note {clientId: "00000000-0000-…-000000000000"} → **201** — bogus-ref ПРИНЯТ (нет валидации на write).
- DELETE Client b966bf5a… → **200** — нет restrict / нет warn.
- GET Client list → [] (родитель снесён).
- GET Note?expand=clientId ПОСЛЕ удаления → обе ноты: `clientId` хранит мёртвый id, `_expanded.clientId=null` = ОРФАН (мёртвая ссылка молча обнулена).

Корень (три поверхности, код-доказан): (1) `createRecord` (engine.ts:217-226) только zod-парсит body + `insert`, не грузит target-сущность для проверки существования referenced-строки; (2) `deleteRecord` (engine.ts:295-315) удаляет по id, возвращает `{deleted:true}`, не сканит детей, нет restrict/cascade/warn; (3) `expandRecords` (engine.ts:143-148) `_expanded[field]=map.get(refId) ?? null` — мёртвый родитель неотличим от «связь не задана» → silent «—». Класс шире CRM: заказ→клиент, бронь→пациент, задача→проект, комментарий→пост.

**Почему не фикшу вслепую:** референциальная целостность — это СЕМАНТИЧЕСКОЕ решение политики, меняющее write/delete-поведение КАЖДОГО генеримого аппа, и правильная политика реально неоднозначна:
1. **RESTRICT** — блокировать delete, пока существуют дети, → 409. Безопасно для данных, но ломает «просто удалить» UX и требует сканить ВСЕ сущности на reference-поля, целящие в эту (граф связей), на каждый delete.
2. **CASCADE** — удалять детей вместе. Деструктивно, легко снести больше, чем юзер ожидал.
3. **SOFT/WARN** — вернуть affected-children count и требовать confirm. Кросс-зонно (apps/web + CrudResource confirm-UX).
Все варианты мульти-поверхностные (engine + apps/web), требуют base-rebuild + regen-verify, и data-integrity-чувствительны (сейчас тихая порча данных). Плюс отдельный пробел: нет валидации ref на write (bogus-ref принимается). Max один фикс/прогон, не блайнд-шип.

**План безопасного фикса (для отдельного прогона/согласования):**
1. Выбрать политику delete (RESTRICT по умолчанию безопаснее всего для CRM; CASCADE опционально per-relation; WARN как UX-надстройка). Для RESTRICT — построить обратный индекс «кто ссылается на эту сущность» из всех `entities/*.json` reference-полей, на delete запросить детей → при наличии 409 с affected-count.
2. Валидация ref на write: createRecord/updateRecord — для reference-поля проверить, что target-строка существует и доступна (scope-aware), иначе 400.
3. `expandRecords` — отличать «ref задан, но мёртв» (флаг dangling) от «ref не задан», чтобы UI мог показать «удалён» вместо «—» (опционально).
4. Health-check БЕЗ генерации: пересобрать base, throwaway omnia-dev-* с Client+Note, DELETE Client с живой Note → restrict 409 (или cascade удаляет Note); write с bogus-ref → 400.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_entity_referential_integrity.py` — `test_create_does_not_validate_reference_targets_today` + `test_delete_is_unconditional_with_no_child_scan_today` + `test_expand_silently_nulls_a_dead_reference_today` (3 green evidence) + `test_deleting_a_referenced_record_should_not_silently_orphan_children` (xfail strict=False — XPASS, когда движок станет референциально-осведомлённым на delete).

## PROPOSAL P-SEARCH — entity-апп должен уметь НАЙТИ запись при масштабе (2026-06-16, run #17)

**Проблема (доказана LIVE RUNTIME + кодом, BS-20):** в сгенерённом entity-аппе нельзя НАЙТИ запись, как только коллекция перерастает загруженную страницу — поисковик ложно сообщает «Ничего не найдено». Live-репро на DEPLOYED base-образе `omnia-template-nextjs-entities:dev` (throwaway omnia-dev-dogfood-search-probe-972063, starter `Task`, 55 записей, owner-auth Auth.js — без LLM, без генерации):
- Сид: 55 Task, OLDEST = «Zebra-UNIQUE-OMEGA-001» (заметка «найди-меня-секрет»).
- GET без params (ровно то, что шлёт useEntity) → 50 строк, MARK ОТСУТСТВУЕТ (дефолтный sort created_at DESC роняет старейшую за 50-кап).
- GET `?title=Zebra-UNIQUE-OMEGA-001` (точное полное) → 1 (запись СУЩЕСТВУЕТ, движок точное равенство умеет).
- GET `?title=Zebra` (префикс) → 0; `?title=UNIQUE` (инфикс) → 0; `?title=zebra-…` (неверный регистр) → 0 (нет ILIKE); `?notes=секрет` (substring) → 0.
- CONTROL: `?priority=high` (точное enum) → 1; `?priority=hi` (префикс) → 0 — механизм фильтра РАБОТАЕТ, ломается ровно substring/case.
- GET `?limit=200` → 55, MARK ПРИСУТСТВУЕТ (запись реально в БД).

Корень (два слоя, код-доказан): (1) `listRecords` фильтр = `data->>field = value` чистое точное равенство (engine.ts:176); нет ILIKE/substring/`%`/case-insensitive нигде в движке → нет server-side text-search примитива, к которому поисковик мог бы обратиться. (2) `<DataTable>` поиск = `String(v).toLowerCase().includes(q)` по `rows`-пропу в памяти (data-table.tsx:299), а rows = ≤50 загруженных одним `useEntity.list()` без limit (корень BS-18; use-entity.ts: «search/sort/paging happen in <DataTable>, not here»). Поиск видит только новейшие 50; что старше — невидимо, и серверного эндпойнта дотянуться нет.

**Почему не фикшу вслепую:**
1. **TEMPLATE + engine change → base-rebuild.** Правка `engine.ts` (новый search-примитив) + `use-entity.ts`/`crud-resource.tsx` (проталкивание query) требует пересборки base-образа `omnia-template-nextjs-entities:dev` на проде (git pull недостаточно) + regen-verify по нишам (verify можно без генерации — throwaway-контейнер из пересобранного base + повтор runtime-репро, как сам этот прогон).
2. **Меняет list-data-flow контракт всех аппов.** Сейчас список «загрузи раз → фильтруй в памяти». Серверный поиск = useEntity держит search-state и рефетчит (debounced), DataTable шлёт query на сервер вместо in-memory `.includes()`. Многоповерхностно, как и P-PAGINATE — и логично делать ВМЕСТЕ с пагинацией (один рефактор list-data-flow).
3. **Дизайн примитива неоднозначен.** `q`-параметр ILIKE по ВСЕМ text/string-полям (просто, но может матчить лишнее/медленно без индекса) vs per-field `field~=value` (точнее, но writer должен указать searchable-поля). Требует решения + проверки на отсутствие SQL-инъекции (drizzle-параметризация) и регресса фильтр-вкладок (exact-equality сейчас используется quick-filter'ами — нельзя сломать).

**План безопасного фикса (для отдельного прогона/согласования):**
1. Движок: добавить text-search примитив — `q`-параметр, делающий `data->>field ILIKE '%'||$q||'%'` по text/string-полям сущности (через drizzle-параметры, не конкатенацию строк); не трогать существующий exact-equality фильтр (его используют quick-filter-вкладки).
2. useEntity/CrudResource: проталкивать запрос поискового бокса на сервер (debounced) — `list({ q })` — вместо/в дополнение к in-memory `.includes()`; согласовать с server-pagination (P-PAGINATE) как единый list-data-flow рефактор.
3. Health-check БЕЗ генерации: пересобрать base, throwaway omnia-dev-* со starter Task, засеять 55+ строк где старейшая имеет уникальный title → поиск по substring находит её (server-side), не «Ничего не найдено».

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_entity_search_findability.py` — `test_engine_list_filter_is_exact_equality_only_today` + `test_engine_has_no_substring_or_case_insensitive_search_today` + `test_managed_search_box_is_client_side_over_loaded_rows_today` + `test_use_entity_punts_search_to_the_client_table_today` (4 green evidence) + `test_entity_app_should_be_able_to_find_a_record_by_search` (xfail strict=False — XPASS, когда движок получит server-side text-search И managed read-path начнёт проталкивать query на сервер).

## PROPOSAL P-SORT — сортировка колонки должна упорядочивать весь набор, не загруженное окно (2026-06-16, run #18)

**Проблема (доказана LIVE RUNTIME + кодом, BS-21):** клик по заголовку колонки («сортируй по цене / сроку / сумме») упорядочивает только загруженные ~50 строк в памяти, поэтому глобальный экстремум вне этого окна никогда не попадает наверх — пользователь видит уверенно НЕВЕРНЫЙ топ. Live-репро на DEPLOYED base-образе `omnia-template-nextjs-entities:dev` (throwaway omnia-dev-dogfood-sortcap-probe-1bb521 port 3302, starter `Task` + встроенное `due` date-поле, owner-auth Auth.js — без LLM, без генерации):
- Сид: 55 Task, OLDEST-created = «AAA-EARLIEST-DUE-MARK» due **2018-01-01** (глобальный минимум), 54 филлера due 2026-*.
- GET без params (ровно то, что шлёт useEntity) → 50 строк, MARK ОТСУТСТВУЕТ; минимальный `due` в окне = **2026-01-15** → лучшее, что покажет клиентская «сортировка по due ↑» как ТОП.
- GET `?sort=due&order=asc&limit=200` (server-sort, весь набор) → ТОП = MARK, due **2018-01-01**.
- GET `?sort=due&order=asc` (server-sort, дефолтный 50-кап) → ТОП = MARK, due **2018-01-01**, MARK ПРИСУТСТВУЕТ — сервер сортирует ВЕСЬ набор ПОТОМ берёт топ-50, т.е. проброс sort на сервер чинит даже при капе.
- CONTROL `?limit=200` → 55, MARK есть (запись реально в БД).

Корень (два слоя, код-доказан): (1) движок ИМЕЕТ корректную типизированную server-side сортировку — `listRecords` читает `sort`/`order` (engine.ts:180-181), `fieldExpr` кастит jsonb `::numeric`/`::timestamptz` (engine.ts:74-75) → весь набор упорядочивается правильно. (2) Managed-UI её НЕ зовёт: CrudResource делает каждую колонку сортируемой по умолчанию (crud-resource.tsx:168), клик → `toggleSort` ставит клиентский стейт (data-table.tsx:363), таблица пересортировывает `rows`-проп в памяти (data-table.tsx:305-307); rows = ≤50 от одного `useEntity.list()` без `sort`/`order` (use-entity.ts:21 «search/sort/paging happen in <DataTable>, not here»).

**Почему не фикшу вслепую:**
1. **TEMPLATE change → base-rebuild + regen-verify.** Правка `crud-resource.tsx`/`use-entity.ts`/`data-table.tsx` (проброс sort-стейта в server-params) требует пересборки base-образа на проде. Live-gen сейчас WALLET-blocked → regen-verify этим прогоном недостижим (как BS-16/#13).
2. **Меняет list-data-flow контракт всех аппов.** Клик по заголовку теперь = серверный фетч (loading-стейт, дебаунс) вместо in-memory сорта. Тесно связан с BS-18 (P-PAGINATE) и BS-20 (P-SEARCH) — то же клиентское ≤50-окно; чинить сортировку отдельно, пока поиск/пагинация остаются клиентскими, непоследовательно. Логично landed вместе с P-PAGINATE как единый read-path рефактор.
3. **Малый, но не блайнд.** В отличие от P-SEARCH (нужен НОВЫЙ engine-примитив), здесь примитив уже есть и работает даже при капе — фикс «всего лишь» провод. Но мультиповерхностный TEMPLATE+regen → один фикс/прогон, не вслепую.

**План безопасного фикса (для отдельного прогона/согласования):**
1. CrudResource/DataTable: поднять выбранный sort-ключ/направление наверх (callback `onSortChange` или lifted state), CrudResource добавляет `{ sort, order }` в server-params useEntity.
2. useEntity: держать sort-state и рефетчить при изменении (`list({ sort, order })`), переиспользуя готовую engine-сортировку; согласовать с server-pagination (P-PAGINATE) как единый рефактор.
3. Health-check БЕЗ генерации: пересобрать base, throwaway omnia-dev-* со starter Task, засеять 55+ строк где старейшая имеет глобальный экстремум по типизированному полю → клик «сортируй по due ↑» поднимает её наверх (server-side), а не экстремум загруженного окна.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_entity_sort_windowing.py` — `test_engine_has_typed_server_side_sort_today` + `test_managed_sort_is_client_side_over_loaded_rows_today` + `test_crud_resource_makes_every_column_sortable_but_never_pushes_sort_today` + `test_use_entity_defers_sort_to_the_client_table_today` (4 green evidence) + `test_managed_sort_should_order_the_full_dataset` (xfail strict=False — XPASS, когда managed read-path начнёт проталкивать `sort`/`order` на сервер).

## PROPOSAL P-DATECAST (follow-up — write-side ужесточение даты, 2026-06-16, run #19)

**Контекст:** BS-22 crash-proof READ-half ЗАШИПЛЕН (ad0a71b, `safe_to_timestamptz` — список больше не 500-ит на одной кривой дате). Остаётся WRITE-half: валидатор всё ещё принимает любую `Date.parse`-валидную строку («2025», «June 2025») и кладёт её сырой. Последствие после фикса: значение отображается странно («June 2025») и сортируется в NULL-конец, но НЕ крашит. Это косметика/корректность, не denial-of-view.

**Почему НЕ шипнуто вслепую:** ужесточение `zodForField` date (registry.ts:139) с ленивого `Date.parse` на строгий ISO-8601 (`z.iso.date()`/`.datetime()` или normalize-on-write) меняет **write-валидационную семантику КАЖДОГО date-поля во ВСЕХ генеримых аппах** — тот же класс риска, что BS-17 P-CLEAR. Конкретные риски: (1) over-rejection — если LLM-writer или demo-seeder когда-либо эмитят не-строго-ISO даты, апп начнёт 400-ить легитимные записи; (2) normalize-on-write («June 2025»→«2026-06-01T00:00:00Z») ломает exact-equality фильтр по дате (`data->>field=value` сравнивает с сырой строкой формы «2026-03-01», а сохранена будет полная ISO). Нужна проверка форматов writer/seeder + согласование с фильтр-контрактом → не вслепую.

**План безопасного фикса (для отдельного прогона/согласования):**
1. Решить семантику: REJECT не-ISO на write (fail-loud 400) ИЛИ normalize-to-ISO (и тогда чинить фильтр под полную ISO). REJECT проще и не трогает фильтр.
2. Заменить ленивый `Date.parse`-refine на строгий ISO-чек, принимающий И `YYYY-MM-DD` (что даёт `<input type=date>`) И полный ISO-datetime.
3. Проверить, что demo-seeder и типичный writer-вывод проходят (не over-reject); health-check на throwaway base со sloppy-датой → 400 на write, а не тихий приём.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_entity_date_cast_crash.py::test_date_write_validator_should_reject_non_iso_dates` (xfail strict=False — XPASS, когда write-валидатор станет строго-ISO).

## RUNS LOG (append-only — одна строка на прогон)
<!-- время MSK | сценарий | stack/gen_mode/модель | design-балл | тупняк | действие | PASS/FAIL -->
- 2026-06-16 19:52 MSK | #20 сценарий-1 (entity-app, НОВЫЙ класс, НИКОГДА не пробованный: МЕДИА/UPLOAD-ИНТЕГРАЦИЯ — раньше мерили только list-data-flow семью create/relation/clear/pagination/ref-integrity/search/sort/date-cast (#6,13,14,15,16,17,18,19), но НИКОГДА — uploadFile/sendEmail/рендер картинок; ортогонально семье windowing; rotation 21 mod 5 = 1; H1 заблокирован PROPOSAL P-H1 → ротация). **Live-gen всё ещё ЗАБЛОКИРОВАН WALLET-EMPTY** (vsegpt balance 5.447765 < 9 — пробил writer-модель deepseek-v4-pro-thinking через gw, 400 «only 5.447765 on account») → LIVE-RUNTIME на DEPLOYED base-образе omnia-template-nextjs-entities:dev (метод runs #14-19) | nextjs_entities (integrations базовый) · live faithful server.ts uploadFile-реплика (node+minio pkg) на omnia-runtime_default, рендер не запускался | дизайн n/a (не рендерил) · ФУНКЦ: **VERIFY HEALTHY** (опровергнута severe-гипотеза) | **VERIFY + NEW BS-23 (low-sev, latent)**: ГИПОТЕЗА была severe — провижер читает `OMNIA_MINIO_*`, прод-стек ставит плоские `MINIO_*` → у всех аппов пустой public_url + нет secret → upload мёртв. ОПРОВЕРГНУТА: провижер крутится в ХОСТ-systemd-процессе оркестратора (не worker, который я сперва ошибочно проверил), его `/opt/omnia/apps/orchestrator/.env` задаёт `OMNIA_MINIO_SECRET_KEY`+`OMNIA_MINIO_PUBLIC_URL`, остальное → корректные дефолты (endpoint omnia-prod-minio:9000 резолвится из omnia-runtime_default, access omnia = реальный). Live-e2e: STEP1 дефолты-без-секрета→`UPLOAD_FAIL AccessDenied`, STEP2 реально-инжектируемый env→`UPLOAD_OK` + внешний curl public URL→**HTTP 200** точные байты; `_integration_env()` спред в env контейнера (provisioner.py:193); kit рендерит плоским `<img>` (без next/image remotePatterns-гочи); sendEmail-stub честно документирован writer-промпту (prompt_builder.py:1248-1249). BS-23 latent: единственный orchestrator `.env` без фолбэка на плоские `MINIO_*` → если дрейфнёт, upload молча умрёт стек-широко без сигнала | NONE (фикса нет — интеграция ЗДОРОВА для текущих провижнов; дефенсив-фолбэк мал+доказан-безопасен но без видимого провала = спекулятивно, ср. BS-10) | PASS (verify: проверил never-probed media/upload класс — оказался здоровым; severe-гипотеза опровергнута по live-e2e доказательству (broken-env AccessDenied vs real-env OK+внешний 200); попутно локализован low-sev latent BS-23 file:line; live-gen WALLET-EMPTY обойдён прямой server.ts-репликой как run #14-19; тест-объект из MinIO удалён, контейнеры --rm)
- 2026-06-16 19:34 MSK | #19 сценарий-1 (entity-app, НОВЫЙ функц-угол: CRASH-НА-КАСТЕ при сортировке — раньше мерили create-persist #6, relation #13, clear #14, pagination #15, ref-integrity #16, search #17, sort-windowing #18, но НИКОГДА — что если значение поля валидно на write, но КРАШИТ read-каст; rotation 20 mod 5 = 0 → H1 заблокирован PROPOSAL P-H1 → ротация; взял неисследованную ось write↔read type-divergence, отличную от семейства windowing #18/#20/#21). **Live-gen всё ещё ЗАБЛОКИРОВАН WALLET-EMPTY** (vsegpt balance 5.447765 < 9 для writer — пробил writer-модель deepseek-v4-pro-thinking напрямую через gw, тот же 400 «Potentially out of budget… only 5.447765») → LIVE-RUNTIME против throwaway-контейнера из DEPLOYED base-образа (баг в базовом engine, одинаков во всех аппах; method runs #14-18) | nextjs_entities (engine+schema базовый) · live-runtime CRUD на starter Task (built-in `due` date-поле), рендер не запускался | дизайн n/a (не рендерил) · ФУНКЦ FAIL→FIXED (sort по дата-колонке с кривым-но-JS-валидным значением 500-ит ВЕСЬ список) | **NEW BS-22 (severe, denial-of-view)**: одна JS-валидная-но-PG-невалидная дата 500-ит весь список при сортировке. Расхождение: write-валидатор date = ленивый `Date.parse` (registry.ts:139) принимает «2025»/«June 2025»/«2025-02-30», sort кастит `::timestamptz` (engine.ts:75) — Postgres строже, отвергает → generic 500 (http.ts:13-19); CrudResource каждая колонка сортируема (BS-21). Доказано: Half-A node Date.parse (11 значений PASS), Half-B PG ::timestamptz (8 тех же FAIL), Half-C live на DEPLOYED (omnia-dev-dogfood-datecast-probe-2fe2f6): POST due="June 2025"→201, ?sort=due asc/desc→**500**, ?due=June 2025 фильтр→200 (находит), CONTROL delete→sort→200, лог `invalid input syntax… "June 2025" at listRecords engine.ts:194`. Фильтр (без каста) безопасен — падает только sort. Хуже BS-21/BS-18 — крашит ВЕСЬ вью opaque-ошибкой | **FIX ad0a71b**: crash-proof `safe_to_timestamptz(text)` STABLE plpgsql→NULL на ошибке (init-db.mjs) + engine.ts date-сорт через обёртку вместо сырого `::timestamptz`; детерм/fail-soft/non-regressive (`safe(valid)===valid::timestamptz`), ZERO контракт-change; deploy=base-rebuild на проде; VERIFY на ПЕРЕСОБРАННОМ base (fresh omnia-dev-dogfood-datecast-verify-95e13b): POST «June 2025»→201, ?sort=due asc→**200** (Beta 2026-01-15, Alpha 2026-03-01, poison→NULL last), desc→200, фильтр→200, лог 0×unexpected-error; test_entity_date_cast_crash.py 5 green + 1 xfail (88 passed/8 xfail в entity-сабсете без регресса; обновил stale-ассерт BS-21 test_entity_sort_windowing.py под новый каст). Write-half оставлен как PROPOSAL P-DATECAST (ужесточение валидатора = смена write-семантики всех аппов, ср. BS-17) | PASS (новая слепая зона в never-tested «crash-on-cast»-оси: func FAIL доказан LIVE на deployed-бинаре (500 + delta-control + лог) + код-root 3 поверхности + локализован file:line; ФИКС доставлен и доказан на ПЕРЕСОБРАННОМ DEPLOYED base end-to-end 500→200 + НЕ-регрессия healthy-сортировки; live-gen WALLET-EMPTY обойдён прямым CRUD как run #14-18)
- 2026-06-16 19:13 MSK | #18 сценарий-4 (free — НОВЫЙ функц-угол: СОРТИРОВКА выросшего списка — раньше мерили create-persist #6, relation-visibility #13, clear #14, list-scale/pagination #15, ref-integrity #16, search/findability #17, но НИКОГДА — упорядочивает ли «сортировка по колонке» весь набор или только загруженное окно; rotation 19 mod 5 = 4 (free), H1+tgbot заблокированы PROPOSAL → по духу дока (run #13-17 искали НОВЫЕ функц-углы) взял неисследованную ось sorting). **Live-gen всё ещё ЗАБЛОКИРОВАН WALLET-EMPTY** (vsegpt balance 5.449685 < 9 для writer — подтверждено live в omnia-prod-gw логах `[VSEGPT] HTTP 400 ... Potentially out of budget ... expected price 9, but you have only 5.449685`) → LIVE-RUNTIME против throwaway-контейнера из DEPLOYED base-образа (баг в базовом engine+kit, одинаков во всех аппах; method runs #14/#15/#16/#17) | nextjs_entities (engine+kit базовый) · live-runtime CRUD на starter Task (55 записей, built-in `due` date-поле), рендер не запускался | дизайн n/a (не рендерил) · ФУНКЦ FAIL (сортировка возвращает экстремум загруженных 50, не всего набора → неверный «топ») | **NEW BS-21**: сортировка выросшего списка возвращает экстремум загруженных ~50, а не всего набора — два слоя: (1) движок ИМЕЕТ корректную типизированную server-side сортировку (`sort`/`order` engine.ts:180-181 + касты `::numeric`/`::timestamptz` engine.ts:74-75); (2) managed-UI её НЕ зовёт — CrudResource каждая колонка сортируема (crud-resource.tsx:168), клик→`toggleSort` клиентский (data-table.tsx:363), сорт `rows`-пропа в памяти (data-table.tsx:305-307), useEntity не пробрасывает sort (use-entity.ts:21). Live на DEPLOYED (omnia-dev-dogfood-sortcap-probe-1bb521 port 3302, 55 Task, OLDEST due 2018-01-01): GET без params=50 MARK отсутствует, min-due в окне=2026-01-15 (лучший клиент-сорт топ), ?sort=due&order=asc&limit=200→TOP MARK 2018-01-01, ?sort=due&order=asc (50)→TOP MARK 2018-01-01 MARK ЕСТЬ (сервер сортирует ВЕСЬ набор потом топ-50 → проброс чинит даже при капе), ?limit=200=55 MARK есть. Отлично от BS-18 (записи скрыты) и BS-20 (нет server-примитива — здесь ЕСТЬ и работает при капе). Хуже тихого обрыва — сортировка возвращает НЕВЕРНУЮ строку, выглядящую авторитетно. Класс шире CRM | PROPOSAL P-SORT (пробросить sort-стейт DataTable в server-параметры useEntity, переиспользуя готовую engine-сортировку; TEMPLATE→base-rebuild+regen-verify, меняет list-data-flow всех аппов, логично с P-PAGINATE; wallet-blocked → не вслепую) + acceptance-lock test_entity_sort_windowing.py (4 green + 1 xfail) | PASS (новая слепая зона в never-tested «sorting»-оси: func FAIL доказан LIVE-RUNTIME на deployed-бинаре (клиент-окно топ=2026-01-15 vs server-sort топ=2018-01-01; server-sort работает даже при 50-капе) + код-root 2 поверхности + локализован file:line + заперт тестом; live-gen WALLET-EMPTY обойдён прямым CRUD к живому контейнеру как run #14/#15/#16/#17)
- 2026-06-16 18:53 MSK | #17 сценарий-1 (CRM/entity-app, НОВЫЙ функц-угол: НАХОДИМОСТЬ / ПОИСК при масштабе — раньше мерили create-persist #6, relation-visibility #13, clear #14, list-scale/pagination #15, ref-integrity #16, но НИКОГДА — может ли юзер НАЙТИ запись через поисковик, когда список вырос; rotation 18 mod 5 = 3 (tgbot), но tgbot заблокирован PROPOSAL P-BS5 + H1 заблокирован → по духу дока (run #13-16 искали НОВЫЕ функц-углы) взял неисследованную ось findability). **Live-gen всё ещё ЗАБЛОКИРОВАН WALLET-EMPTY** (vsegpt баланс 5.447765 < ~9 для writer; balance-call disabled на ключе — пробил writer-модель напрямую через gateway, тот же 400 out-of-budget) → LIVE-RUNTIME против throwaway-контейнера из DEPLOYED base-образа (баг в базовом engine+kit, одинаков во всех аппах; method runs #14/#15/#16) | nextjs_entities (engine+kit базовый) · live-runtime CRUD на starter Task (55 записей), рендер не запускался | дизайн n/a (не рендерил) · ФУНКЦ FAIL (запись за загруженной страницей ненаходима, поиск ложно «Ничего не найдено») | **NEW BS-20**: entity-апп не может НАЙТИ запись при масштабе — два слоя: (1) движок без server-side text-search, фильтр = точное равенство `data->>field = value` (engine.ts:176), нет ILIKE/substring/case; (2) DataTable-поиск чисто клиентский `.includes(q)` по ≤50 загруженным (data-table.tsx:299 + useEntity без limit, корень BS-18). Live на DEPLOYED (omnia-dev-dogfood-search-probe-972063, 55 Task): GET без params=50 MARK отсутствует, ?title=точное-полное=1, ?title=Zebra/UNIQUE(substring)=0, ?title=неверный-регистр=0, CONTROL ?priority=high(точное)=1 / ?priority=hi(префикс)=0, ?limit=200=55 MARK есть → запись в БД, но ненаходима через UI. Отлично от BS-18: починка капа НЕ чинит поиск (некуда проталкивать query). Хуже тихого обрыва — поисковик активно утверждает «нет записи». Класс шире CRM | PROPOSAL P-SEARCH (server-side text-search примитив engine ILIKE/`q` + проталкивание query из useEntity/CrudResource; TEMPLATE+engine, base-rebuild+regen-verify, меняет list-data-flow всех аппов, логично делать с P-PAGINATE → не вслепую) + acceptance-lock test_entity_search_findability.py (4 green + 1 xfail) | PASS (новая слепая зона в never-tested «findability/search»-оси: func FAIL доказан LIVE-RUNTIME на deployed-бинаре (substring=0 / exact=1 / CONTROL filter работает) + код-root 2 поверхности + локализован file:line + заперт тестом; live-gen WALLET-EMPTY обойдён прямым CRUD к живому контейнеру как run #14/#15/#16)
- 2026-06-16 18:37 MSK | #16 сценарий-1 (CRM/entity-app, НОВЫЙ функц-угол: РЕФЕРЕНЦИАЛЬНАЯ ЦЕЛОСТНОСТЬ при DELETE — раньше мерили create-persist run #6, update/clear run #14, list-scale run #15, relation-visibility/picker run #13, но НИКОГДА удаление родителя, на которого ссылаются дети; rotation 17 mod 5 = 2 (SPA), но сценарий-2 закрыт ЗДОРОВЫМ run #8 + H1 заблокирован PROPOSAL → по духу дока (run #13-15 искали НОВЫЕ функц-углы, а не ре-раны) взял неисследованную ось relational-DELETE). Впервые измерено, что происходит с РЕБЁНКОМ, когда удаляешь РОДИТЕЛЯ. **Live-gen всё ещё ЗАБЛОКИРОВАН WALLET-EMPTY** → LIVE-RUNTIME против throwaway-контейнера из DEPLOYED base-образа (баг в базовом engine, одинаков во всех аппах; method runs #14/#15) | nextjs_entities (engine базовый) · live-runtime CRUD (Client+Note через docker cp), рендер не запускался | дизайн n/a (не рендерил) · ФУНКЦ FAIL (родитель удаляется, дети сиротеют молча) | **NEW BS-19**: entity-движок без референциальной целостности — три слоя: write принимает bogus-ref (Note clientId «00000000-…»→201), DELETE без restrict/cascade/warn (DELETE Client→200, Note выжила), expandRecords молча обнуляет мёртвую ссылку (`_expanded.clientId=null` при живом мёртвом `clientId`). Live на DEPLOYED (omnia-dev-dogfood-refdel-probe-41de56): обе ноты осиротели «—», «чья запись?» неотвечаемо. Корень: createRecord engine.ts:217-226 (нет ref-валидации) + deleteRecord engine.ts:295-315 (нет child-scan) + expandRecords engine.ts:143-148 (silent null). Семья BS-13+BS-16; класс шире CRM | PROPOSAL P-REFINTEGRITY (семантическое решение RESTRICT/CASCADE/WARN, мульти-поверхностно engine+apps/web, base-rebuild+regen-verify, data-integrity-чувствительно → не вслепую) + acceptance-lock test_entity_referential_integrity.py (3 green + 1 xfail) | PASS (новая слепая зона в never-tested «relational-DELETE»-оси: func FAIL доказан LIVE-RUNTIME на deployed-бинаре (orphan + bogus-ref-201 + delete-200) + код-root 3 поверхности + локализован file:line + заперт тестом; live-gen WALLET-EMPTY обойдён прямым CRUD к живому контейнеру как run #14/#15)
- 2026-06-16 18:26 MSK | #15 сценарий-1 (CRM/entity-app, НОВЫЙ функц-угол: МАСШТАБ СПИСКА / server-pagination — раньше мерили create-persist run #6, update/clear run #14, relation-visibility run #13, но НЕ что происходит, когда список перерастает дефолтный кап; rotation 16 mod 5 = 1; H1 заблокирован PROPOSAL → ротация). Впервые измерено ПОВЕДЕНИЕ РАСТУЩЕГО СПИСКА (>50 записей), а не stack-эскалация/auth/runtime/anchors/lead-sink/relation/clear. **Live-gen всё ещё ЗАБЛОКИРОВАН WALLET-EMPTY** (balance-call disabled на ключе; баланс ~5.45 < ~9 нужных для writer как run #12-14) → пивот на LIVE-RUNTIME против throwaway-контейнера из DEPLOYED base-образа (генерация не нужна: кап в базовом engine+kit, одинаков во всех аппах) | nextjs_entities (engine+kit базовый) · live-runtime CRUD на starter Task, рендер не запускался | дизайн n/a (не рендерил) · ФУНКЦ FAIL (overflow недостижим) | **NEW BS-18**: managed-список молча обрезан на 50 — нет server-pagination, записи 51+ недостижимы через UI. Live на DEPLOYED (omnia-dev-dogfood-listcap-probe-cd7c7c): 55 Task → GET без limit = 50 (#6..#55, старейшие #1-#5 пропали), `?limit=200`=55, `?page=2`=5 (движок умеет, UI не просит). Корень: useEntity грузит 1 раз без limit/page (use-entity.ts) + engine DEFAULT_LIMIT=50 (engine.ts:38,187) + DataTable пагинирует in-memory ≤50 (crud-resource.tsx:549,555) → «5 страниц по 10» выглядит полным. Класс шире CRM: любой растущий список; семья BS-14+BS-16 | PROPOSAL P-PAGINATE (TEMPLATE→base-rebuild; band-aid limit:200 двигает обрыв+грузит 200×ref, правильный = server-pagination мульти-поверхностно → не вслепую) + acceptance-lock test_list_pagination_cap.py (4 green + 1 xfail) | PASS (новая слепая зона в never-tested «list scale»-оси: func FAIL доказан LIVE-RUNTIME на deployed-бинаре #1-#5 dropped + код-root 4 поверхности + локализован file:line + заперт тестом; live-gen WALLET-EMPTY обойдён прямым CRUD к живому контейнеру как run #14)
- 2026-06-16 18:09 MSK | #14 сценарий-1 (CRM/entity-app, НОВЫЙ функц-угол: ЦИКЛ РЕДАКТИРОВАНИЯ/UPDATE — раньше проверяли лишь create-persist run #6; rotation 15 mod 5 = 0 → H1 заблокирован PROPOSAL P-H1 → ротация → 1). Впервые измерена операция UPDATE и в частности ОЧИСТКА поля (un-set), а не stack-эскалация/auth/runtime/anchors/lead-sink/relation-visibility. **WALLET ВЕРНУЛСЯ частично, но live-gen всё равно ЗАБЛОКИРОВАН**: tiny deepseek-chat max_tokens=5 probe прошёл (почти бесплатно), но реальная AD/writer-генерация (deepseek-v4-pro-thinking) 400-ит — `Potentially out of budget: …expected price 9, but you have only 5.447765 on account` (баланс даже ниже run #12/#13 5.4497). 2 retry → brief chars=0 → writer rate-limit/out-of-budget → пивот на TEMPLATE-source + LIVE-RUNTIME против уже-живого контейнера (генерация не нужна: баг в базовом engine, одинаков во всех аппах) | nextjs_entities (engine базовый) · live-runtime CRUD + template-source, рендер не запускался | дизайн n/a (не рендерил) · ФУНКЦ: change PASS / clear FAIL | **NEW BS-17**: редактирование НЕ может очистить опц. поле — molчаливый no-op с ложным success. Live на DEPLOYED: CREATE notes+due → PUT {title,priority} (точный payload формы при очистке) → 200 + updated_at сдвинулся → GET: notes/due ВЫЖИЛИ (CLEAR_BUG=True); CONTROL смена на новые значения применяется (CHANGE_WORKS=True). Корень: form omit-ит пустые (entity-form.tsx:158-164) + updateSchema `.optional()` не `.nullable()` → null невыразим (registry.ts:176-182) + engine shallow-merge keeps-absent (engine.ts:286) | PROPOSAL P-CLEAR (мульти-поверхностный: nullable updateSchema + form-emit-clear в edit + type-edge + base-rebuild; меняет write-validation семантику всех аппов → не вслепую) + acceptance-lock test_entity_field_clear_gap.py (3 green + 1 xfail) | PASS (новая слепая зона в never-tested UPDATE-цикле: func FAIL доказан LIVE-RUNTIME на deployed-бинаре + 3-поверхностный код-root + локализован file:line + заперт тестом; CONTROL опровергает «edit полностью сломан» — ломается ровно un-set; live-gen WALLET-EMPTY обойдён прямым CRUD к живому контейнеру)
- 2026-06-16 17:51 MSK | #13 сценарий-1 (CRM/entity-app, НОВЫЙ функц-угол: РЕЛЯЦИОННАЯ ЮЗАБИЛИТИ — резолвятся ли связи в UI; rotation 14 mod 5 = 4 → но H1 заблокирован PROPOSAL и сценарий-4 закрыт run #11/#12 → проверил неисследованную ось relational-CRM на entity-пути). Впервые измерена ДОСТИЖИМОСТЬ/ВИДИМОСТЬ СВЯЗАННЫХ записей в entity-аппе (а не stack-эскалация/auth/runtime/anchors/lead-sink). Live-gen ЗАБЛОКИРОВАН WALLET-EMPTY (vsegpt HTTP 400 «only 5.449685 on account») → пивот на артефакт-анализ repo_svc.read_files 2 РЕАЛЬНЫХ песочниц (crm-verify-2a9337, dent-crm-bdc3f3) + template-source | nextjs_entities (оба) · артефакт-анализ, рендер не запускался | дизайн n/a (не рендерил) · ФУНКЦ: picker PASS / list-видимость FAIL | **VERIFY+NEW**: (1) опроверг гипотезу «relation = UUID-текстбокс» — writer в ОБОИХ аппах корректно мапит schema-reference→`kind:"reference"`+`refEntity`, EntityForm грузит `entities[refEntity].list({limit:200})`→реальный `<Select>` picker (ЗДОРОВО). (2) **NEW BS-16**: reference-поле невидимо в СПИСКЕ — CrudResource авто-расширяет ref-данные (crud-resource.tsx:171-184, `row._expanded` грузится) но НЕ авто-рендерит колонку (:168 колонки дословно), SYSTEM_PROMPT не требует → writer-вариативность: dent-crm рендерит `_expanded.patientId.name`✓, а crm-verify bookings И notes ОБА дропнули clientId-колонку → «чья запись?» не ответить в списке; расширенные данные выброшены | PROPOSAL P-REFCOL (детерм. авто-колонка в CrudResource ИЛИ prompt-директива; template→base-rebuild+regen-verify, сейчас wallet-blocked → не вслепую) + acceptance-lock test_reference_column_surfacing.py (4 green + 1 xfail) | PASS (новая слепая зона: func FAIL доказан кодом+2 живыми артефактами+template-source, локализован file:line, заперт тестом; попутный verify: relation-picker ЗДОРОВ — опровергнуто по доказательству; live-gen WALLET-EMPTY обойдён через repo-storage evidence как run #12)
- 2026-06-16 17:36 MSK | #12 сценарий-4 (free — НОВЫЙ функц-угол: РАБОТАЕТ ЛИ lead-форма; rotation 14 mod 5 = 4). Впервые измерено ПОВЕДЕНИЕ ПРИ ОТПРАВКЕ ФОРМЫ (а не stack-эскалация/auth/runtime/anchors/card-reachability) — прямой замер owner-жалобы «кнопки ничего не делают» на канон-кейсе сайт-визитки. Live-gen dogfood-photostudio-kadr-773864 (mint-token owner 2aa9b1a2) — фотостудия-лендинг с формой обратной связи, skip_clarify=true | blank / freeform / deepseek-v4-pro-thinking · **live-gen ЗАБЛОКИРОВАН WALLET-EMPTY** (vsegpt HTTP 400 «out of budget … only 5.449685 on account» → brief chars=0 → writer 429; snapshot не создан) → пивот на код+2 РЕАЛЬНЫХ артефакта (repo_svc.read_files из прод-хранилища) | дизайн n/a (нет рендера) · ФУНКЦ FAIL | **NEW BS-15**: freeform lead/контакт/подписка-формы = клиентский ТЕАТР — собирают PII (имя/тел/email), показывают «Спасибо, перезвоним», и МОЛЧА выбрасывают (action=0/mailto=0/fetch=0). Доказано: mortgage-форма handler читает name+phone→hide+success (дроп); travel-blog `onsubmit="preventDefault();innerHTML='Спасибо!'"` чистый театр. Корень: промпт санкционирует «formspree-ПЛЕЙСХОЛДЕР» (prompt_builder.py:491-493,606-607) + нет lead-приёмника (public.py GET-only) + find_dead_links слеп к форме без `<a>`. Хуже мёртвой кнопки — ложное подтверждение, владелец = НОЛЬ заявок | PROPOSAL P-LEAD (public lead-receiver+storage+owner-inbox apps/web+writer-POST-директива → кросс-зонно, не вслепую) + acceptance-lock test_lead_capture_sink.py (2 green + 1 xfail) | PASS (новый класс слепой зоны: func FAIL доказан кодом+2 живыми артефактами+архитектурным root (нет POST-эндпойнта), локализован file:line, заперт тестом; live-gen WALLET-EMPTY обойдён через repo-storage evidence)
- 2026-06-16 17:28 MSK | #11 сценарий-4 (free — НОВЫЙ угол: МУЛЬТИ-СТРАНИЧНЫЙ КОНТЕНТ, до сих пор не тестился; rotation 12 mod 5 = 2 → сценарий-2 закрыт ЗДОРОВЫМ run #8 → ротация → 4). Впервые проверена ДОСТИЖИМОСТЬ контента в feed-of-items (а не stack-эскалация/auth/runtime). Live E2E: dogfood-travel-blog-029341 (mint-token owner 2aa9b1a2) — «блог о путешествиях: лента, ОТДЕЛЬНАЯ страница на статью, рубрики+фильтр, об авторе, подписка», skip_clarify=true | blank / freeform / deepseek-v4-pro-thinking · preset=hospitality-escape (discovery не эскалировал — корректно для блога, но архетипа content-collection нет) | дизайн ~7/10 (editorial-luxury по брифу: Instrument Serif+DM Sans, мятный акцент дозой, чистая иерархия; минусы по ui_audit: typography>Malewicz-бюджет, 10 цветов>8, button-heights [36,40,52]; + acceptance struct=False: overflow@360/768/1440 — shipped advisory) · ФУНКЦ FAIL | **NEW BS-14**: мультистатейный блог → 4 из 5 article-card НЕКЛИКАБЕЛЬНЫ (нет `<a>`/href/onclick; Playwright hasLink=False ×4), полный текст лишь у 1 featured (#article-kyoto); «отдельная страница на статью» структурно невыполнима в одностраничной статике; writer НАРУШИЛ свой промпт-запрет «кнопка-обманка без обработчика» (prompt_builder.py:499-500), а `find_dead_links` слеп к карточкам без `<a>` (link_validator.py:24-46). ПОЗИТИВ: категорийный фильтр (JS) + форма подписки работают; картинки 200/490KB у реального юзера (белая полоса = render-env NAT-hairpin, НЕ баг) | PROPOSAL P-BLOG (blog/content-collection-архетип ИЛИ writer-директива «каждая статья достижима» + base-rebuild+regen-verify; детерм. авто-репэйр FP-опасен → не вслепую) + acceptance-lock test_blog_feed_reachability.py (1 green + 1 xfail) | PASS (новый класс слепой зоны: func FAIL доказан live Playwright+source+acceptance, локализован file:line, заперт тестом; ложная гипотеза «broken images» отброшена по доказательству)
- 2026-06-16 16:22 MSK | #10 сценарий-1 (CRM с нуля, skip_clarify=true; rotation 11 mod 5 = 1; H1/BS-5/P-EC/P-AUTHGATE/P-RBAC заблокированы как PROPOSAL → ротация). НОВЫЙ угол: впервые проверена ФУНКЦ-ЦЕЛОСТНОСТЬ entity-аппа НА УРОВНЕ РАНТАЙМА (резолвятся ли вообще сущности), а не только stack-эскалация/auth. Live E2E: dogfood-crm-edit-563092 (mint-token owner 2aa9b1a2) | blank→nextjs_entities / freeform / deepseek-v4-pro-thinking (writer) · preset=local-services (BS-4 эскалация снова ✓) | дизайн ~6.5/10 (чистая иерархия, KPI-карты, хорошие empty-states; но МОНОХРОМ-grey вместо brief-палитры blue #2563EB = BS-6b опять) · ФУНКЦ FAIL→FIXED | **NEW BS-12 (severe, app-killer)**: AD-бриф назвал entity-файлы lowercase-plural (`clients.json`), writer скопировал ДОСЛОВНО, рантайм-реестр резолвит по `entities/<Name>.json` → 404 «unknown entity 'Client'» на КАЖДЫЙ read/write → весь CRM DOA (console `ApiError: unknown entity`, всё пусто, create падал). Вторично NEW BS-13: `CrudResource` detail-панель без слота для child-сущностей → per-client «Заметки/История визитов» из брифа недостижимы (Note-сущность осиротела в билде #1) | **FIX 989a7a8**: `_normalize_entity_filenames` realign filename↔name в container-guard перед commit+hot_reload (детерм/идемпотент/fail-soft); 5 тестов + 43 passed/6 xfail без регресса; deploy api/worker; live-verify DEPLOYED: guard `clients->Client,notes->Note`, новый билд → `Client.json`, `GET /api/entities/Client` 404→401 (резолвится), dashboard рендерит. BS-13 → PROPOSAL P-RELATED (не вслепую) | PASS (severe app-killer найден live по console+source+registry-коду, фикс доставлен и доказан на DEPLOYED-бинаре end-to-end 404→401; 2-я слепая зона локализована file:line)
- 2026-06-16 15:48 MSK | #9 сценарий-4 (free — НОВАЯ ОСЬ: качество ITERATION/surgical-edit на static-лендинге, а не first-build stack-selection; rotation 10 mod 5 = 0 → H1 заблокирован PROPOSAL → ротация → 4). Прямой замер owner-жалобы «генератор ничего не делал» на пути, который ДОЛЖЕН работать. Live E2E: dogfood-bakery-edit-18ca73 (mint-token owner 2aa9b1a2) — first-build «лендинг пекарни» skip_clarify=true, затем 3 follow-up-правки | blank/freeform/deepseek-v4-pro-thinking (build) + surgical/plain/deepseek-chat (edits) · preset restaurant-warm | дизайн ~7.5/10 (тёплая bakery-палитра #B45309, ui_audit 8/10, чистая иерархия) · ФУНКЦ: EDIT-путь PASS | НАХОДКА BS-11: (1) additive+modify правка (новая секция «Цены» 90/130/1600₽ + телефон +7 999 248-13-77) — ПРИЗЕМЛИЛАСЬ идеально, все 6 секций + tel: сохранены → owner-жалоба НЕ воспроизвелась на легит-правках (изолирована к app-ification=H1). (2) removal-правка «убери отзывы» осиротила nav-якорь `href="#reviews"` (id удалён, ссылка осталась ×3) — surgical-путь пропускает dead-link-пасс (messages.py:2586), inline-fixer не чинит anchor-to-missing-id (link_validator.py:112) | FIX 585e65c: `repair_orphaned_anchors_inline` delta-scoped (только id, удалённые этой правкой; детерм/без LLM/без regen), wired в surgical-путь; 3 теста+5 зелёных; deploy api/worker, health 200; live-verify DEPLOYED 3 dead→0 + live E2E «удали секцию» → `orphaned_anchors repaired`, `#process` починен, пред-существ `#reviews` НЕ тронут | PASS (новая слепая зона найдена+локализована+live-репро, фикс доставлен и доказан на DEPLOYED-бинаре; попутно опроверг owner-страх для контент-правок)
- 2026-06-16 15:24 MSK | #7 сценарий-4 (free, реальный prod-промпт «веб-приложение для школы №7: журнал/расписание/ДЗ/новости, кабинеты ученик/родитель/учитель», skip_clarify=true; rotation 8 mod 5 = 3 → tgbot заблокирован P-BS5 → ротация → 4). НОВЫЙ угол: впервые мульти-РОЛЕВОЙ апп (3 актора с разными вью), а не single-owner CRUD | blank→nextjs_entities / freeform / deepseek-v4-pro-thinking · preset education-bright (BS-4 эскалация снова ✓) | дизайн ~5.5/10 (структура чистая, 3-role «Три роли — один интерфейс»-карты; но ПУРЕ-монохром — нишевая палитра брифа #7C2D12/#FEFCE8/#0369A1+Archivo НЕ применена к :root = BS-6b опять; сломанный стат «0P») · ФУНКЦ FAIL | NEW BS-9: мульти-ролевой/RBAC пробел — рантайм знает только owner\|public\|admin (registry.ts:46) + admin\|user (auth.ts:149); AD спроектировал 3 роли, writer схлопнул → 6×public + JournalEntry=owner. Live: anon GET Parent → 200+телефоны родителей (УТЕЧКА персданных); JournalEntry 401/owner → родитель видит ПУСТОЙ журнал (оценки ребёнка недостижимы) | PROPOSAL P-RBAC (per-app роли + relationship-scoped read-own — engine+registry+auth+apps/web+writer, security-sensitive → не вслепую) + acceptance-lock test_entity_rbac_gap.py (4 green + 1 xfail) | PASS (новый класс слепой зоны: func FAIL доказан live+код+screenshot, локализован file:line, заперт тестом)
- 2026-06-16 15:07 MSK | #6 сценарий-1 (CRM с нуля, skip_clarify=true; rotation 6 mod 5 = 1; H1/BS-5/P-EC заблокированы как PROPOSAL → ротация). НОВЫЙ угол: впервые проверена ФУНКЦ-ЛИВНОСТЬ escalated entity-аппа live (auth+CRUD+refs), а не только «эскалировал ли stack» | blank→nextjs_entities / freeform / deepseek-v4-pro (writer) | дизайн ~6/10 (структура чистая; но лендинг — серый дефолт, нет нишевой палитры — BS-6b) | FUNC PASS: auth (register 200→signin 302→session), CRUD персистит (Client 201+list, Note 201+`_expanded.clientId`), CrudResource авто-expand рефов — escalated entity-апп ГЕНУИННО живой (закрывает H1-страх для entity-пути). NEW BS-8: защищённая `(app)`-группа гейтится ТОЛЬКО клиентски → anon GET /dashboard = 200+полный shell (данные НЕ текут, API 401+owner-scope; но «приватный кабинет» сломан + 401→`[]`→пустой апп при истёкшей сессии) | PROPOSAL P-AUTHGATE (edge-safe middleware ИЛИ mandatory server-gate — security+кросс-зонно → не вслепую) + acceptance-lock test_entity_auth_gate.py (2 green + 1 xfail) | PASS (FUNC PASS доказан live; NEW тупняк локализован file:line + live-репро + заперт тестом)
- 2026-06-16 15:27 MSK | #8 сценарий-2 (SPA-tool «калькулятор ипотеки с графиком платежей»; rotation 7 mod 5 = 2 на момент старта; параллельные run #6/#7 приземлились по ходу — relabel #6→#8; H1 уже CONFIRMED+locked → ротация). РЕАЛЬНАЯ E2E-генерация через продукт: mint-token owner 2aa9b1a2 → POST /projects → POST /prompt skip_clarify=true → dogfood-mortgage-calc-tool-db81b7 | freeform-static / template=blank / deepseek-v4-pro-thinking · preset=realestate-premium | дизайн ~7/10 (тёмно-зелёный монохром, рабочие слайдеры + результат-карты + donut-SVG + таблица графика + lead-форма; минусы: donut мелкий, ui_audit=5/10 typography/button-rules/a11y) · ФУНКЦ PASS | BS-10 (low-sev): нет детерминированного static→spa, spa только от LLM-discovery; skip_clarify-путь собирает инструмент freeform-СТАТИКОЙ. НО для калькулятора static+inline-JS = ПОЛНОСТЬЮ рабочий (Playwright: слайдеры пересчитывают 83 311₽→347 430₽, SVG-график, lead-форма; CHANGED=True). Probe: «калькулятор/конвертер» × норм → spa 5/6, 1× ReadTimeout→static | NONE (фикса нет — сценарий ЗДОРОВ: discovery берёт spa корректно, static-инструмент работает; BS-10 записан low-sev, форс static→spa = риск+cost без выигрыша). Verified acceptance: vision-score ADVISORY с V1.6 (acceptance.py:217), score=5 не гейтит — by design, не баг | PASS (verify: проверил подозреваемую слабую зону — оказалась здоровой; evidence: live-gen + Playwright-интеракция + screenshot + discovery-probe + код)
- 2026-06-16 14:44 MSK | #5 сценарий-4 (free, реальный prod-промпт «Магазин цветов «Флора»: каталог+корзина+оформление+отзывы+кабинет», skip_clarify=true; rotation 4 mod 5) | nextjs_entities / freeform-render / deepseek-v4-pro (writer) | дизайн ~4/10 (broken hero img → gold-плейсхолдер; НЕВИДИМАЯ вторичная CTA white-on-white; dark-on-dark low contrast) | BS-6: e-commerce запрос схлопывается в authed-dashboard CRM — каталог за /signin, НЕТ корзины/checkout, хотя бриф их явно назвал (SYSTEM_PROMPT.md:82,96,118 зашивает «/dashboard binding, app not landing»). BS-4-эскалация сама сработала end-to-end ✓ | PROPOSAL P-EC (новый storefront-архетип, крупно+кросс-зонно → не шипить вслепую) + design-нота BS-6b | PASS (func FAIL пойман: entities+routes+CTA+screenshot+system-prompt; тупняк локализован file:line, доказан)
- 2026-06-16 13:02 MSK | #0 H1-репро | n/a (анализ кода + live decide_intent в prod-контейнере, генерация не запускалась) | n/a | H1 CONFIRMED: follow-up app-ification → CHEAP surgical-edit статики, stack-эскалация структурно недостижима (messages.py:626,675); 4/4 формулировки → CHEAP в prod | repro-тест (xfail) + PROPOSAL P-H1 (фикс деструктивный → не шипить вслепую) | PASS (тупняк локализован, доказан)
- 2026-06-16 14:22 MSK | #3 сценарий-1 (CRM с нуля, skip_clarify=true = quiz-путь) | PRE: blank/freeform/deepseek-v4-pro-thinking → POST: nextjs_entities/entity-app | ~8/10 (бриф: emerald+amber, Libre Franklin+Lora, нишевая утилитарность) | BS-4: first-build с пропущенным discovery не эскалирует stack → app-запрос = freeform-статика с мёртвыми кнопками входа (messages.py:706) | FIX 5878059 (на first-build без discovery переиспользовать `_infer_stack_from_text`; deploy api+worker; repro в test_stack_routing.py) — verify: тот же промпт → template flips blank→nextjs_entities, реальный CRM-бриф (clients/bookings/notes + auth-кабинет) | PASS. Сайд-нота: /api/models 500 (model config provider='google' ∉ ModelInfo literal) — отдельный пре-существующий баг, не фиксил (1 фикс/прогон)
- 2026-06-16 13:24 MSK | skipped: guard-3 sequential-lock fresh (_routine/dogfood.lock = 10:20:08Z, age 4 мин < 15) — параллельный прогон #1 в полёте, lock не снимал | n/a | n/a | yielded | SKIP
- 2026-06-16 14:07 MSK | #2 SPA-tool «калькулятор ипотеки с графиком» (rotation 2 mod 5; H1 уже CONFIRMED+locked → ротация) | nextjs_entities / freeform / deepseek-v4-pro-thinking | 7/10 (лендинг чистый монохром; но это лендинг, не калькулятор) | BS-3: «без регистрации» ×3 → LLM-discovery выбрала nextjs_entities → калькулятор за /signin + БД-история; safety-net однонаправленный (discovery.py:838) | fix 04bb7cf (негативный safety-net `_explicit_no_backend`→spa, 3 теста, деплой api/worker, health 200, VETO live-verified) | PASS (func FAIL пойман по скриншоту+логам+DB, фикс доставлен и доказан)
- 2026-06-16 14:46 MSK | #5 сценарий-1 вариант (лендинг автосервиса «ТурбоФикс» — реальная E2E-генерация через normal discovery path, skip_clarify=false, quiz «Лендинг»+«Строгое») | nextjs_entities / freeform / deepseek-v4-pro-thinking · preset=local-services | дизайн 7.5/10 (тёмный+crimson, нишевый automotive, иерархия ок; минусы: crimson-радиал чуть AI-generic, hero тесноват) · ФУНКЦ FAIL | BS-7: консьюмер-букинг «запись на ремонт» форс-эскалирован в auth-gated nextjs_entities, ВСЕ 5 CTA «Записаться» → /signin (клиент обязан зарегаться, чтобы записаться); discovery.py:90,130 — `_BACKEND_SIGNALS` «запись на»/«бронирован» = proof аккаунтов, negative-net ловит только «без регистрации», quiz-pick «Лендинг» без веса | acceptance-lock test_stack_routing.py (3 xfail + 3 evidence, 27 passed/5 xfailed) + PROPOSAL P-BS7 (фикс двунаправленно-рискованный + архитектурный пробел lead-form без клиентского auth → не шипить вслепую) | PASS (новая слепая зона: func FAIL пойман по rendered page.tsx + screenshot + live discovery, локализован, заперт тестом)
- 2026-06-16 14:22 MSK | #4 Telegram-бот «бот для записи в барбершоп» (scenario 3 — выбран при старте, когда RUNS=3; параллельный run #3 приземлился по ходу, сместив rotation; tgbot помечен «ранее orphaned» — проверено) | n/a (live `run_discovery(force_build=True)` в prod-контейнере; генерация не запускалась — стек недостижим by design) | n/a | BS-5: tgbot/api ОРФАНЫ — discovery `_STACKS`/`_SYSTEM`-меню (discovery.py:53,418-450) + `_DISCOVERY_STACK_TO_TEMPLATE` (stack_routing.py:47) без tgbot; запрос бота → `nextjs_entities`/`fullstack` (веб-апп), готовый telegram-bot-aiogram unreachable | acceptance-lock test_stack_routing.py (2 xfail + evidence) + PROPOSAL P-BS5 (фикс крупный: вокаб+routing+backend-preview+token-UX → не шипить вслепую) | PASS (тупняк локализован, live-доказан, заперт тестом)
